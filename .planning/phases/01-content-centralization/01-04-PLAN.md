---
phase: 01-content-centralization
plan: 04
type: execute
wave: 2
depends_on: ["01-02"]
files_modified:
  - src/app/page.tsx
  - src/app/layout.tsx
autonomous: true
requirements: [FOUND-01, SEO-01]
must_haves:
  truths:
    - "The home grid renders from homeGridProjects (6, same order) and skills from src/content/skills — no inline projects/skills literals remain in page.tsx"
    - "The layout JSON-LD ItemList renders from buildItemListSchema() — byte-identical 6-item ItemList, numberOfItems 6, kashmir-fund still absent"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Homepage importing homeGridProjects + skills from centralized content"
      contains: "homeGridProjects"
    - path: "src/app/layout.tsx"
      provides: "Layout JSON-LD ItemList from buildItemListSchema()"
      contains: "buildItemListSchema"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/content/projects + src/content/skills"
      via: "import { homeGridProjects } / import { skills }"
      pattern: "@/content/(projects|skills)"
    - from: "src/app/layout.tsx"
      to: "src/content/seo"
      via: "buildItemListSchema() assigned to projectsListData"
      pattern: "buildItemListSchema"
---

<objective>
Repoint the two render-literal consumers: (1) src/app/page.tsx imports homeGridProjects from @/content/projects and skills from @/content/skills, removing its inline `const projects` (6) and `const skills` (4) literals while keeping accentMap and all JSX/render config in place; (2) src/app/layout.tsx replaces its inline `const projectsListData = {...}` literal with `const projectsListData = buildItemListSchema();`, leaving the rendering `<script>` unchanged.

Purpose: Completes FOUND-01 (home grid + skills + JSON-LD now render from one module) and the JSON-LD half of SEO-01. page.tsx STAYS a client component — it just imports plain data.
Output: modified src/app/page.tsx, src/app/layout.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-content-centralization/01-RESEARCH.md
@.planning/phases/01-content-centralization/01-02-SUMMARY.md

<constraints>
- page.tsx is a client component ("use client"). It STAYS client. A client component can import a plain TS data module fine — do NOT add a directive to the content files, do NOT convert page.tsx to server.
- The home grid currently reads { id, title, description, tech, category } per project. homeGridProjects entries are full Project objects carrying gridTitle/gridDescription/gridTech/gridCategory. Wire the JSX to read the grid* fields (gridTitle, gridDescription, gridTech, gridCategory) so the rendered grid is byte-identical to today. (Alternatively, if Wave 1 named them title/description on a grid-shaped projection — follow whatever 01-02-SUMMARY documents; the rule is: the 6 cards render the EXACT current title/description/tech/category strings in the EXACT current order.)
- KEEP accentMap (page.tsx line 89) in page.tsx — it is render config, not content. Only `projects` and `skills` literals move out.
- layout.tsx: replace ONLY the `const projectsListData = {...}` literal (lines 494-581) with the builder call. Do NOT touch the other 6 JSON-LD blocks (Person, ProfilePage, ProfessionalService, WebSite, FAQPage, WebPage/Speakable) or the `<script ... JSON.stringify(projectsListData) />` render at line 660. The serialized output must be byte-identical (numberOfItems 6, same 6 items, kashmir-fund absent).
- VERBATIM, zero deletions, zero new deps. Match each file's quote style.
</constraints>

<interfaces>
From Wave 1 (confirm exact names in 01-02-SUMMARY):
  @/content/projects -> export const homeGridProjects: Project[]   // 6, order: kashmir-fund, n8n-automation, voice-ai-agent, erp-system, netsuite-integration, cloud-infrastructure
  @/content/skills   -> export const skills: Skill[]               // 4 groups: Frontend(blue), Backend(violet), Cloud & DevOps(emerald), Specializations(amber)
  @/content/seo      -> export function buildItemListSchema()       // returns the exact current projectsListData object

Home grid currently renders (page.tsx ~line 37, must stay byte-identical) 6 cards in order with these title/description/tech/category — these are the GRID copy (distinct from detail/seo copy):
  kashmir-fund "Fund for Azad Kashmir" / "Built in 48 hours..." / ["Next.js 15","React 19","Stripe","Tailwind CSS"] / "Humanitarian"
  ... (the 6 listed in 01-02 interfaces) ...

Skills (page.tsx ~line 82): 4 groups, accentMap stays in page.tsx mapping accent -> {dot,text} tailwind classes.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repoint page.tsx grid + skills to centralized content</name>
  <files>src/app/page.tsx</files>
  <read_first>
    - src/app/page.tsx (lines 1-95 for the literals + imports; AND the JSX that maps `projects` and `skills` further down — find via the `.map(` over projects and over skills to know which field names the cards read)
    - src/content/projects.ts (homeGridProjects export + Project grid* field names)
    - src/content/skills.ts (skills export + Skill field names)
    - .planning/phases/01-content-centralization/01-02-SUMMARY.md (exact projection/field names to import)
  </read_first>
  <action>
    Edit src/app/page.tsx:
    1. Remove the inline `const projects = [ ... ];` (lines 37-80) and `const skills = [ ... ];` (lines 82-87). KEEP `const accentMap` (line 89).
    2. Add imports near the other @/ imports: `import { homeGridProjects } from "@/content/projects";` and `import { skills } from "@/content/skills";`.
    3. Find the JSX that maps over the old `projects` (the project-cards grid). Repoint it to `homeGridProjects`. Update the field reads so each card shows the SAME strings as today: if Project uses grid* field names, read `p.gridTitle`, `p.gridDescription`, `p.gridTech`, `p.gridCategory` (and `p.id` for the link). The card layout/classes/order must not change.
    4. The skills map references `skills` (now imported) — field names (title, accent, items) are unchanged, and accentMap[skill.accent] still resolves. No JSX change needed beyond the literal removal.
    5. Confirm "use client" stays at the top; do not change it.
  </action>
  <verify>
    <automated>npx tsc --noEmit && node scripts/content-diff.mjs && node -e "const t=require('fs').readFileSync('src/app/page.tsx','utf8'); const a=require('assert'); a.ok(t.includes('homeGridProjects'),'imports homeGridProjects'); a.ok(t.includes('@/content/skills'),'imports skills'); a.ok(!/const projects = \[/.test(t),'inline projects removed'); a.ok(!/const skills = \[/.test(t),'inline skills removed'); a.ok(t.includes('accentMap'),'accentMap kept'); a.ok(/use client/.test(t),'still client'); console.log('page.tsx repoint OK');"</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx imports homeGridProjects from "@/content/projects" and skills from "@/content/skills"
    - Inline `const projects = [` and `const skills = [` are GONE; `const accentMap` REMAINS
    - "use client" still present (page stays client)
    - `node scripts/content-diff.mjs` exits 0 (home grid 6 + skills 4 preserved verbatim)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Home grid renders the same 6 cards (same order/copy) from homeGridProjects and skills from the centralized module; accentMap and client directive intact.</done>
</task>

<task type="auto">
  <name>Task 2: Replace layout.tsx ItemList literal with buildItemListSchema()</name>
  <files>src/app/layout.tsx</files>
  <read_first>
    - src/app/layout.tsx lines 494-581 (the projectsListData literal) and line 660 (the `<script ... JSON.stringify(projectsListData) />` render)
    - src/content/seo.ts (buildItemListSchema output shape)
    - .planning/phases/01-content-centralization/baseline.json (itemList target: 6 items, numberOfItems 6, kashmir-fund absent)
  </read_first>
  <action>
    Edit src/app/layout.tsx:
    1. Add import near the top imports: `import { buildItemListSchema } from "@/content/seo";` (match the file's double-quote style).
    2. Replace the entire `const projectsListData = { ...the 6-item ItemList literal... };` (lines 494-581) with a single line: `const projectsListData = buildItemListSchema();`.
    3. Do NOT touch the other 6 JSON-LD blocks or the render at line 660 (`dangerouslySetInnerHTML={{ __html: JSON.stringify(projectsListData) }}`).
    The serialized projectsListData must be byte-identical to before: numberOfItems 6, the same 6 SoftwareApplication items in the same order with the same curated copy/applicationCategory/url/author, kashmir-fund absent.
  </action>
  <verify>
    <automated>npx tsc --noEmit && node scripts/content-diff.mjs && node -e "const t=require('fs').readFileSync('src/app/layout.tsx','utf8'); const a=require('assert'); a.ok(t.includes('buildItemListSchema'),'imports builder'); a.ok(t.includes('const projectsListData = buildItemListSchema()'),'literal replaced with call'); a.ok(t.includes('JSON.stringify(projectsListData)'),'render unchanged'); console.log('layout ItemList repoint OK');"</automated>
  </verify>
  <acceptance_criteria>
    - layout.tsx imports buildItemListSchema from "@/content/seo"
    - `const projectsListData = buildItemListSchema();` replaces the inline 6-item literal (the inline `itemListElement: [` literal for projectsListData is gone)
    - The `<script ... JSON.stringify(projectsListData) />` render is unchanged; other 6 JSON-LD blocks untouched
    - `node scripts/content-diff.mjs` exits 0 (ItemList still 6 items, numberOfItems 6, kashmir-fund absent, curated copy unchanged)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Layout JSON-LD ItemList comes from buildItemListSchema(), byte-identical output, other schemas and render untouched.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean
- `node scripts/content-diff.mjs` exits 0 (home grid 6, skills 4, ItemList 6 all preserved)
- page.tsx stays "use client"; accentMap retained; no inline projects/skills literals
- layout.tsx other 6 schemas + render untouched
</verification>

<success_criteria>
- FOUND-01 complete: home grid, skills, and JSON-LD ItemList all render from src/content — no duplicated literals remain across the four consumers
- SEO-01 (JSON-LD half): ItemList byte-identical (6 items, numberOfItems 6, kashmir-fund absent, curated SEO copy preserved)
- Zero deletions, zero new deps
</success_criteria>

<output>
After completion, create `.planning/phases/01-content-centralization/01-04-SUMMARY.md` noting: the grid field names wired (grid* vs title/description), that page.tsx stayed client + accentMap retained, and confirmation the ItemList serialized output is byte-identical.
</output>
