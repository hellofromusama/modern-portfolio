---
phase: 01-content-centralization
plan: 05
type: execute
wave: 3
depends_on: ["01-03", "01-04"]
files_modified: []
autonomous: false
requirements: [SHIP-01, FIX-03, SEO-01, FOUND-01]
must_haves:
  truths:
    - "npx tsc --noEmit + npm run lint + npm run build all pass"
    - "The build route table prerenders all 7 /projects/<slug> routes"
    - "content-diff.mjs exits 0 (zero deletions across all four consumers)"
    - "Owner confirms both-theme smoke of / and a /projects/<slug> renders correctly with no console errors and unchanged JSON-LD"
  artifacts: []
  key_links:
    - from: "npm run build route table"
      to: "src/content/projects (generateStaticParams)"
      via: "7 prerendered /projects/* routes"
      pattern: "/projects/"
---

<objective>
Run the SHIP-01 phase verification gate that proves the centralization preserved everything: the full compiler/lint/build suite, the zero-deletion diff, a build-output route check confirming all 7 project routes prerender, and an owner both-theme manual smoke + JSON-LD Rich Results check.

Purpose: build alone passes with type/lint errors ignored (next.config ignores both) — so this gate runs tsc + lint explicitly and verifies the SEO/route fidelity that the project's #1 risk (silent content loss) threatens. This plan has a human checkpoint because both-theme rendering and Rich Results have no E2E framework.
Output: no source files; a verification record in the summary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-content-centralization/01-VALIDATION.md
@.planning/phases/01-content-centralization/01-01-SUMMARY.md
@.planning/phases/01-content-centralization/01-02-SUMMARY.md
@.planning/phases/01-content-centralization/01-03-SUMMARY.md
@.planning/phases/01-content-centralization/01-04-SUMMARY.md

<constraints>
- next.config.ts has typescript.ignoreBuildErrors + eslint.ignoreDuringBuilds = true. A green `next build` proves NOTHING about types/lint. Run `npx tsc --noEmit` and `npm run lint` EXPLICITLY and require them green.
- node_modules may be absent — run `npm ci` first if needed.
- Do NOT modify any source file in this plan. If the gate fails, STOP and report which check failed — a fix is a follow-up edit to the relevant Wave 1/2 plan's files, not a silent patch here.
- Both-theme smoke and Rich Results are owner-verified (checkpoint). Automate everything up to that point.
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full automated verification suite + 7-route build check</name>
  <files></files>
  <read_first>
    - .planning/phases/01-content-centralization/01-VALIDATION.md (Full suite command + Wave 0 build-output route check for all 7 slugs)
    - .planning/phases/01-content-centralization/01-01-SUMMARY.md (exact content-diff.mjs invocation)
  </read_first>
  <action>
    Run, in order, and capture output:
    1. `npm ci` (only if node_modules is missing; otherwise skip).
    2. `npx tsc --noEmit` — must exit 0.
    3. `npm run lint` — must exit 0 (or only pre-existing warnings unrelated to src/content; new errors fail the gate).
    4. `npm run build` — must complete; capture the route table.
    5. From the build output, confirm all 7 project routes are present/prerendered: /projects/kashmir-fund, /projects/n8n-automation, /projects/voice-ai-agent, /projects/erp-system, /projects/netsuite-integration, /projects/modern-portfolio, /projects/cloud-infrastructure. (generateStaticParams should mark them prerendered; at minimum all 7 must appear in the route table.)
    6. `node scripts/content-diff.mjs` — must exit 0 (zero deletions across home grid 6, detail 7, ItemList 6, sitemap 8, skills 4).
    7. Grep the post-migration source to confirm no inline duplicated literals remain: there is no `const projects = [` in src/app/page.tsx, no `const projects = {` in src/app/projects/[id]/page.tsx, and no inline ItemList `itemListElement: [` for projectsListData in src/app/layout.tsx.
    Record each result.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run build 2>&1 | node -e "let s='';process.stdin.on('data',d=>s+=d);process.stdin.on('end',()=>{const slugs=['kashmir-fund','n8n-automation','voice-ai-agent','erp-system','netsuite-integration','modern-portfolio','cloud-infrastructure'];const missing=slugs.filter(x=>!s.includes('/projects/'+x));if(missing.length){console.error('MISSING ROUTES:',missing);process.exit(1);}console.log('all 7 project routes present');});" && node scripts/content-diff.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `npm run lint` exits 0 (no new errors from src/content or the repointed consumers)
    - `npm run build` completes and the route table contains all 7 /projects/<slug> entries (the automated check above prints "all 7 project routes present")
    - `node scripts/content-diff.mjs` exits 0
    - grep: `const projects = [` absent from src/app/page.tsx; `const projects = {` absent from src/app/projects/[id]/page.tsx
  </acceptance_criteria>
  <done>The full automated gate is green: tsc + lint + build pass, all 7 project routes build, zero-deletion diff passes, no duplicated literals remain.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Owner both-theme smoke + JSON-LD Rich Results check</name>
  <files></files>
  <action>
    This is a human-verification checkpoint. The executor presents the what-built summary and the how-to-verify steps below to the owner, then PAUSES for the resume-signal. Do not modify source. If the owner reports a discrepancy, STOP and report it as a gap against the relevant Wave 1/2 plan (do not silently patch here).
  </action>
  <what-built>
    Content is now centralized in src/content/. The home grid (6 cards), every /projects/<slug> detail page (7), the layout JSON-LD ItemList (6 entities), and the sitemap (8 project URLs) all render from one module. Automated gate (tsc + lint + build + 7-route check + zero-deletion diff) is already green.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000.
    2. Home grid: confirm 6 project cards appear in this order — Fund for Azad Kashmir, N8N Automation Platform, Voice AI Booking Agent, Enterprise ERP System, NetSuite Integration Suite, Cloud Infrastructure Platform. Confirm the 4 skill groups (Frontend, Backend, Cloud & DevOps, Specializations) render with their accent dots.
    3. Toggle the theme (ThemeToggle) to light and back to dark. Confirm the grid + skills render correctly in BOTH themes with no console errors.
    4. Hard-refresh (not client nav) at least 2 detail pages, e.g. http://localhost:3000/projects/modern-portfolio and http://localhost:3000/projects/kashmir-fund. Confirm both load (no 404), show the correct title/subtitle/description, and kashmir-fund shows its live link. Confirm both themes.
    5. View source on http://localhost:3000 → find the ItemList JSON-LD script. Confirm "numberOfItems": 6, exactly 6 items, the curated names (e.g. "Enterprise N8N Automation Platform"), and that kashmir-fund is NOT in the ItemList.
    6. Open http://localhost:3000/sitemap.xml → confirm 8 /projects/ URLs present.
    7. (Optional but recommended) Paste the homepage source or URL into Google Rich Results Test and confirm the ItemList still validates.
  </how-to-verify>
  <verify>
    <automated>MANUAL — owner both-theme smoke + view-source JSON-LD + /sitemap.xml check; no E2E framework (V2-04 defers it). Automated portion covered by Task 1.</automated>
  </verify>
  <resume-signal>Type "approved" if both themes render correctly, all detail pages resolve, the ItemList is unchanged (6 items, no kashmir-fund), and the sitemap has 8 project URLs — or describe any discrepancy.</resume-signal>
  <acceptance_criteria>
    - Owner confirms 6 grid cards in correct order, 4 skill groups, both themes clean (no console errors)
    - Owner confirms 2+ detail pages resolve on hard refresh in both themes (including modern-portfolio and kashmir-fund)
    - Owner confirms ItemList JSON-LD = 6 items, numberOfItems 6, kashmir-fund absent, curated copy intact
    - Owner confirms /sitemap.xml lists 8 project URLs
  </acceptance_criteria>
  <done>Owner approves the both-theme smoke + JSON-LD/sitemap fidelity check; Phase 1 verification gate is complete.</done>
</task>

</tasks>

<verification>
- tsc + lint + build all green (run explicitly — build's ignore flags make it untrustworthy alone)
- All 7 /projects/<slug> routes build; content-diff.mjs exits 0
- Owner both-theme smoke + Rich Results/sitemap check passes
</verification>

<success_criteria>
- SHIP-01 satisfied: tsc + lint + build + both-theme manual smoke pass
- FIX-03 confirmed: all 7 project routes prerender
- SEO-01 + FOUND-01 confirmed: ItemList + sitemap + grid + skills unchanged, single source, zero deletions
</success_criteria>

<output>
After completion, create `.planning/phases/01-content-centralization/01-05-SUMMARY.md` recording the gate results (tsc/lint/build status, 7-route confirmation, diff result, owner approval) — this is the phase's verification record.
</output>
