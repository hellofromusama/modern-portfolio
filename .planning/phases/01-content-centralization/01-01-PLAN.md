---
phase: 01-content-centralization
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - scripts/content-baseline.mjs
  - scripts/content-diff.mjs
  - .planning/phases/01-content-centralization/baseline.json
autonomous: true
requirements: [SEO-01, FOUND-01]
must_haves:
  truths:
    - "A committed baseline.json captures every existing project slug, JSON-LD ItemList entity, sitemap URL, home-grid project, and homepage skill BEFORE any source edit"
    - "Running content-diff.mjs after a deletion exits non-zero; with no deletions it exits 0"
  artifacts:
    - path: "scripts/content-baseline.mjs"
      provides: "One-time snapshot of the four divergent sources to baseline.json"
      min_lines: 40
    - path: "scripts/content-diff.mjs"
      provides: "Zero-deletion gate comparing live sources vs baseline.json"
      min_lines: 40
    - path: ".planning/phases/01-content-centralization/baseline.json"
      provides: "Frozen snapshot of pre-migration outputs"
      contains: "homeGrid"
  key_links:
    - from: "scripts/content-diff.mjs"
      to: ".planning/phases/01-content-centralization/baseline.json"
      via: "readFileSync + JSON.parse + set comparison"
      pattern: "baseline\\.json"
---

<objective>
Establish the zero-deletion validation harness BEFORE any content is moved. Capture the four divergent sources (home grid = 6 projects, detail map = 7 keys, JSON-LD ItemList = 6 curated entities, sitemap = 8 project URLs) plus the homepage skills into a committed baseline.json, and build a diff script that fails on any deletion or modification of an existing entry.

Purpose: The phase's #1 risk is silent content loss across a 6/7/6/8 divergence with no test suite. This plan is the definition of done — every later migration plan re-runs content-diff.mjs to prove nothing was lost.
Output: scripts/content-baseline.mjs, scripts/content-diff.mjs, baseline.json (committed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-content-centralization/01-RESEARCH.md
@.planning/phases/01-content-centralization/01-VALIDATION.md

<constraints>
- ZERO new dependencies. `tsx`/`ts-node` are NOT installed. Do NOT add them. The scripts parse the source `.ts`/`.tsx` files as TEXT (regex extraction), they do NOT import them.
- Scripts are plain Node ESM (.mjs), run with `node scripts/<name>.mjs`. Node v22.22.2 is available.
- baseline.json MUST be captured from the CURRENT (pre-edit) source files. This plan runs in Wave 0 — no content has moved yet, so the live sources ARE the baseline. Generate it now and commit it.
</constraints>

<interfaces>
The four sources to snapshot, with their EXACT current contents (verified line-by-line):

1. Home grid — src/app/page.tsx, `const projects = [` at line 37. 6 ids in order:
   ["kashmir-fund","n8n-automation","voice-ai-agent","erp-system","netsuite-integration","cloud-infrastructure"]

2. Detail map — src/app/projects/[id]/page.tsx, `const projects = {` at line 4. 7 keys in order:
   ["kashmir-fund","n8n-automation","voice-ai-agent","erp-system","netsuite-integration","modern-portfolio","cloud-infrastructure"]
   Only "kashmir-fund" has liveUrl: "https://iamstandingwithkashmir.org".

3. JSON-LD ItemList — src/app/layout.tsx, `const projectsListData = {` at line 494. numberOfItems: 6.
   6 items, each a SoftwareApplication with curated name/description/url/applicationCategory. URLs in order:
   n8n-automation, voice-ai-agent, erp-system, netsuite-integration, cloud-infrastructure, modern-portfolio
   (NOTE: kashmir-fund is ABSENT from ItemList.)

4. Sitemap — src/app/sitemap.ts. 8 project URLs under "// Project pages" in order with priorities:
   n8n-automation(0.8), voice-ai-agent(0.8), erp-system(0.8), netsuite-integration(0.8),
   cloud-infrastructure(0.8), modern-portfolio(0.7), kashmir-fund(0.7)

5. Homepage skills — src/app/page.tsx, `const skills = [` at line 82. 4 groups:
   Frontend(blue), Backend(violet), Cloud & DevOps(emerald), Specializations(amber); 4 items each.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write content-baseline.mjs and generate baseline.json</name>
  <files>scripts/content-baseline.mjs, .planning/phases/01-content-centralization/baseline.json</files>
  <read_first>
    - src/app/page.tsx (lines 37-87: projects + skills arrays)
    - src/app/projects/[id]/page.tsx (lines 4-266: detail map keys)
    - src/app/layout.tsx (lines 494-581: projectsListData ItemList)
    - src/app/sitemap.ts (lines 83-126: project URLs + priorities)
    - .planning/phases/01-content-centralization/01-VALIDATION.md (Wave 0 Requirements section)
  </read_first>
  <action>
    Create scripts/content-baseline.mjs as a Node ESM script that reads the four source files as TEXT (fs.readFileSync, no import of .ts/.tsx) and extracts five projections into an object, then writes it to .planning/phases/01-content-centralization/baseline.json with JSON.stringify(obj, null, 2).

    Extraction approach (regex on text — robust, no TS execution):
    - `homeGrid`: from src/app/page.tsx, slice the `const projects = [ ... ];` block (between `const projects = [` and the first `];` after it), then collect all `id: "<slug>"` matches in order. Expected: exactly 6 ids ["kashmir-fund","n8n-automation","voice-ai-agent","erp-system","netsuite-integration","cloud-infrastructure"].
    - `detailKeys`: from src/app/projects/[id]/page.tsx, slice between `const projects = {` and `export default`, then collect all top-level keys matching /^\s{2}"([a-z0-9-]+)":\s*\{/gm in order. Expected: exactly 7 ["kashmir-fund","n8n-automation","voice-ai-agent","erp-system","netsuite-integration","modern-portfolio","cloud-infrastructure"].
    - `itemList`: from src/app/layout.tsx, slice the `const projectsListData = {` block up to the matching `};`. Capture `numberOfItems` (expected 6) and, for each ListItem, the item object's `name`, `description`, `url`, `applicationCategory` (capture in document order). Store as { numberOfItems, items: [{name, description, url, applicationCategory}, ...] }. Expected 6 items, kashmir-fund absent.
    - `sitemapProjects`: from src/app/sitemap.ts, find every block whose url matches `/projects/<slug>` and capture { slug, priority } in document order. Expected 8: n8n-automation/0.8, voice-ai-agent/0.8, erp-system/0.8, netsuite-integration/0.8, cloud-infrastructure/0.8, modern-portfolio/0.7, kashmir-fund/0.7.
    - `skills`: from src/app/page.tsx, slice the `const skills = [ ... ];` block and capture each group's `title` and its `items` array (the 4 strings) in order. Expected 4 groups: Frontend, Backend, Cloud & DevOps, Specializations.

    After extraction, the script MUST assert the expected counts (6/7/6-items/8/4) and throw with a clear message if any count is wrong (this protects against the source having changed since research). Then write baseline.json.

    Run `node scripts/content-baseline.mjs` to generate baseline.json now, while sources are still pristine.
  </action>
  <verify>
    <automated>node scripts/content-baseline.mjs && node -e "const b=require('./.planning/phases/01-content-centralization/baseline.json'); const a=require('assert'); a.strictEqual(b.homeGrid.length,6); a.strictEqual(b.detailKeys.length,7); a.strictEqual(b.itemList.numberOfItems,6); a.strictEqual(b.itemList.items.length,6); a.strictEqual(b.sitemapProjects.length,8); a.strictEqual(b.skills.length,4); a.ok(!b.itemList.items.some(i=>i.url.includes('kashmir-fund')),'kashmir-fund must be absent from itemList'); a.ok(b.detailKeys.includes('modern-portfolio')); console.log('baseline OK 6/7/6/8/4');"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/content-baseline.mjs exists and runs with exit code 0
    - .planning/phases/01-content-centralization/baseline.json exists
    - baseline.json: homeGrid has exactly 6 ids; detailKeys has exactly 7 keys including "modern-portfolio"; itemList.numberOfItems === 6 and itemList.items.length === 6 with NO url containing "kashmir-fund"; sitemapProjects has exactly 8 entries (modern-portfolio priority 0.7, kashmir-fund priority 0.7, the other five 0.8); skills has exactly 4 groups
    - The script imports nothing from .ts/.tsx (grep -c "import" of project source returns 0 dynamic source imports; it only uses node:fs)
  </acceptance_criteria>
  <done>baseline.json committed with the verified 6/7/6/8/4 snapshot; the script is re-runnable and self-asserting.</done>
</task>

<task type="auto">
  <name>Task 2: Write content-diff.mjs zero-deletion gate</name>
  <files>scripts/content-diff.mjs</files>
  <read_first>
    - scripts/content-baseline.mjs (reuse its extraction logic)
    - .planning/phases/01-content-centralization/baseline.json
    - .planning/phases/01-content-centralization/01-VALIDATION.md (content-diff.mjs requirement: exits non-zero on ANY missing/changed entry; additions allowed)
  </read_first>
  <action>
    Create scripts/content-diff.mjs as a Node ESM script that:
    1. Re-extracts the same five projections from the LIVE source files using the identical regex logic as content-baseline.mjs. To avoid duplication, refactor the extraction into an exported function in content-baseline.mjs (e.g. `export function extractAll()`), and have BOTH the baseline generator and the diff script import it. content-baseline.mjs keeps a top-level guard so running it directly still writes baseline.json, but importing it does not.

       IMPORTANT FORWARD-COMPATIBILITY: After Wave 1+ the literals move OUT of page.tsx/layout.tsx/[id]/page.tsx and INTO src/content/*. The diff script's extraction MUST therefore source each projection from wherever it lives at run time. Make extractAll() resilient: for each projection, try the centralized module file first (src/content/projects.ts, src/content/skills.ts, src/content/seo.ts) and fall back to the original page file if the centralized file does not yet exist. Extract by TEXT regex in both cases (never import .ts). Document this dual-source lookup in a comment.
    2. Loads baseline.json.
    3. Compares each projection as a SET/ordered-list:
       - homeGrid: every baseline id MUST still be present (deletion = fail). Order preserved = warn if changed, fail only on deletion per "additions allowed, deletions forbidden". Phase 1 adds nothing, so any count change down is a failure.
       - detailKeys: every baseline key MUST still resolve (all 7 present).
       - itemList: numberOfItems MUST still be 6; every baseline item (matched by url) MUST still be present with byte-identical name + description + applicationCategory.
       - sitemapProjects: every baseline {slug, priority} MUST still be present and unchanged.
       - skills: every baseline group title + its 4 items MUST still be present unchanged.
    4. Collects all violations into an array. If any violation exists, print each with a clear "DELETED/CHANGED: <what>" line and process.exit(1). Otherwise print "content-diff: zero deletions, all entries preserved" and exit 0.

    Additions (e.g. Phase 2 AI projects later) MUST NOT trigger failure — only missing/changed baseline entries fail.
  </action>
  <verify>
    <automated>node scripts/content-diff.mjs && echo "DIFF_PASS_BASELINE"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/content-diff.mjs` exits 0 against the pristine sources and prints "zero deletions"
    - Negative test passes: temporarily simulate a deletion and confirm exit 1. Run this exact check: `node -e "const{extractAll}=await import('./scripts/content-baseline.mjs'); const b=require('./.planning/phases/01-content-centralization/baseline.json'); console.log('extractAll present:', typeof extractAll==='function')" --input-type=module` returns "extractAll present: true"
    - content-diff.mjs contains a dual-source comment explaining it reads src/content/* first, falls back to src/app/* (so it keeps working after Wave 1 moves literals)
    - Script uses only node:fs / node:path — zero .ts/.tsx imports
  </acceptance_criteria>
  <done>content-diff.mjs exits 0 on pristine code and exit 1 on any deletion; extraction is shared with the baseline script and survives the Wave 1 file move.</done>
</task>

</tasks>

<verification>
- `node scripts/content-baseline.mjs` regenerates baseline.json with counts 6/7/6/8/4
- `node scripts/content-diff.mjs` exits 0 on current (pristine) sources
- Neither script imports any .ts/.tsx file (no new dependency; no tsx/ts-node)
- baseline.json is committed (it is the frozen reference all later plans diff against)
</verification>

<success_criteria>
- baseline.json captures the divergent 6/7/6/8 project sets + 4 skill groups verbatim, BEFORE any migration
- content-diff.mjs is a working zero-deletion gate that later plans (Wave 1, 2a, 2b) run after their edits
- Zero new npm dependencies added
</success_criteria>

<output>
After completion, create `.planning/phases/01-content-centralization/01-01-SUMMARY.md` documenting: the baseline.json shape, the dual-source extraction strategy in content-diff.mjs, and the exact command later plans must run (`node scripts/content-diff.mjs`).
</output>
