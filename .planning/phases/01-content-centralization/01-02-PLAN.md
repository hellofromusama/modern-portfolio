---
phase: 01-content-centralization
plan: 02
type: execute
wave: 1
depends_on: ["01-01"]
files_modified:
  - src/content/types.ts
  - src/content/projects.ts
  - src/content/skills.ts
  - src/content/seo.ts
autonomous: true
requirements: [FOUND-01]
must_haves:
  truths:
    - "A single src/content module stores all 7 projects (superset of grid + detail + JSON-LD + sitemap fields) verbatim"
    - "Named projections reproduce each consumer's exact set/order: homeGridProjects (6), itemList builder (6, no kashmir-fund), sitemapProjects (8 with priorities), detail via getProject (7)"
    - "Homepage skills (4 groups) live in src/content/skills.ts verbatim"
  artifacts:
    - path: "src/content/types.ts"
      provides: "Project + Skill + ProjectSeo interfaces"
      contains: "interface Project"
    - path: "src/content/projects.ts"
      provides: "projects[] (7), getProject, homeGridProjects, sitemapProjects, itemListProjects"
      min_lines: 60
    - path: "src/content/skills.ts"
      provides: "skills[] (4 groups) verbatim"
      contains: "Specializations"
    - path: "src/content/seo.ts"
      provides: "buildItemListSchema() reproducing the layout ItemList byte-for-byte"
      min_lines: 30
  key_links:
    - from: "src/content/projects.ts"
      to: "src/content/types.ts"
      via: "import type { Project }"
      pattern: "from \"\\./types\""
    - from: "src/content/seo.ts"
      to: "src/content/projects.ts"
      via: "reads itemListProjects / getProject seo fields"
      pattern: "from \"\\./projects\""
---

<objective>
Create the single typed content source under src/content/: one `projects` array holding all 7 projects as the superset of every field any consumer needs (grid fields + detail fields + JSON-LD seo fields + sitemap priority), plus named projections that reproduce each consumer's CURRENT exact output, plus the homepage skills array, plus a buildItemListSchema() that emits the layout JSON-LD byte-for-byte.

Purpose: This module is the spine. It does NOT yet change any consumer (that is Wave 2) — it only stores the content and exposes projections. No page imports it yet; this plan is verified by tsc + by the projections matching baseline counts.
Output: src/content/{types.ts, projects.ts, skills.ts, seo.ts}.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-content-centralization/01-RESEARCH.md
@.planning/phases/01-content-centralization/01-01-SUMMARY.md

<constraints>
- VERBATIM. Copy every string literal (titles, descriptions, longDescription, tech arrays, features/challenges/results, JSON-LD curated copy) byte-for-byte from the current source files. Do NOT paraphrase, reword, trim whitespace inside strings, or "clean up". The diff gate fails on any change.
- The four sources DIVERGE (6/7/6/8). Do NOT normalize. Store the 7-project superset and reproduce each consumer's subset via projections. A naive "one array everyone renders" silently changes 3 of 4 outputs — forbidden.
- JSON-LD copy is DISTINCT from grid/detail copy. Carry it as separate fields (seoName, seoDescription, applicationCategory) on each project — do NOT reuse the grid description for the ItemList description.
- Module is pure data — NO "use client", no JSX, no React import. Server- and client-importable.
- Zero new dependencies. Plain TS literals + typed interfaces. No zod.
- Conventions: 2-space indent, semicolons always, trailing commas in multiline literals. Use string-literal unions for constrained values, `<Name>` interfaces. Import alias for cross-tree is @/*; siblings within src/content use relative "./types".
</constraints>

<interfaces>
EXACT current shapes to centralize (verified line-by-line — copy verbatim):

HOME GRID (src/app/page.tsx line 37) — 6 entries, fields { id, title, description, tech[], category }, in order:
  kashmir-fund   | "Fund for Azad Kashmir" | desc "Built in 48 hours after the crisis. Stripe-powered donations with real-time fund tracking so donors can see exactly where money goes." | tech ["Next.js 15","React 19","Stripe","Tailwind CSS"] | "Humanitarian"
  n8n-automation | "N8N Automation Platform" | "A mining company was losing 120 hrs/week to manual data entry across 8 systems. Built automation that cut that by 70% and saved $180K/year." | ["N8N","Node.js","PostgreSQL","Docker"] | "Automation"
  voice-ai-agent | "Voice AI Booking Agent" | "Client was losing 40% of phone bookings to hold times. Built a voice AI using GPT-4 that now handles 500+ bookings/day with zero hold time." | ["OpenAI","Whisper","Node.js","Twilio"] | "AI/ML"
  erp-system     | "Enterprise ERP System" | "Mining company running 5 legacy apps that didn't talk to each other. Unified everything into NetSuite for 200+ users with mobile field access." | ["NetSuite","SuiteScript","React","PostgreSQL"] | "Enterprise"
  netsuite-integration | "NetSuite Integration Suite" | "Retailer needed 12 systems talking to NetSuite in real-time. Built middleware processing 50K+ transactions/day at 99.99% uptime for 18 months." | ["SuiteScript","REST APIs","Redis","Docker"] | "Integration"
  cloud-infrastructure | "Cloud Infrastructure Platform" | "SaaS startup needed to go from 1K to 100K+ concurrent users. Designed the Kubernetes architecture that got them there at 200ms response times." | ["AWS EKS","Docker","Kubernetes","Terraform"] | "DevOps"
  (NOTE: home grid has its OWN title+description per project, DIFFERENT from the detail map's title/description.)

DETAIL MAP (src/app/projects/[id]/page.tsx line 4) — 7 keys in order, rich fields
  { title, subtitle, description, longDescription, tech[], category, timeline, team, features[], challenges[], results[], gradient } + optional liveUrl:
  kashmir-fund, n8n-automation, voice-ai-agent, erp-system, netsuite-integration, modern-portfolio, cloud-infrastructure
  Only kashmir-fund has liveUrl: "https://iamstandingwithkashmir.org".
  (NOTE: home grid does NOT include modern-portfolio; detail map DOES.)

JSON-LD ITEMLIST (src/app/layout.tsx line 494) — numberOfItems 6, 6 SoftwareApplication items in this order with curated SEO copy (DISTINCT from grid/detail):
  1 n8n-automation       | name "Enterprise N8N Automation Platform"   | desc "Workflow automation platform reducing manual processes by 70% and saving $180,000/year for a Perth mining company" | applicationCategory "BusinessApplication"
  2 voice-ai-agent       | "Voice AI Booking Agent"                     | "AI-powered voice agent handling 500+ daily bookings using OpenAI GPT-4 and Whisper, achieving 35% conversion increase" | "BusinessApplication"
  3 erp-system           | "Enterprise ERP System"                      | "Unified NetSuite ERP replacing 5 legacy systems for 200+ mining company users with real-time dashboards and mobile field access" | "BusinessApplication"
  4 netsuite-integration | "NetSuite Integration Suite"                 | "Custom middleware connecting 12 business systems with real-time sync, processing 50,000+ daily transactions at 99.99% uptime" | "BusinessApplication"
  5 cloud-infrastructure | "Cloud Infrastructure Platform"              | "Kubernetes-based microservices architecture supporting 100K+ concurrent users with 99.99% uptime and 200ms global response" | "DeveloperApplication"
  6 modern-portfolio     | "Modern Portfolio Website"                   | "Next.js 15 portfolio with React 19, Tailwind CSS 4, perfect Lighthouse scores, and comprehensive AI SEO optimization" | "WebApplication"
  Each item also: operatingSystem 'Web', author { '@id': 'https://www.usamajaved.com.au/#person' }, url 'https://www.usamajaved.com.au/projects/<id>'.
  Wrapper: '@context':'https://schema.org', '@type':'ItemList', '@id':'https://www.usamajaved.com.au/#projects', name 'Usama Javed Portfolio Projects', description 'Featured enterprise projects by Usama Javed, Senior Full Stack Developer in Perth', numberOfItems 6.
  (NOTE: kashmir-fund ABSENT from ItemList.)

SITEMAP (src/app/sitemap.ts) — 8 project URLs in order with priorities, changeFrequency 'monthly':
  n8n-automation 0.8, voice-ai-agent 0.8, erp-system 0.8, netsuite-integration 0.8, cloud-infrastructure 0.8, modern-portfolio 0.7, kashmir-fund 0.7

SKILLS (src/app/page.tsx line 82) — 4 groups, { title, accent, items[4] }:
  Frontend(blue) ["React / Next.js 15","TypeScript","Tailwind CSS","Vue.js / Nuxt.js"]
  Backend(violet) ["Node.js / Express","Python / Django","PostgreSQL / MongoDB","REST / GraphQL"]
  Cloud & DevOps(emerald) ["AWS / Azure / GCP","Docker / Kubernetes","CI/CD Pipelines","Terraform"]
  Specializations(amber) ["NetSuite SuiteScripts","N8N Automation","AI/ML Integration","ERP Systems"]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define types.ts and the 7-project superset in projects.ts</name>
  <files>src/content/types.ts, src/content/projects.ts</files>
  <read_first>
    - src/app/projects/[id]/page.tsx (the full 7-entry detail object — source of the rich fields, copy verbatim)
    - src/app/page.tsx lines 37-80 (home grid title/description/tech/category — these are SEPARATE from detail copy)
    - src/app/layout.tsx lines 494-581 (curated seo name/description/applicationCategory per project)
    - .planning/phases/01-content-centralization/01-RESEARCH.md (Pattern 2: superset + projections)
  </read_first>
  <action>
    Create src/content/types.ts exporting:
    - `interface Project` — the SUPERSET. Fields: id (string), and the detail fields { title, subtitle, description, longDescription, tech: string[], category, timeline, team, features: string[], challenges: string[], results: string[], gradient } plus optional `liveUrl?: string`. ADD grid-specific fields that differ from detail: `gridTitle: string`, `gridDescription: string`, `gridTech: string[]`, `gridCategory: string` (the home grid has its OWN title/description/tech/category per project — keep them distinct so the grid renders verbatim). ADD seo fields: `seoName?: string`, `seoDescription?: string`, `applicationCategory?: 'BusinessApplication' | 'DeveloperApplication' | 'WebApplication'`. ADD `sitemapPriority?: number`.
    - `interface ProjectSeoItem` if helpful for seo.ts.
    - `interface Skill { title: string; accent: 'blue' | 'violet' | 'emerald' | 'amber'; items: string[]; }`

    Create src/content/projects.ts:
    - `import type { Project } from "./types";`
    - `export const projects: Project[] = [ ... ]` — all 7 entries in the DETAIL map order [kashmir-fund, n8n-automation, voice-ai-agent, erp-system, netsuite-integration, modern-portfolio, cloud-infrastructure]. For each, copy the detail fields verbatim from projects/[id]/page.tsx. For the 6 that appear in the home grid, also set gridTitle/gridDescription/gridTech/gridCategory verbatim from page.tsx (modern-portfolio is NOT in the grid — leave its grid* fields unset/undefined OR set but it will simply not be selected by homeGridProjects). For the 6 that appear in the ItemList, set seoName/seoDescription/applicationCategory verbatim from layout.tsx (kashmir-fund is NOT in the ItemList — leave its seo* fields unset). Set sitemapPriority per the sitemap (0.8 for the five, 0.7 for modern-portfolio and kashmir-fund). kashmir-fund keeps liveUrl.
    - `export const getProject = (id: string): Project | undefined => projects.find((p) => p.id === id);`
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/content/types.ts exports `interface Project` and `interface Skill`
    - src/content/projects.ts exports `const projects` whose `.length === 7` and exports `getProject`
    - `node -e "const t=require('fs').readFileSync('src/content/projects.ts','utf8'); const ids=[...t.matchAll(/id:\s*\"([a-z0-9-]+)\"/g)].map(m=>m[1]); const a=require('assert'); a.deepStrictEqual(ids,['kashmir-fund','n8n-automation','voice-ai-agent','erp-system','netsuite-integration','modern-portfolio','cloud-infrastructure']); console.log('7 ids in detail order OK');"` prints "7 ids in detail order OK"
    - grep confirms `liveUrl: "https://iamstandingwithkashmir.org"` appears exactly once in projects.ts
    - `npx tsc --noEmit` exits 0 (no type errors in the new module)
  </acceptance_criteria>
  <done>The 7-project superset compiles, carries grid/detail/seo/sitemap fields verbatim, and getProject resolves any of the 7 ids.</done>
</task>

<task type="auto">
  <name>Task 2: Add projections, skills.ts, and the JSON-LD builder seo.ts</name>
  <files>src/content/projects.ts, src/content/skills.ts, src/content/seo.ts</files>
  <read_first>
    - src/content/projects.ts (the superset from Task 1)
    - src/app/page.tsx lines 82-87 (skills — copy verbatim)
    - src/app/layout.tsx lines 494-581 (the exact ItemList wrapper + per-item fields)
    - .planning/phases/01-content-centralization/baseline.json (the target shapes the projections must reproduce)
  </read_first>
  <action>
    Append projections to src/content/projects.ts:
    - `export const homeGridProjects: Project[]` = these 6 ids mapped through getProject in THIS order: ["kashmir-fund","n8n-automation","voice-ai-agent","erp-system","netsuite-integration","cloud-infrastructure"]. Use `.map((id) => getProject(id)!)`.
    - `export const itemListProjects: Project[]` = these 6 ids (NO kashmir-fund) in THIS order: ["n8n-automation","voice-ai-agent","erp-system","netsuite-integration","cloud-infrastructure","modern-portfolio"].
    - `export const sitemapProjects` = an array of { id, priority } in THIS order: n8n-automation 0.8, voice-ai-agent 0.8, erp-system 0.8, netsuite-integration 0.8, cloud-infrastructure 0.8, modern-portfolio 0.7, kashmir-fund 0.7. (8 entries — derive priority from each project's sitemapPriority or list explicitly; order is observable, preserve it exactly.)

    Create src/content/skills.ts:
    - `import type { Skill } from "./types";`
    - `export const skills: Skill[] = [ ... ]` — the 4 groups verbatim (Frontend/blue, Backend/violet, Cloud & DevOps/emerald, Specializations/amber), 4 items each, copied byte-for-byte from page.tsx.

    Create src/content/seo.ts:
    - `import { itemListProjects } from "./projects";`
    - `export function buildItemListSchema()` returning an object byte-identical to the current `projectsListData` literal in layout.tsx: same '@context'/'@type'/'@id'/name/description, `numberOfItems: 6`, and `itemListElement` built by mapping itemListProjects with position (1-based index), item = { '@type':'SoftwareApplication', name: p.seoName, description: p.seoDescription, url: 'https://www.usamajaved.com.au/projects/'+p.id, applicationCategory: p.applicationCategory, operatingSystem: 'Web', author: { '@id': 'https://www.usamajaved.com.au/#person' } }.
    - CRITICAL: the emitted object must equal the current literal exactly (the content-diff gate compares serialized ItemList). numberOfItems stays the literal 6.
  </action>
  <verify>
    <automated>npx tsc --noEmit && node scripts/content-diff.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `node scripts/content-diff.mjs` exits 0 (projections reproduce the baseline: home grid 6, itemList 6 no-kashmir, sitemap 8, skills 4)
    - homeGridProjects has length 6 and does NOT include modern-portfolio; itemListProjects has length 6 and does NOT include kashmir-fund; sitemapProjects has length 8
    - src/content/skills.ts exports `skills` with length 4; grep confirms "NetSuite SuiteScripts" and "Vue.js / Nuxt.js" present verbatim
    - src/content/seo.ts exports buildItemListSchema; `node -e "..."` or tsc confirms numberOfItems literal is 6
  </acceptance_criteria>
  <done>All projections and the JSON-LD builder exist and content-diff.mjs confirms they reproduce the divergent baseline outputs exactly; skills centralized verbatim.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean (the module is the type safety net; build ignores errors so this is the real gate)
- `node scripts/content-diff.mjs` exits 0 — projections match baseline 6/7/6/8/4
- No "use client" in any src/content/* file; no JSX; no new dependency
- Every literal copied verbatim (diff gate is the proof)
</verification>

<success_criteria>
- src/content/ holds the single typed source: 7-project superset + getProject + homeGridProjects/itemListProjects/sitemapProjects + skills + buildItemListSchema
- Projections reproduce each consumer's exact current set/order (verified by content-diff.mjs)
- Module compiles under strict tsc with zero errors and zero new deps
</success_criteria>

<output>
After completion, create `.planning/phases/01-content-centralization/01-02-SUMMARY.md` documenting: the Project interface field set (grid* vs detail vs seo* vs sitemapPriority), the three projection export names + their exact id lists/order, and buildItemListSchema's output shape — so the Wave 2 consumer-repointing plans import the right names.
</output>
