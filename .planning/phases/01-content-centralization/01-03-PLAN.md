---
phase: 01-content-centralization
plan: 03
type: execute
wave: 2
depends_on: ["01-02"]
files_modified:
  - src/app/projects/[id]/page.tsx
  - src/app/sitemap.ts
autonomous: true
requirements: [FIX-03, SEO-01]
must_haves:
  truths:
    - "projects/[id] sources its data from src/content/projects via getProject — no inline 7-entry literal remains"
    - "generateStaticParams emits all 7 slugs; every /projects/<slug> prerenders (no 404)"
    - "generateMetadata returns title + description per slug from the centralized source"
    - "sitemap emits the same 8 project URLs in the same order with the same priorities; non-project URLs untouched"
  artifacts:
    - path: "src/app/projects/[id]/page.tsx"
      provides: "Detail page sourcing content from @/content/projects + generateStaticParams + generateMetadata"
      contains: "generateStaticParams"
    - path: "src/app/sitemap.ts"
      provides: "Sitemap sourcing project URLs from sitemapProjects projection"
      contains: "sitemapProjects"
  key_links:
    - from: "src/app/projects/[id]/page.tsx"
      to: "src/content/projects"
      via: "import { getProject, projects } / generateStaticParams"
      pattern: "@/content/projects"
    - from: "src/app/sitemap.ts"
      to: "src/content/projects"
      via: "import { sitemapProjects }"
      pattern: "sitemapProjects"
---

<objective>
Repoint the two SEO/route consumers at the centralized module: (1) projects/[id]/page.tsx imports getProject + projects from @/content/projects, removes its inline 7-entry literal, and adds generateStaticParams + generateMetadata (FIX-03); (2) sitemap.ts imports sitemapProjects and maps the 8 project URLs from it, leaving all non-project URLs byte-for-byte untouched.

Purpose: This is the FIX-03 retrofit plus the sitemap half of SEO-01. The page is ALREADY a server async component — generateStaticParams/generateMetadata are direct sibling exports, no client-island restructuring.
Output: modified src/app/projects/[id]/page.tsx, src/app/sitemap.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-content-centralization/01-RESEARCH.md
@.planning/phases/01-content-centralization/01-02-SUMMARY.md

<constraints>
- projects/[id]/page.tsx is ALREADY `export default async function ProjectDetail({ params }: { params: Promise<{ id: string }> })` with notFound() on miss. Do NOT add "use client", do NOT restructure into a client island. Just swap the data source and add two sibling exports.
- generateStaticParams MUST emit all 7 slugs (from the detail-key superset / projects array), NOT from a 6-item projection — building from the grid(6) or ItemList(6) would drop modern-portfolio or kashmir-fund and 404 that URL.
- generateMetadata uses the Next 15 async-params signature: `({ params }: { params: Promise<{ id: string }> }): Promise<Metadata>`, await params. generateMetadata is not used anywhere else yet — follow the async signature exactly.
- The rendered JSX of the detail page must NOT change — only the source of `projects`/`project`. Render the same fields. Preserve liveUrl handling for kashmir-fund.
- sitemap.ts: ONLY the project-URL block changes. The core pages, Australia landing, and 2 blog article entries stay byte-for-byte. Keep changeFrequency 'monthly' and the exact priorities (0.8 ×5, 0.7 ×2) and the exact order.
- Quote style: match each file you edit (projects/[id] uses double quotes; sitemap.ts uses single quotes).
- Zero new dependencies.
</constraints>

<interfaces>
From src/content/projects.ts (built in Wave 1):
  export const projects: Project[]            // all 7, detail order
  export const getProject: (id) => Project | undefined
  export const sitemapProjects               // [{ id, priority }] x8 in sitemap order

Detail keys (generateStaticParams must emit all 7):
  ["kashmir-fund","n8n-automation","voice-ai-agent","erp-system","netsuite-integration","modern-portfolio","cloud-infrastructure"]

Sitemap project order + priority (unchanged):
  n8n-automation 0.8, voice-ai-agent 0.8, erp-system 0.8, netsuite-integration 0.8, cloud-infrastructure 0.8, modern-portfolio 0.7, kashmir-fund 0.7

generateMetadata target (research Pattern 1):
  const { id } = await params; const p = getProject(id); if (!p) return {}; return { title: p.title, description: p.description };
  (Use the DETAIL title/description fields — p.title / p.description — matching what the page shows, not the grid copy.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repoint projects/[id]/page.tsx + add generateStaticParams + generateMetadata</name>
  <files>src/app/projects/[id]/page.tsx</files>
  <read_first>
    - src/app/projects/[id]/page.tsx (full file — note the inline `const projects = {` at line 4, the access `projects[id as keyof typeof projects]` at line 269, notFound() at 272, and all JSX field reads)
    - src/content/projects.ts (getProject + projects exports + Project field names)
    - src/content/types.ts (Project interface — confirm detail field names match what the JSX reads)
    - .planning/phases/01-content-centralization/01-RESEARCH.md (Pattern 1 + Pitfall 2: build params from the 7-key superset)
  </read_first>
  <action>
    Edit src/app/projects/[id]/page.tsx:
    1. Remove the inline `const projects = { ... };` object literal (lines 4-266). Add at top: `import { getProject, projects } from "@/content/projects";` and `import type { Metadata } from "next";`.
    2. In the component body, replace `const project = projects[id as keyof typeof projects];` with `const project = getProject(id);`. Keep `if (!project) notFound();` exactly. The rest of the JSX reads the same field names (title, subtitle, description, longDescription, tech, category, timeline, team, features, challenges, results, gradient, liveUrl) — those now come from the Project interface; do NOT change the JSX markup.
    3. Add sibling export `export function generateStaticParams() { return projects.map((p) => ({ id: p.id })); }` — this yields all 7 slugs.
    4. Add `export async function generateMetadata({ params }: { params: Promise<{ id: string }> }): Promise<Metadata> { const { id } = await params; const p = getProject(id); if (!p) return {}; return { title: p.title, description: p.description }; }`.

    Confirm there is NO "use client" added and the default export stays `async function`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && node -e "const t=require('fs').readFileSync('src/app/projects/[id]/page.tsx','utf8'); const a=require('assert'); a.ok(t.includes('generateStaticParams'),'has generateStaticParams'); a.ok(t.includes('generateMetadata'),'has generateMetadata'); a.ok(t.includes('@/content/projects'),'imports content'); a.ok(!/const projects = \{/.test(t),'inline literal removed'); a.ok(!t.includes('use client'),'no use client'); console.log('detail page retrofit OK');"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/projects/[id]/page.tsx imports getProject + projects from "@/content/projects"
    - The inline `const projects = {` literal is GONE (regex /const projects = \{/ does not match)
    - File contains `generateStaticParams` returning `projects.map((p) => ({ id: p.id }))` and an async `generateMetadata` with `params: Promise<{ id: string }>`
    - No "use client" directive; default export remains `async function`
    - `npx tsc --noEmit` exits 0
    - After `npm run build` (run in plan 01-05 gate), the route table lists all 7 /projects/<slug> as prerendered — captured as a build-output check in the verification plan
  </acceptance_criteria>
  <done>Detail page renders from the centralized source with generateStaticParams (7 slugs) + generateMetadata, no inline literal, no client conversion.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint sitemap.ts project URLs to sitemapProjects projection</name>
  <files>src/app/sitemap.ts</files>
  <read_first>
    - src/app/sitemap.ts (full file — note "// Project pages" block at lines 83-125, 8 entries; everything above it must stay untouched)
    - src/content/projects.ts (sitemapProjects export shape: [{ id, priority }])
    - .planning/phases/01-content-centralization/baseline.json (sitemapProjects target: exact 8 slug+priority pairs in order)
  </read_first>
  <action>
    Edit src/app/sitemap.ts:
    1. Add at top: `import { sitemapProjects } from '@/content/projects';` (single quotes — match this file).
    2. Replace the 8 hand-written project-URL objects (the `// Project pages` block) with a spread:
       `...sitemapProjects.map((p) => ({ url: `${baseUrl}/projects/${p.id}`, lastModified: currentDate, changeFrequency: 'monthly' as const, priority: p.priority, })),`
    3. Leave ALL other entries (core pages, developer-australia, the 2 blog articles) exactly as they are — do not touch them.
    Verify the emitted project URL set/order/priorities are identical to before: n8n-automation 0.8, voice-ai-agent 0.8, erp-system 0.8, netsuite-integration 0.8, cloud-infrastructure 0.8, modern-portfolio 0.7, kashmir-fund 0.7.
  </action>
  <verify>
    <automated>npx tsc --noEmit && node scripts/content-diff.mjs</automated>
  </verify>
  <acceptance_criteria>
    - src/app/sitemap.ts imports `sitemapProjects` from '@/content/projects'
    - The 8 inline `${baseUrl}/projects/<slug>` literal blocks are replaced by a single `sitemapProjects.map(...)` spread; non-project entries are unchanged (grep confirms `/services`, `/developer-australia`, `/blog/ai-developer-perth` still present)
    - `node scripts/content-diff.mjs` exits 0 (sitemap projection still 8 URLs, same order, same priorities)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Sitemap project URLs come from the projection with identical set/order/priority; non-project URLs untouched; diff gate green.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean
- `node scripts/content-diff.mjs` exits 0 (sitemap + detail-key sets preserved)
- projects/[id] has generateStaticParams (7 slugs) + generateMetadata, no inline literal, still a server async component
- sitemap non-project URLs byte-identical
</verification>

<success_criteria>
- FIX-03 satisfied: generateStaticParams + generateMetadata from the centralized source, all 7 slugs preserved
- SEO-01 (sitemap half): 8 project URLs unchanged in URL space, order, and priority
- Zero deletions (content-diff green), zero new deps
</success_criteria>

<output>
After completion, create `.planning/phases/01-content-centralization/01-03-SUMMARY.md` noting: confirmation the detail page stayed a server component, the generateStaticParams/generateMetadata signatures used, and that sitemap non-project entries were left untouched.
</output>
