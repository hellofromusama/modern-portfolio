---
phase: 04-r3f-infrastructure
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - src/app/_scene-harness/layout.tsx
  - src/app/_scene-harness/page.tsx
  - scripts/bundle-gate.mjs
autonomous: false
requirements: [FOUND-04, PERF-01, SHIP-01]
must_haves:
  truths:
    - "The island mounts on one isolated harness route via ClientScene, proving end-to-end render WITHOUT touching the live hero"
    - "The harness route is noindex/nofollow and absent from the sitemap — it is not publicly discoverable"
    - "Toggling data-theme on the harness changes the icosahedron edge color (dark #60a5fa → light #3b82f6) with no remount and no THREE.Color warning"
    - "The bundle-gate script is green: three is present ONLY on the harness route and absent from /layout and every text/SEO route"
    - "The home LCP element renders server-side without waiting on three.js (poster/hero content present in curl'd HTML)"
    - "SHIP-01 gate is green: tsc + lint + build + bundle-gate, plus both-theme + reduced-motion + WebGL-disabled smoke"
  artifacts:
    - path: "src/app/_scene-harness/page.tsx"
      provides: "'use client' isolated verification route mounting <ClientScene/> in a sized container"
      contains: "ClientScene"
    - path: "src/app/_scene-harness/layout.tsx"
      provides: "Server layout exporting metadata.robots = { index:false, follow:false } for the harness route (client page cannot export metadata)"
      contains: "index: false"
    - path: "scripts/bundle-gate.mjs"
      provides: "CANVAS_ROUTES updated to include the harness route now that it ships three"
      contains: "CANVAS_ROUTES"
  key_links:
    - from: "src/app/_scene-harness/page.tsx"
      to: "src/components/three/ClientScene.tsx"
      via: "import ClientScene + render inside a fixed-height container"
      pattern: "ClientScene"
    - from: "scripts/bundle-gate.mjs CANVAS_ROUTES"
      to: ".next/app-build-manifest.json harness route"
      via: "harness route added to the allow-set; all other routes must stay three-free"
      pattern: "scene-harness"
---

<objective>
Prove the island works end-to-end and pass the SHIP-01 gate (FOUND-04, PERF-01, SHIP-01) WITHOUT degrading the live hero. Mount `ClientScene` on ONE isolated, dev-only harness route (`/_scene-harness`), make that route noindex/nofollow + sitemap-excluded so it is not publicly discoverable, update the bundle-gate allow-set to that single route, then run the full verification suite and a human evidence checkpoint covering both-theme reactivity, reduced-motion freeze, WebGL-disabled poster fallback, and the poster-first LCP proof.

Depends on 04-02 (island built). This is the phase's SHIP-01 gate + evidence checkpoint. **Per orchestrator: the demo must NOT degrade the live hero** — research notes the homepage hero is the Phase-5 target and sits behind its own poster, so mounting the new R3F island there now risks the live LCP element. A standalone harness route is the research-endorsed "throwaway/dev route" that verifies the boundary in isolation; Phase 5 mounts the real scene on the hero.

Purpose: Turn the built-but-unmounted island into proven, gated infrastructure — green bundle budget, server-rendered LCP, both-theme WebGL reactivity, and a non-indexable harness — so Phase 5 inherits a trusted boundary.
Output: `/_scene-harness` route (noindex/nofollow, sitemap-excluded); updated `scripts/bundle-gate.mjs`; SHIP-01 green; evidence checkpoint approved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-r3f-infrastructure/04-RESEARCH.md

<facts>
<!-- Verified / decided at planning time. -->
- ClientScene (from 04-02) is the ONLY public import surface. The harness imports ClientScene, never SceneCanvas/three directly.
- next.config.ts has ignoreBuildErrors:true + ignoreDuringBuilds:true → `npm run build` alone proves nothing. SHIP-01 = `npx tsc --noEmit && npm run lint && npm run build && node scripts/bundle-gate.mjs` + manual smoke (research Pitfall 4).
- bundle-gate.mjs (from 04-01) currently allows CANVAS_ROUTES that include no real scene; this plan adds the harness route to the allow-set since it now legitimately ships three.
- Text/SEO routes that MUST stay three-free: /blog, /blog/*, /developer-australia, /expertise, /services, /tech-stack, /projects/[id], and shared /layout.
- The live hero (src/app/page.tsx → Hero3D, Canvas-2D) is NOT touched this phase. No edits to page.tsx.
- Underscore-prefixed app dirs are a common Next convention for non-routable/dev helpers, but Next App Router DOES route `/_scene-harness` — it is publicly reachable by URL. It MUST therefore be made non-indexable (robots noindex/nofollow) AND kept out of sitemap.ts (which is an explicit list). It is removed in Phase 5.
- sitemap.ts is an explicit hard-coded list of routes; the harness must NOT be added to it. A client page CANNOT `export const metadata` — the robots directive goes in a sibling server `layout.tsx` for the route segment.
</facts>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Mount the island on an isolated, non-indexable harness route + update the bundle allow-set</name>
  <files>src/app/_scene-harness/layout.tsx, src/app/_scene-harness/page.tsx, scripts/bundle-gate.mjs</files>
  <action>
    **Read first:** `src/components/three/ClientScene.tsx` (its props + default export), `src/app/sitemap.ts` (confirm it is an explicit list and that scene-harness is NOT in it), `scripts/bundle-gate.mjs` (the CANVAS_ROUTES set + matcher chosen in 04-01).

    File 1 — `src/app/_scene-harness/layout.tsx` (server component, NO 'use client'). Because the page is a client component and client components cannot export `metadata`, add a sibling server layout for the route segment that exports:
    `export const metadata = { robots: { index: false, follow: false } };`
    This makes `/_scene-harness` emit `<meta name="robots" content="noindex, nofollow">` so the throwaway route is never indexed. The layout body just renders `{children}`.

    File 2 — `src/app/_scene-harness/page.tsx`. `'use client'` first line. A minimal verification page (NOT a designed page — this is throwaway, removed in Phase 5):
    - Render a sized container (e.g. `<div style={{ width:'100%', height:'70vh' }}>`) holding `<ClientScene className="..." />` so the canvas has real dimensions.
    - Include a short instruction line and rely on the existing global `ThemeToggle` (it lives in Navigation; if Navigation isn't rendered here, add a tiny inline button that flips `document.documentElement.getAttribute('data-theme')` between 'dark'/'light' so the evidence checkpoint can toggle without leaving the page). Keep it trivial.
    - Do NOT add it to sitemap.ts, robots.ts, or Navigation — it stays unlinked/dev-only. Confirm via the verify command that sitemap.ts contains no 'scene-harness' reference.

    File 3 — update `scripts/bundle-gate.mjs`: add the harness route key (match the manifest's route format observed in 04-01, e.g. `/_scene-harness/page`) to `CANVAS_ROUTES`. Leave the comment "Phase 5 replaces this with the hero route; remove /_scene-harness then." The gate must now assert: three present on `/_scene-harness` is OK; three on ANY other route (esp. /layout + text routes) fails.

    Then build and run the gate to confirm three landed ONLY on the harness route.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/app/sitemap.ts','utf8');if(/scene-harness/.test(s)){console.error('FAIL: scene-harness leaked into sitemap.ts');process.exit(1)}const l=fs.readFileSync('src/app/_scene-harness/layout.tsx','utf8');if(!/index:\s*false/.test(l)||!/follow:\s*false/.test(l)){console.error('FAIL: harness layout missing robots noindex/nofollow');process.exit(1)}console.log('harness noindex + sitemap-excluded OK')" && (npx tsc --noEmit 2>&1 | grep -iE "_scene-harness|ClientScene" && exit 1 || exit 0) && npm run build 2>&1 | tail -5 && node scripts/bundle-gate.mjs</automated>
  </verify>
  <done>`src/app/_scene-harness/layout.tsx` exports `metadata.robots = { index:false, follow:false }` (route emits noindex,nofollow); `sitemap.ts` contains no scene-harness entry (verified by grep); the harness segment + ClientScene type-check clean (targeted tsc, run BEFORE the build so it is not masked by ignoreBuildErrors); `/_scene-harness/page.tsx` renders `<ClientScene/>` in a sized container; `bundle-gate.mjs` allow-set includes only the harness route; `node scripts/bundle-gate.mjs` exits 0 with three confined to `/_scene-harness` (absent from /layout and all text/SEO routes).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: SHIP-01 full gate + poster-first LCP proof + PERF-01 leak grep</name>
  <files>src/lib/webgl.ts, src/components/three/ScenePoster.tsx, src/components/three/SceneCanvas.tsx, src/components/three/ThemedScene.tsx, src/components/three/ClientScene.tsx, src/app/_scene-harness/layout.tsx, src/app/_scene-harness/page.tsx, scripts/bundle-gate.mjs</files>
  <action>
    **Read first:** nothing new — this is a verification task; run commands, do not edit source unless a gate fails (then fix minimally in the owning Phase-4 file listed in `<files>` above and note it).

    Run the SHIP-01 suite explicitly (research Pitfall 4 — build alone is meaningless here):
    1. `npx tsc --noEmit` — must be clean on all Phase-4-touched files (src/lib/webgl.ts, src/components/three/*, src/app/_scene-harness/*, scripts). Pre-existing unrelated errors elsewhere (e.g. api/expertise, noted deferred in Phase 1) do NOT block, but Phase-4 files MUST be clean — grep the output for the Phase-4 paths.
    2. `npm run lint` — clean on Phase-4 files.
    3. `npm run build` — succeeds.
    4. `node scripts/check-stack.mjs && node scripts/bundle-gate.mjs` — both green.
    5. **PERF-01 source-level leak grep (route-split guard):** assert NO text/SEO route or the shared layout imports `three`, `@react-three/*`, or `ClientScene`. The bundle-gate enforces this at the chunk level; this grep is the companion source-level guard, captured as an automated command in `<verify>` below.
    6. **PERF-01 poster-first LCP proof (orchestrator acceptance):** start the built app (`npm run start` in the background on its port), then `curl -s http://localhost:3000/ | rg -i "hero|poster|Usama|<canvas"` — confirm the home LCP/hero content is present in the SERVER HTML (the live Canvas-2D hero text/poster is server-rendered; three.js is NOT in the home payload — re-confirm via the bundle gate that `/page` has no three). The point: the home LCP element does not wait on three.js. Also `curl -s http://localhost:3000/_scene-harness | rg -i "poster|<canvas|scene"` — the harness HTML contains the poster (the ssr:false canvas is client-only, so HTML shows the poster, proving poster-first). Stop the server after.
    Record every command's result in the SUMMARY. If any gate is red, fix in the owning Phase-4 file (minimal) and re-run from step 1.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | (grep -iE "three/|lib/webgl|_scene-harness" && exit 1 || exit 0) && npm run lint 2>&1 | tail -3 && npm run build 2>&1 | tail -3 && node scripts/check-stack.mjs && node scripts/bundle-gate.mjs && (rg -l "from 'three'|@react-three|ClientScene" src/app/blog src/app/developer-australia src/app/expertise src/app/services src/app/tech-stack src/app/projects src/app/layout.tsx && echo "FAIL: three/ClientScene leaked into a text/SEO route or layout" && exit 1 || echo "PERF-01 leak grep OK: no text/SEO route imports three/@react-three/ClientScene")</automated>
  </verify>
  <done>tsc + lint clean on all Phase-4 files; build succeeds; check-stack + bundle-gate both exit 0; PERF-01 source-level leak grep confirms no text/SEO route or /layout imports three/@react-three/ClientScene; curl confirms home hero/poster content is server-rendered with no three in the home payload (poster-first LCP proven), and the harness HTML shows the poster (ssr:false canvas client-only).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Evidence checkpoint — both-theme + reduced-motion + WebGL-disabled + CLS smoke</name>
  <what-built>
    A reusable SSR-safe R3F island (ClientScene → SceneCanvas → ThemedScene, IslandBoundary + ScenePoster), the bundle-gate proving three is route-split, and an isolated, noindex/nofollow `/_scene-harness` route mounting it. The live hero was NOT touched. tsc/lint/build/bundle-gate are green and the home LCP/hero is server-rendered without three (Task 2).
  </what-built>
  <how-to-verify>
    Run `npm run start` (or `npm run dev`), then in a browser:
    1. **Both-theme reactivity (FOUND-04):** Visit http://localhost:3000/_scene-harness . A rotating wireframe icosahedron renders. Toggle the theme (the inline button / ThemeToggle). The icosahedron EDGE color must visibly change (dark `#60a5fa` blue → light `#3b82f6` blue) live, with NO page reload / NO scene remount, and NO `THREE.Color` warning in the console.
    2. **Off-screen / tab-blur pause (PERF foundation):** Open DevTools Performance (or just watch CPU). Scroll the canvas off-screen → rotation stops. Switch to another tab and back → it was paused while blurred. (frameloop bound to useAnimationGate.)
    3. **Reduced-motion (PERF-04 foundation):** In DevTools → Rendering → "Emulate CSS prefers-reduced-motion: reduce", hard-reload `/_scene-harness`. The icosahedron is frozen (no rotation) — equivalent static view.
    4. **WebGL-disabled fallback (FOUND-04/FOUND-05):** Disable WebGL (chrome://flags → WebGL off, or DevTools to force context loss) and reload. The `ScenePoster` static gradient shows instead of a crash; page chrome intact.
    5. **Hard-refresh, no hydration error:** Hard-refresh `/_scene-harness` (not client nav) → no hydration mismatch error in console (ssr:false excludes the server pass).
    6. **Live hero untouched:** Visit http://localhost:3000/ → the existing Canvas-2D hero looks exactly as before (no regression).
    7. **CLS ≤ 0.1 on canvas mount (PERF-01):** With DevTools Performance/Lighthouse (or the Layout Shift overlay in Rendering), load `/_scene-harness` and watch as the ssr:false `<canvas>` mounts on top of the poster. There must be NO visible layout jump — Cumulative Layout Shift ≤ 0.1. This holds because `ScenePoster` is sized 100%/100% to the same container the canvas mounts into, so swapping poster→canvas causes zero reflow.
    Stop the server when done.
  </how-to-verify>
  <resume-signal>Type "approved" if all seven checks pass, or describe which check failed (theme color didn't change / didn't pause / didn't freeze under reduced-motion / no poster on WebGL-off / hydration error / hero regressed / layout shifted on canvas mount).</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit && npm run lint && npm run build` — green (Phase-4 files clean).
- `node scripts/check-stack.mjs && node scripts/bundle-gate.mjs` — both exit 0; three only on `/_scene-harness`.
- `/_scene-harness` is noindex/nofollow (layout robots metadata) and absent from sitemap.ts (Task 1 verify).
- PERF-01 source-level leak grep returns nothing (no text/SEO route or /layout imports three/@react-three/ClientScene).
- curl of `/` shows server-rendered hero/poster content with no three in the home payload (PERF-01 poster-first LCP).
- Human checkpoint approved: theme reactivity, off-screen/tab-blur pause, reduced-motion freeze, WebGL-disabled poster, no-hydration-error, hero-untouched, CLS ≤ 0.1 on canvas mount — all confirmed in both themes.
</verification>

<success_criteria>
- The island is proven end-to-end on an isolated, non-indexable route without degrading the live hero (FOUND-04).
- Bundle budget is GREEN: three route-split to the canvas route only; home LCP renders without three.js; CLS ≤ 0.1 on canvas mount (PERF-01).
- SHIP-01 gate passes (tsc + lint + build + bundle-gate + leak grep + both-theme/reduced-motion/WebGL-disabled smoke) — Phase 4 complete.
</success_criteria>

<output>
After completion, create `.planning/phases/04-r3f-infrastructure/04-03-SUMMARY.md`. Record: full gate command outputs, the curl LCP evidence, the noindex/sitemap-exclusion confirmation, the PERF-01 leak-grep result, the CLS observation, the checkpoint result, and a note that `/_scene-harness` (route + layout) + its bundle-gate allow-entry are temporary (Phase 5 swaps the hero in and removes the harness).
</output>
