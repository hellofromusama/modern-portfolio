---
phase: 04-r3f-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - scripts/bundle-gate.mjs
  - scripts/check-stack.mjs
autonomous: true
requirements: [FOUND-04, PERF-01]
must_haves:
  truths:
    - "Phase 3 outputs (useAnimationGate, useThemeColors, IslandBoundary) are confirmed present before any Phase-4 work proceeds"
    - "three + @react-three/fiber@^9.6 + @react-three/drei@^10.7 + @types/three are installed and peer-clean against React 19.2.4"
    - "motion is present (installed by Phase 3) and NOT reinstalled/duplicated"
    - "A zero-dependency Node bundle-gate script asserts three-containing chunks attach ONLY to canvas routes, tuned against the REAL built manifest"
    - "A zero-dependency stack-presence script asserts all four deps are in package.json"
  artifacts:
    - path: "scripts/bundle-gate.mjs"
      provides: "Parses .next/app-build-manifest.json; exits 1 if three leaks into non-canvas routes"
      contains: "app-build-manifest.json"
    - path: "scripts/check-stack.mjs"
      provides: "Asserts three/@react-three/fiber/@react-three/drei/motion present in package.json deps"
      contains: "@react-three/fiber"
    - path: "package.json"
      provides: "3D stack dependencies (three, fiber, drei) + @types/three devDep"
      contains: "@react-three/fiber"
  key_links:
    - from: "scripts/bundle-gate.mjs"
      to: ".next/app-build-manifest.json"
      via: "readFile + JSON.parse of manifest.pages"
      pattern: "app-build-manifest"
    - from: "package.json @react-three/fiber"
      to: "react 19.2.4"
      via: "peer react >=19 <19.3 satisfied"
      pattern: "@react-three/fiber"
---

<objective>
Wave 0 / foundation for the R3F island (FOUND-04, PERF-01): guard that Phase 3 merged, install the React-19-verified 3D stack, and author the two zero-dependency Node gate scripts (bundle budget + stack presence) — tuning the bundle-gate chunk matcher against the REAL built manifest so it is not brittle. This plan writes NO island component code; it stands up dependencies and the automatable gates the later plans rely on.

**Execution sequencing: this phase begins only after Phase 3 completes.** At authoring time (2026-06-12) `src/hooks/` and `src/components/IslandBoundary.tsx` do NOT yet exist and `motion`/`three` are ABSENT from package.json — Phase 3 is concurrent. Task 1 is a hard ls-guard that BLOCKS if Phase 3 outputs are missing.

Purpose: Every later Phase-4 plan consumes these deps and gates. Get them right once, prove the gate against a real build, remove the next.config.ts collision point entirely (no analyzer).
Output: 3D stack installed; `scripts/bundle-gate.mjs`; `scripts/check-stack.mjs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-r3f-infrastructure/04-RESEARCH.md

<facts>
<!-- Verified from repo at planning time — executor should not re-discover these. -->
- package.json: motion ABSENT, three ABSENT at plan time (Phase 3 installs motion). grep before installing motion.
- scripts/ already holds content-baseline.mjs + content-diff.mjs (Phase 1). Add new scripts here, same convention.
- .next/app-build-manifest.json EXISTS; structure is { pages: { "<route>": ["static/chunks/...js", ...] } } (verified from this repo's .next).
- Existing routes that MUST NOT ship three (text/SEO server routes per ARCHITECTURE): /blog, /blog/*, /developer-australia, /expertise, /services, /tech-stack, /projects/[id], plus the shared /layout.
- Node v22.22.2 local; lockfileVersion 3. Run `npm ci` first ONLY if node_modules is absent.
- next.config.ts has ignoreBuildErrors:true + ignoreDuringBuilds:true — a green `next build` proves nothing; tsc/lint must run explicitly (later plan).
- Verified live versions (npm 2026-06-12): three@0.184.0; @react-three/fiber@9.6.1 peer react >=19 <19.3, three >=0.156; @react-three/drei@10.7.7 peer react ^19, fiber ^9.0.0, three >=0.159; motion@12.40.0.
</facts>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Phase-3 sequencing guard + install the React-19 3D stack</name>
  <files>package.json, package-lock.json</files>
  <action>
    **Read first:** `package.json` (confirm current deps), `src/hooks/` listing, `src/components/IslandBoundary.tsx`.

    Step A — HARD GUARD (research Open Question 1; orchestrator Wave-0 MUST (a)). Verify ALL THREE Phase-3 outputs exist:
    - `src/hooks/useAnimationGate.ts`
    - `src/hooks/useThemeColors.ts`
    - `src/components/IslandBoundary.tsx`
    Run: `node -e "const fs=require('fs');['src/hooks/useAnimationGate.ts','src/hooks/useThemeColors.ts','src/components/IslandBoundary.tsx'].forEach(f=>{if(!fs.existsSync(f))throw new Error('BLOCKED: Phase 3 output missing: '+f+' — Phase 4 must execute AFTER Phase 3 merges. Do NOT re-author these files (creates merge conflict). Stop and resume Phase 4 once Phase 3 completes.')});console.log('Phase 3 outputs present — proceeding.')"`
    If this throws, STOP the entire plan with the message — do not install anything, do not author the rest.

    Step B — install the stack (orchestrator Wave-0 MUST (b); research §Standard Stack). If `node_modules` is absent, run `npm ci` first (lockfile-faithful). Then:
    - `grep '"motion"' package.json` (or `node -e "console.log(!!require('./package.json').dependencies.motion)"`). Motion is a Phase-3 deliverable — if present, do NOT reinstall it. If absent (Phase 3 didn't add it), `npm install motion@^12.40.0`.
    - `npm install three@^0.184.0 @react-three/fiber@^9.6.1 @react-three/drei@^10.7.7`
    - `npm install -D @types/three`
    Do NOT install lenis/maath/postprocessing/r3f-perf (Phase 5) or @next/bundle-analyzer (using the Node script instead — keeps next.config.ts untouched). Do NOT edit next.config.ts.
    Watch for peer-dep warnings: fiber@9 hard-ceilings react <19.3 — React is pinned 19.2.4, so it must resolve clean. If npm reports an unmet/conflicting peer for react, STOP and report (do NOT --force / --legacy-peer-deps).
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json');const d={...p.dependencies,...p.devDependencies};['three','@react-three/fiber','@react-three/drei','@types/three','motion'].forEach(x=>{if(!d[x])throw new Error('missing '+x)});console.log('stack present:',JSON.stringify(['three','@react-three/fiber','@react-three/drei','@types/three','motion'].map(x=>x+'@'+d[x])))"</automated>
  </verify>
  <done>Phase-3 guard passed; three@^0.184, @react-three/fiber@^9.6, @react-three/drei@^10.7 in dependencies, @types/three in devDependencies, motion present (not duplicated); npm install completed with no react peer conflict.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author the stack-presence + bundle-gate scripts and tune against the real manifest</name>
  <files>scripts/check-stack.mjs, scripts/bundle-gate.mjs</files>
  <action>
    **Read first:** `scripts/content-diff.mjs` (match its node:fs / ESM style and exit-code convention), `.next/app-build-manifest.json` (inspect the real structure + chunk names).

    File 1 — `scripts/check-stack.mjs` (FOUND-04 scripted gate). Zero deps, ESM, Node 22. Read `./package.json`, merge `dependencies`+`devDependencies`, assert `three`, `@react-three/fiber`, `@react-three/drei`, `motion` all present; `console.error` + `process.exit(1)` listing any missing; else `console.log('stack OK')`. This is the FOUND-04 install proof that survives lockfile churn.

    File 2 — `scripts/bundle-gate.mjs` (PERF-01 hard gate; orchestrator decision — zero-dep manifest parser, NO @next/bundle-analyzer, NO next.config.ts changes). Per research §Code Examples:
    - `import { readFile } from 'node:fs/promises'`; parse `.next/app-build-manifest.json` → `manifest.pages` (`{ route: [chunkPaths] }`).
    - `const CANVAS_ROUTES = new Set([...])` — the ONLY routes allowed to ship three. In Phase 4 the demo mounts on one route (Plan 04-03 decides which; default to the homepage `/page` per research, since Phase 5's hero replaces it there). Leave a clear comment: "Update this set as Phase 5 adds scenes."
    - For each route, flag if any chunk matches the three-detector AND the route is not in CANVAS_ROUTES. Collect offenders; if any, `console.error` the offenders as JSON and `process.exit(1)`; else `console.log('bundle budget OK')`.
    - **Tune the matcher against the REAL manifest (orchestrator Wave-0 MUST (c); research Open Question 2 — webpack may hash the three vendor chunk to e.g. `static/chunks/4521.js`).** Build first (`npm run build`), open `.next/app-build-manifest.json`, find which chunk(s) `three`/`react-three` land in. If they are NAMED (regex `/three|react-three|fiber|drei/i` on the chunk path matches), use the filename regex. If they are HASHED (generic numeric chunk), switch to the robust cross-route assertion: compute the set of chunks that appear ONLY on CANVAS_ROUTES and assert no non-canvas route includes any of them (i.e. the heavy vendor chunk unique to canvas routes must never appear under /layout or a text route). Pick whichever matcher the real manifest supports and HARD-CODE that approach with an explanatory comment citing what you observed. At this point in Phase 4 NO route mounts a scene yet, so the gate should pass trivially (three absent everywhere) — that green-on-empty result is expected and still proves the parser runs.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && node scripts/check-stack.mjs && node scripts/bundle-gate.mjs</automated>
  </verify>
  <done>`scripts/check-stack.mjs` exits 0 (all four deps present). `scripts/bundle-gate.mjs` parses the real `.next/app-build-manifest.json` with a matcher proven against the actual chunk names and exits 0 (three absent from all routes this phase). Both scripts are zero-dependency ESM. next.config.ts is unchanged.</done>
</task>

</tasks>

<verification>
- `node scripts/check-stack.mjs` → "stack OK", exit 0.
- `node scripts/bundle-gate.mjs` → "bundle budget OK", exit 0 (no scene mounted yet, so three is absent everywhere).
- `git diff next.config.ts` → empty (analyzer NOT adopted; no collision point introduced).
- Phase-3 guard: `src/hooks/useAnimationGate.ts`, `src/hooks/useThemeColors.ts`, `src/components/IslandBoundary.tsx` all exist (else the plan blocked at Task 1).
</verification>

<success_criteria>
- 3D stack installed React-19-clean; motion present and not duplicated (FOUND-04 install half).
- Two zero-dependency gate scripts exist and pass; bundle-gate matcher tuned against the real built manifest (PERF-01 gate ready for later plans).
- next.config.ts untouched — the analyzer collision point is eliminated per orchestrator decision.
</success_criteria>

<output>
After completion, create `.planning/phases/04-r3f-infrastructure/04-01-SUMMARY.md`. Record: exact installed versions, the bundle-gate matcher approach chosen (named-regex vs cross-route diff) and WHY (what the real manifest showed), and confirmation the Phase-3 guard passed.
</output>
