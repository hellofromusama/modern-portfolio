---
phase: 04-r3f-infrastructure
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/lib/webgl.ts
  - src/components/three/ScenePoster.tsx
  - src/components/three/SceneCanvas.tsx
  - src/components/three/ThemedScene.tsx
  - src/components/three/ClientScene.tsx
autonomous: true
requirements: [FOUND-04, PERF-01]
must_haves:
  truths:
    - "A reusable Canvas island loads via dynamic import ssr:false from inside a 'use client' wrapper"
    - "The Canvas clamps DPR to [1,2] and binds frameloop to Phase 3's useAnimationGate (pauses off-screen / tab-blur / reduced-motion)"
    - "A trivial themed scene (rotating wireframe icosahedron) consumes Phase 3's useThemeColors INSIDE the Canvas and reacts to data-theme without remounting"
    - "A static ScenePoster serves as loading view, IslandBoundary fallback, reduced-motion view, and WebGL-unavailable view — sized to the container for zero CLS"
    - "WebGL-unavailable and context-loss both degrade to the poster instead of crashing"
  artifacts:
    - path: "src/lib/webgl.ts"
      provides: "isWebGLAvailable() getContext probe, SSR-guarded (no React)"
      contains: "getContext"
    - path: "src/components/three/ScenePoster.tsx"
      provides: "Static themed poster (CSS gradient from tokens), full-size, the LCP element"
      contains: "var(--bg"
    - path: "src/components/three/SceneCanvas.tsx"
      provides: "'use client' <Canvas> provider: dpr [1,2], frameloop from useAnimationGate, context-loss handler"
      contains: "dpr={[1, 2]}"
    - path: "src/components/three/ThemedScene.tsx"
      provides: "Rotating icosahedron wireframe; useThemeColors → THREE.Color for two accents"
      contains: "icosahedronGeometry"
    - path: "src/components/three/ClientScene.tsx"
      provides: "'use client' wrapper: dynamic(ssr:false) + IslandBoundary + ScenePoster fallback"
      contains: "ssr: false"
  key_links:
    - from: "src/components/three/SceneCanvas.tsx"
      to: "src/hooks/useAnimationGate.ts"
      via: "frameloop={shouldAnimate ? 'always' : 'never'}"
      pattern: "useAnimationGate"
    - from: "src/components/three/ThemedScene.tsx"
      to: "src/hooks/useThemeColors.ts"
      via: "useThemeColors(['--accent-blue','--accent-violet']) → new THREE.Color()"
      pattern: "useThemeColors"
    - from: "src/components/three/ClientScene.tsx"
      to: "src/components/IslandBoundary.tsx"
      via: "IslandBoundary fallback={<ScenePoster/>} wrapping dynamic SceneCanvas"
      pattern: "IslandBoundary"
    - from: "src/components/three/ClientScene.tsx"
      to: "src/components/three/SceneCanvas.tsx"
      via: "dynamic(() => import('./SceneCanvas'), { ssr:false, loading: ScenePoster })"
      pattern: "ssr:\\s*false"
---

<objective>
Build the SSR-safe WebGL island itself (FOUND-04, PERF-01): a `dynamic(ssr:false)` client wrapper composing Phase 3's `IslandBoundary` + a static poster, a DPR-clamped `SceneCanvas` provider whose frameloop is driven by `useAnimationGate` with a context-loss → poster handler, and ONE trivial themed scene (rotating icosahedron wireframe) that consumes `useThemeColors` inside the Canvas and reacts to `data-theme`. All NEW files — no existing component touched. The island is built here but NOT yet mounted on any route (that is Plan 04-03's demo + gate).

Depends on 04-01 (3D stack installed) AND on Phase 3 (consumes `useAnimationGate`, `useThemeColors`, `IslandBoundary` — confirmed present by 04-01's guard). Single GL context, one Canvas, named drei imports only.

Purpose: A thin, reusable boundary wiring Phase-3 primitives to R3F so Phase 5 can drop real scenes in without re-solving gating/theming/error-fallback. The poster-first pattern keeps `three` off the LCP path.
Output: `src/lib/webgl.ts`, `src/components/three/{ScenePoster,SceneCanvas,ThemedScene,ClientScene}.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-r3f-infrastructure/04-RESEARCH.md

<interfaces>
<!-- Phase 3 CONTRACTS this plan consumes (from 03-02-PLAN.md). Import and use exactly — do NOT re-author. -->

// src/hooks/useAnimationGate.ts  ('use client')
export function useAnimationGate<T extends Element>(
  ref: React.RefObject<T | null>,
  opts?: { rootMargin?: string; threshold?: number }
): { shouldAnimate: boolean; prefersReduced: boolean; inView: boolean; tabVisible: boolean };
// shouldAnimate = !prefersReduced && inView && tabVisible

// src/hooks/useThemeColors.ts  ('use client')
export function useThemeColors(varNames: string[]): Record<string, string>;
// returns RAW token strings (hex, e.g. '#60a5fa'); re-reads on data-theme MutationObserver change; never per-frame

// src/components/IslandBoundary.tsx  (Phase 3 — class error boundary)
// Renders `fallback` if a child throws during render. Import default; usage: <IslandBoundary fallback={<X/>}>{children}</IslandBoundary>
// NOTE: confirm the exact prop name from the file at execution time — if Phase 3 named it differently (e.g. `fallback`), match it.

// Token facts (from globals.css, verified): accents are HEX.
//   dark:  --accent-blue #60a5fa, --accent-violet #a78bfa
//   light: --accent-blue #3b82f6, --accent-violet #8b5cf6
// THREE.Color accepts hex strings directly. data-theme on <html> drives the swap (NOT Tailwind dark:).
</interfaces>

<precedent>
<!-- Existing island pattern to mirror (verified). -->
// src/app/page.tsx (line 1 'use client') already does: const FAQ = dynamic(() => import("@/components/FAQ"), { loading: ..., ssr: false });
// src/components/Hero3D.tsx: const Hero3DScene = dynamic(() => import("./Hero3DScene"), { ssr: false, loading: () => <div .../> });
// Repo hydration convention: `mounted` state + `if (!mounted) return null` guards anything touching window/document.
</precedent>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: WebGL probe + static ScenePoster (the LCP/fallback element)</name>
  <files>src/lib/webgl.ts, src/components/three/ScenePoster.tsx</files>
  <action>
    **Read first:** `src/components/Hero3D.tsx` (loading-div style for the poster shape), `src/app/globals.css` (token names: `--bg-primary`, `--bg-secondary`, `--accent-blue`).

    File 1 — `src/lib/webgl.ts` (research §Pattern 4). No React, no 'use client'. Export `isWebGLAvailable(): boolean`:
    - `if (typeof document === 'undefined') return false;` (never run on server).
    - try: create a canvas, return `!!(window.WebGLRenderingContext && (canvas.getContext('webgl2') || canvas.getContext('webgl')))`; catch → false.

    File 2 — `src/components/three/ScenePoster.tsx` (research §Pattern 5). `'use client'` (it's used as a React fallback element; keep it client for consistency). Accept `{ className?: string }`. Render a single full-size div that fills its parent (`width:100%; height:100%`) with a theme-token CSS gradient — e.g. `background: 'linear-gradient(160deg, var(--bg-secondary), var(--bg-primary))'` plus a subtle accent glow using `var(--accent-blue)` at low opacity (radial-gradient or box-shadow). It MUST size to the container so mounting the canvas on top causes ZERO layout shift (PERF-01 / Pitfall 2). No animation loop, no JS timers — it is the instantly-painted LCP element and the reduced-motion/no-WebGL view. Theme-aware for free via the tokens.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | (grep -iE "three/ScenePoster|lib/webgl" && exit 1 || exit 0)</automated>
  </verify>
  <done>`isWebGLAvailable()` exists, SSR-guarded, uses getContext('webgl2'||'webgl'). `ScenePoster` renders a full-size token-driven gradient (no hardcoded hex, no animation), type-checks clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: SceneCanvas provider (DPR clamp + frameloop gate + context-loss) and ThemedScene</name>
  <files>src/components/three/SceneCanvas.tsx, src/components/three/ThemedScene.tsx</files>
  <action>
    **Read first:** `src/hooks/useAnimationGate.ts` + `src/hooks/useThemeColors.ts` (confirm exact return shapes match the interfaces block above before importing).

    File 1 — `src/components/three/SceneCanvas.tsx` (research §Pattern 2). `'use client'`. Accept `{ className?: string }`.
    - `const wrapRef = useRef<HTMLDivElement>(null);` `const { shouldAnimate, prefersReduced } = useAnimationGate(wrapRef);`
    - `const [contextLost, setContextLost] = useState(false);`
    - `onCreated` callback: from `state.gl.domElement` add `webglcontextlost` (call `e.preventDefault()`, `setContextLost(true)`, `{ passive:false }`) and `webglcontextrestored` (`setContextLost(false)`) listeners (research Pattern 2 — context-loss → poster).
    - `if (contextLost) return <ScenePoster />;`
    - Render the wrapper div (`ref={wrapRef}`, full-size) containing one `<Canvas>` with: `dpr={[1, 2]}` (set EXPLICITLY so the budget is provable — research §Pattern 2), `frameloop={shouldAnimate ? 'always' : 'never'}` (binds the Phase-3 gate → pauses off-screen/tab-blur/reduced-motion), `gl={{ antialias: true, powerPreference: 'high-performance', failIfMajorPerformanceCaveat: false }}`, `onCreated`. Inside render `<ThemedScene paused={!shouldAnimate || prefersReduced} />`.
    - ONE Canvas only (research Anti-Pattern 2 — never multiple GL contexts).

    File 2 — `src/components/three/ThemedScene.tsx` (research §Pattern 3 — the #1 research flag, the theme bridge). `'use client'`. Accept `{ paused: boolean }`.
    - `import * as THREE from 'three'; import { useFrame } from '@react-three/fiber';` and `useThemeColors` from Phase 3.
    - `const t = useThemeColors(['--accent-blue', '--accent-violet']);`
    - `const edge = useMemo(() => new THREE.Color(t['--accent-blue'] || '#60a5fa'), [t['--accent-blue']]);` and `const fill = useMemo(() => new THREE.Color(t['--accent-violet'] || '#a78bfa'), [t['--accent-violet']]);` — fallback hex while colors hydrate; THREE.Color accepts the raw hex tokens directly.
    - `const mesh = useRef<THREE.Mesh>(null);` `useFrame((_, delta) => { if (paused || !mesh.current) return; mesh.current.rotation.x += delta * 0.3; mesh.current.rotation.y += delta * 0.4; });`
    - Render `<mesh ref={mesh}><icosahedronGeometry args={[1.4, 0]} /><meshBasicMaterial color={edge} wireframe /></mesh>` plus `<ambientLight color={fill} intensity={0.8} />` (proves the SECOND token also reacts). Passing a new `color` prop reconciles onto the material — NO remount on theme toggle (research §Pattern 3). Never hardcode hex except the hydration fallback. NO drei import needed.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | (grep -iE "three/SceneCanvas|three/ThemedScene" && exit 1 || exit 0)</automated>
  </verify>
  <done>`SceneCanvas` sets `dpr={[1, 2]}` explicitly, binds `frameloop` to `useAnimationGate`, handles `webglcontextlost`→poster, mounts exactly one `<Canvas>`. `ThemedScene` reads two accents via `useThemeColors`, builds `THREE.Color`s with `useMemo` keyed on the resolved tokens, rotates only when not paused, renders an icosahedron wireframe. Both type-check clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: ClientScene wrapper — dynamic(ssr:false) + IslandBoundary + poster</name>
  <files>src/components/three/ClientScene.tsx</files>
  <action>
    **Read first:** `src/components/IslandBoundary.tsx` (confirm the EXACT fallback prop name + default-vs-named export), `src/app/page.tsx` (the working `dynamic(ssr:false, loading)` shape).

    Create `src/components/three/ClientScene.tsx` (research §Pattern 1 — the elite future-proof wrapper). `'use client'` MUST be the first line (this is what makes `ssr:false` legal — Next 15 build-errors `dynamic(ssr:false)` in a server file).
    - `import dynamic from 'next/dynamic';`
    - `import IslandBoundary from '@/components/IslandBoundary';` (match the actual export style you read)
    - `import ScenePoster from './ScenePoster';`
    - `const SceneCanvas = dynamic(() => import('./SceneCanvas'), { ssr: false, loading: () => <ScenePoster /> });` — the `loading` view IS the poster (LCP + reduced-motion + pre-mount view).
    - Default-export `ClientScene(props: { className?: string })` returning `<IslandBoundary fallback={<ScenePoster />}><SceneCanvas {...props} /></IslandBoundary>` (so a runtime WebGL throw degrades to the poster, not error.tsx). Use the IslandBoundary's REAL prop name from the file.
    - Optionally gate on `isWebGLAvailable()` from `@/lib/webgl` behind a `mounted` flag (research Pattern 4/Pitfall 5) so "no WebGL at all" also lands on the poster; if added, guard with `mounted` state + the repo's `if (!mounted) return <ScenePoster/>` convention so the probe runs client-side only.
    - `ClientScene` is the ONLY symbol any page ever imports — never import `SceneCanvas`/`ThemedScene`/`three` directly from a page, layout, nav, or footer (Pitfall 1 — that is what keeps three route-split). Do NOT import this wrapper from any text/SEO route in this plan (no mount happens until 04-03).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | (grep -i "three/ClientScene" && exit 1 || exit 0)</automated>
  </verify>
  <done>`ClientScene.tsx` first line is `'use client'`, lazy-loads `SceneCanvas` with `ssr:false` + `ScenePoster` loading view, wraps it in `IslandBoundary` with a `ScenePoster` fallback (real prop name), and is the single public entry point. Type-checks clean. No page imports it yet.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean across `src/lib/webgl.ts` and `src/components/three/*`.
- `rg "ssr:\s*false" src/components/three/ClientScene.tsx` → match, and `ClientScene.tsx` line 1 is `'use client'` (ssr:false legal only in a client file).
- `rg "useAnimationGate" src/components/three/SceneCanvas.tsx` and `rg "useThemeColors" src/components/three/ThemedScene.tsx` → both match (Phase-3 primitives consumed, not re-authored).
- `rg "dpr=\{\[1, 2\]\}" src/components/three/SceneCanvas.tsx` → match (explicit DPR clamp).
- `rg -l "from 'three'|@react-three|ClientScene" src/app/{blog,developer-australia,expertise,services,tech-stack,projects} src/app/layout.tsx` → NOTHING (no scene mounted on text/SEO/shared routes this plan).
</verification>

<success_criteria>
- Reusable `ssr:false` Canvas island exists, composes Phase-3 IslandBoundary + poster, with WebGL-unavailable and context-loss both degrading to the poster (FOUND-04 island half).
- DPR clamped [1,2], frameloop bound to the Phase-3 gate, one GL context, theme bridge via useThemeColors → THREE.Color with no-remount updates (FOUND-04 + PERF foundation).
- three is reachable ONLY through the dynamic wrapper; no page/layout imports it yet (PERF-01 route-split preserved).
</success_criteria>

<output>
After completion, create `.planning/phases/04-r3f-infrastructure/04-02-SUMMARY.md`. Record: the exact IslandBoundary fallback prop name used, whether the isWebGLAvailable() pre-mount gate was added to ClientScene, and the public import surface (ClientScene only).
</output>
