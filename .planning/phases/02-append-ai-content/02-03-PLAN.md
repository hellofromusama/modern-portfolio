---
phase: 02-append-ai-content
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/AiBridgeDiagram.tsx
  - src/app/ai-engineering/page.tsx
  - src/components/Navigation.tsx
autonomous: true
requirements: [AICON-01, AICON-02, AICON-03, SEO-02, SHIP-01]
must_haves:
  truths:
    - "Visiting /ai-engineering returns a server-rendered page telling the ESIA MCP-server story with the 6-step query flow"
    - "An SVG architecture diagram of the MCP bridge renders visually and themes correctly in BOTH light and dark modes"
    - "The v1->v2 LangGraph evolution and the 3 production fixes read as a Problem->Architecture->Trade-offs->Impact narrative"
    - "The page exports its own metadata and renders page-level JSON-LD"
    - "An 'AI Engineering' link appears in the main Navigation (desktop + mobile)"
    - "No Horizon Digital or interview-prep content appears anywhere in the new files"
  artifacts:
    - path: "src/components/AiBridgeDiagram.tsx"
      provides: "Inline JSX SVG of the 6-step MCP bridge, themed via CSS custom properties, aria-labelled"
      contains: "var(--"
    - path: "src/app/ai-engineering/page.tsx"
      provides: "Server route: ESIA MCP narrative + 6-step flow + diagram + metadata + JSON-LD"
      contains: "export const metadata"
    - path: "src/components/Navigation.tsx"
      provides: "navItems entry { href: '/ai-engineering', label: 'AI Engineering' }"
      contains: "/ai-engineering"
  key_links:
    - from: "src/app/ai-engineering/page.tsx"
      to: "src/components/AiBridgeDiagram.tsx"
      via: "import + render of the SVG component"
      pattern: "AiBridgeDiagram"
    - from: "src/components/Navigation.tsx navItems"
      to: "/ai-engineering route"
      via: "new nav link, rendered by both desktop and mobile .map"
      pattern: "/ai-engineering"
---

<objective>
Create the net-new AI Engineering experience surface: a server route /ai-engineering telling the ESIA MCP-server production story (AICON-01/02), an inline theme-aware SVG architecture diagram (AICON-03), and a single Navigation entry. Server route is required because client pages cannot export metadata/JSON-LD (SEO-02).

Purpose: Deliver the deepest proof of "production AI engineer" — a full production-engineering case study with a visual system diagram.
Output: AiBridgeDiagram.tsx (CSS-var-themed SVG), ai-engineering/page.tsx (narrative + metadata + JSON-LD), Navigation.tsx nav item. Both themes work; no forbidden content.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-append-ai-content/02-RESEARCH.md

# Patterns to mirror — READ FIRST
@src/components/Navigation.tsx
@src/content/types.ts

<interfaces>
<!-- SERVER ROUTE pattern (mirror src/app/blog/page.tsx and expertise/page.tsx): -->
<!--   - NO "use client" directive (server component) -->
<!--   - export const metadata: Metadata = { title, description, keywords } -->
<!--   - page-level JSON-LD via -->
<!--     <script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(schema) }} /> -->
<!--   - render <Navigation /> at top (import from "@/components/Navigation") -->

<!-- THEMING: never hardcode theme colours. Use CSS custom-property tokens so both -->
<!-- themes work from one source. Available tokens (from globals.css): -->
<!--   --bg-primary, --bg-secondary, --bg-card, --border-subtle, --border-default -->
<!--   --text-primary, --text-secondary, --text-tertiary, --text-muted -->
<!--   --accent-blue (#60a5fa), --accent-violet (#a78bfa), --accent-emerald (#34d399) -->
<!--   --font-space-grotesk -->

<!-- Navigation.tsx (line 32) flat navItems array — both desktop (.map ~line 76) and -->
<!-- mobile (.map ~line 145) render from it. Adding ONE entry is the only nav change. -->
const navItems = [
  { href: '/', label: 'Home' },
  { href: '/ideas', label: 'Ideas' },
  { href: '/contact', label: 'Contact' },
  { href: '/budget', label: 'Budget' },
  { href: '/fund-me', label: 'Fund Me' },
];
<!-- isActivePage already handles pathname.startsWith(path) for active highlighting. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the theme-aware SVG architecture diagram component</name>
  <files>src/components/AiBridgeDiagram.tsx</files>
  <action>
    Create a server component (NO "use client") `export default function AiBridgeDiagram()` rendering an inline JSX `<svg>` of the MCP bridge 6-step flow. Every stroke/fill MUST use CSS custom-property tokens (e.g. `stroke="var(--border-default)"`, `fill="var(--bg-card)"`, accent arrows `stroke="var(--accent-blue)"`, text `fill="var(--text-primary)"`) so it themes automatically in both modes — NO hardcoded hex/theme colours.

    Render the 6 verified-fact nodes as labelled boxes connected by arrows in flow order:
    Marketing UI (NL question) -> MCP Server -> OAuth + NetSuite API -> SuiteScript RESTlet -> NetSuite DB -> Ollama LLM (format) -> Answer in UI.
    (Collapse to ~6 boxes; "OAuth + NetSuite API" and "SuiteScript RESTlet -> NetSuite DB" may be one box each as the research example shows.)

    Use `viewBox` with `className="w-full h-auto"`, a `<defs>` arrowhead `<marker>`, `role="img"`, and `aria-label="MCP bridge: marketing UI to NetSuite via OAuth and SuiteScript RESTlet, formatted by an Ollama LLM and returned to the UI"`. Keep it STATIC (no animation — motion infra lands Phase 3). Use the repo's font token via `fontFamily="var(--font-space-grotesk)"`.

    Do NOT include any Horizon Digital or interview-prep references in comments or labels.
  </action>
  <verify>
    <automated>cd "C:\Users\Usama.Javed\Desktop\modern-portfolio" && node ./node_modules/typescript/bin/tsc --noEmit src/components/AiBridgeDiagram.tsx 2>&1 | Select-String "AiBridgeDiagram" ; echo "EXIT:$LASTEXITCODE"</automated>
  </verify>
  <done>Component exists, exports default, renders an SVG using only var(--...) colours, has role="img" + aria-label + a marker arrowhead; tsc reports no error in this file; grep for "var(--" in the file returns matches and grep for hardcoded "#" hex theme colours in stroke/fill returns none.</done>
</task>

<task type="auto">
  <name>Task 2: Create the /ai-engineering server route (narrative + diagram + metadata + JSON-LD)</name>
  <files>src/app/ai-engineering/page.tsx</files>
  <action>
    Create a server component page (NO "use client") at src/app/ai-engineering/page.tsx mirroring blog/page.tsx structure. It MUST:

    1. `export const metadata: Metadata = { title: "AI Engineering — Production MCP & LLM Systems", description: "...", keywords: [...] }` (unique SEO; client pages can't do this — that's why this is a server route).
    2. Render `<Navigation />` (import from "@/components/Navigation") at the top.
    3. Render page-level JSON-LD via `<script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(schema) }} />` — a TechArticle or WebPage schema referencing the Person @id "https://www.usamajaved.com.au/#person".
    4. Import and render `<AiBridgeDiagram />` (from "@/components/AiBridgeDiagram") in an architecture section.
    5. Tell the ESIA MCP story (AICON-01) and the production narrative (AICON-02) as Problem -> Architecture -> Trade-offs -> Impact sections, using ONLY the verified facts:
       - Context: Ear Science Institute Australia (Perth), Senior Systems Developer; MCP server connecting a local Ollama LLM to NetSuite ERP for the marketing team.
       - 6-step query flow (render as an ordered/numbered list complementing the diagram): UI NL question ("Show me details for Jim Brown") -> MCP server -> OAuth + NetSuite API auth -> SuiteScript RESTlet queries DB -> data back to Ollama LLM -> formatted answer in UI.
       - Evolution: v1 simple query->fetch->answer; v2 added LangGraph multi-step reasoning + intelligent chunking.
       - 3 production fixes (Trade-offs/Impact): (a) 250-records-per-batch batching + query optimization to live within NetSuite script execution/governance limits; (b) explicit system-prompt definitions to disambiguate leads vs customers; (c) pagination through batches for large datasets.
    6. Use ONLY CSS-var tokens for all colours (var(--text-primary), var(--bg-card), etc.) so both themes work. NO hardcoded theme colours, NO percentage bars.
    7. Do NOT invent metrics beyond the verified facts. Do NOT include any Horizon Digital or interview-prep content.

    Match the surrounding server-page style (double quotes, semicolons, 2-space indent).
  </action>
  <verify>
    <automated>cd "C:\Users\Usama.Javed\Desktop\modern-portfolio" && node ./node_modules/typescript/bin/tsc --noEmit src/app/ai-engineering/page.tsx 2>&1 | Select-String "ai-engineering" ; echo "EXIT:$LASTEXITCODE"</automated>
  </verify>
  <done>Route file exists, exports metadata + default async/server component, renders Navigation + AiBridgeDiagram + JSON-LD script + the ESIA narrative with the 6-step flow + v1/v2 + 3 fixes; tsc reports no error in this file; no "Horizon"/"interview" strings present.</done>
</task>

<task type="auto">
  <name>Task 3: Add the AI Engineering nav item to Navigation.tsx</name>
  <files>src/components/Navigation.tsx</files>
  <action>
    In src/components/Navigation.tsx, add ONE entry to the `navItems` array (line 32): `{ href: '/ai-engineering', label: 'AI Engineering' }`. Place it after Home (so the AI surface is prominent): Home, AI Engineering, Ideas, Contact, Budget, Fund Me. Both desktop and mobile menus render from this same array and `isActivePage` already handles `startsWith` — no other edit needed. Match the file's single-quote style.

    Do NOT touch the hardcoded inline nav in projects/[id]/page.tsx (pre-existing, out of scope — Phase 5 owns nav visual upgrades; the new link legitimately won't appear on detail pages this phase).
  </action>
  <verify>
    <automated>cd "C:\Users\Usama.Javed\Desktop\modern-portfolio" && node ./node_modules/typescript/bin/tsc --noEmit src/components/Navigation.tsx 2>&1 | Select-String "Navigation.tsx" ; echo "EXIT:$LASTEXITCODE"</automated>
  </verify>
  <done>navItems contains { href: '/ai-engineering', label: 'AI Engineering' }; tsc reports no error in Navigation.tsx; existing nav entries unchanged.</done>
</task>

</tasks>

<verification>
- `node ./node_modules/typescript/bin/tsc --noEmit` surfaces no NEW errors in the three new/edited files.
- AiBridgeDiagram uses only var(--...) colours (theme-safe); has role="img" + aria-label.
- /ai-engineering exports metadata, renders Navigation + diagram + JSON-LD + the full ESIA narrative.
- Navigation has exactly one new entry; existing entries unchanged.
- No "Horizon" / "interview" strings in any of the three files (case-insensitive).
</verification>

<success_criteria>
- AICON-01: /ai-engineering presents the ESIA MCP story with the 6-step flow rendered visually.
- AICON-02: v1->v2 LangGraph + 3 production fixes read as a Problem->Architecture->Trade-offs->Impact narrative.
- AICON-03: SVG architecture diagram of the MCP bridge renders and themes in both modes.
- SEO-02: route carries its own metadata + JSON-LD.
- Nav link present (desktop + mobile). No forbidden content.
</success_criteria>

<output>
After completion, create `.planning/phases/02-append-ai-content/02-03-SUMMARY.md`
</output>
