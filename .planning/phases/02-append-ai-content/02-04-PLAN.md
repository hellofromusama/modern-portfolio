---
phase: 02-append-ai-content
plan: 04
type: execute
wave: 2
depends_on: ["02-01", "02-02", "02-03"]
files_modified: []
autonomous: false
requirements: [AICON-06, SEO-02, SHIP-01]
must_haves:
  truths:
    - "tsc --noEmit (local compiler) surfaces no NEW errors in the phase-touched files"
    - "npm run lint is clean on phase-touched files"
    - "npm run build succeeds and prerenders 12 /projects/[id] routes + the /ai-engineering route"
    - "node scripts/content-diff.mjs exits 0 (zero deletions)"
    - "Neither 'Horizon' nor 'interview' (case-insensitive) appears anywhere in src/"
    - "Owner confirms both light and dark themes render the new AI surfaces correctly"
  artifacts:
    - path: ".planning/phases/02-append-ai-content/02-04-SUMMARY.md"
      provides: "SHIP-01 gate evidence + count assertions + AICON-06 grep result + smoke evidence"
      contains: "SHIP-01"
  key_links:
    - from: "build manifest"
      to: "12 project routes + /ai-engineering"
      via: "generateStaticParams (12 slugs) + the new route"
      pattern: "ai-engineering"
    - from: "grep gate"
      to: "AICON-06 enforcement"
      via: "no Horizon/interview matches in src/"
      pattern: "horizon|interview"
---

<objective>
Run the cross-cutting SHIP-01 gate for Phase 2 and enforce AICON-06 via a grep gate, verify the SEO-02 count assertions, then collect both-theme smoke evidence. This plan touches NO source files — it is pure verification of the work merged from 02-01/02/03.

Purpose: Prove the phase is genuinely green (build masks errors, so tsc + lint run explicitly) and the forbidden content never shipped.
Output: 02-04-SUMMARY.md with gate evidence, count assertions, grep result, and curl smoke evidence; owner both-theme approval (checkpoint, auto-approved by orchestrator on evidence).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-append-ai-content/02-RESEARCH.md
@.planning/phases/02-append-ai-content/02-VALIDATION.md
@.planning/phases/02-append-ai-content/02-01-SUMMARY.md
@.planning/phases/02-append-ai-content/02-02-SUMMARY.md
@.planning/phases/02-append-ai-content/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SHIP-01 gate + AICON-06 grep gate + SEO-02 count assertions</name>
  <files>(verification only — no source edits)</files>
  <action>
    Run the full validation suite from the project root and capture output for the SUMMARY. Use the LOCAL TypeScript compiler (research Pitfall 2: `npx tsc` resolves to an unrelated global; use the node path).

    1. Type check: `node ./node_modules/typescript/bin/tsc --noEmit`. Confirm NO NEW errors in phase-touched files (src/content/projects.ts, src/content/seo.ts, src/content/skills.ts, src/app/layout.tsx, src/app/ai-engineering/page.tsx, src/components/AiBridgeDiagram.tsx, src/components/Navigation.tsx). Pre-existing errors in api/* and expertise/page.tsx are deferred (logged in deferred-items.md) — do NOT fix, just confirm they're the SAME pre-existing set.
    2. Lint: `npm run lint`. Confirm clean on phase-touched files.
    3. Zero-deletion gate: `node scripts/content-diff.mjs` -> must exit 0.
    4. Build: `npm run build`. Confirm success AND the route table shows 12 `/projects/[id]` prerendered routes (was 7) and a `/ai-engineering` route present.
    5. AICON-06 grep gate (case-insensitive, the orchestrator's acceptance criterion): search src/ for "Horizon" and "interview" — BOTH must return zero matches. Use ripgrep: `rg -i "horizon|interview" src/` -> expect no output. Also grep the build output: `rg -i "horizon digital|interview prep" .next/` -> no matches.
    6. SEO-02 count assertions: confirm numberOfItems (seo.ts) === itemListProjects.length === 7; homeGridProjects.length === 11; sitemapProjects.length === 12; projectList.length === 12.

    Record every command's result (pass/fail + key numbers) for the SUMMARY.
  </action>
  <verify>
    <automated>cd "C:\Users\Usama.Javed\Desktop\modern-portfolio" && node ./node_modules/typescript/bin/tsc --noEmit ; npm run lint ; node scripts/content-diff.mjs ; npm run build ; rg -i "horizon|interview" src/ ; echo "GREP_DONE"</automated>
  </verify>
  <done>tsc shows no new errors in phase files; lint clean on phase files; content-diff exits 0; build succeeds with 12 project routes + /ai-engineering; `rg -i "horizon|interview" src/` returns zero matches; count assertions all hold (12/11/12/7).</done>
</task>

<task type="auto">
  <name>Task 2: Both-theme curl smoke evidence (production-build server)</name>
  <files>(verification only — no source edits)</files>
  <action>
    Start the production server (`npm run start` after the Task 1 build, run in background) and gather curl-based evidence for the SUMMARY:
    - `curl -s http://localhost:3000/ai-engineering` -> contains the ESIA narrative + the 6-step flow + an inline `<svg>` (the diagram) + a `<script type="application/ld+json">` block + the metadata `<title>`.
    - `curl -s http://localhost:3000/projects/mcp-netsuite-ollama-bridge` -> 200 with the MCP detail content (and spot-check one more new slug, e.g. /projects/healthcare-voice-agent).
    - `curl -s http://localhost:3000/sitemap.xml` -> 12 `/projects/` URLs.
    - `curl -s http://localhost:3000/` -> home grid shows 11 project cards and the new AI skill groups; nav includes "AI Engineering".
    Theme correctness (light + dark) is a visual property driven by CSS vars + the data-theme attribute and cannot be asserted via curl — defer that to the owner checkpoint below. Record curl evidence (status codes + presence of expected substrings) in the SUMMARY. Stop the server when done.
  </action>
  <verify>
    <automated>cd "C:\Users\Usama.Javed\Desktop\modern-portfolio" && curl -s -o NUL -w "%{http_code}`n" http://localhost:3000/ai-engineering ; curl -s -o NUL -w "%{http_code}`n" http://localhost:3000/projects/mcp-netsuite-ollama-bridge</automated>
  </verify>
  <done>/ai-engineering returns 200 with narrative + 6-step flow + inline SVG + JSON-LD; new project detail pages return 200; sitemap.xml lists 12 /projects/ URLs; home shows 11 cards + AI skill groups + nav link; evidence captured in SUMMARY.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Owner both-theme smoke approval (checkpoint)</name>
  <action>Present the both-theme smoke evidence from Task 2 and pause for owner confirmation. Orchestrator auto-approves on the captured curl + grep evidence.</action>
  <what-built>Phase 2 AI content surfaces: /ai-engineering route (ESIA MCP story + 6-step flow + SVG architecture diagram), 5 new AI project detail pages, 11-card home grid, AI skill groups, nav link — all from the centralized content source, plus AICON-06 grep gate clean.</what-built>
  <how-to-verify>
    1. Run `npm run start` and open http://localhost:3000/ai-engineering — confirm the ESIA MCP narrative, the 6-step flow, and the architecture SVG render.
    2. Toggle the theme (ThemeToggle in nav) and confirm the diagram + page colours adapt correctly in BOTH dark and light (no hardcoded colours, no unreadable text).
    3. Open the home page — confirm 11 project cards and the new AI skill groups (dot+text, no percentage bars), and the "AI Engineering" nav link.
    4. Open http://localhost:3000/projects/mcp-netsuite-ollama-bridge — confirm a populated detail page.
    5. Confirm nothing referencing Horizon Digital or interview prep appears anywhere.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (orchestrator auto-approves on the curl + grep evidence captured in the SUMMARY).</resume-signal>
</task>

</tasks>

<verification>
- SHIP-01: tsc (local) + lint clean on phase files; build succeeds; both-theme smoke evidenced.
- AICON-06: `rg -i "horizon|interview" src/` returns zero matches.
- SEO-02 counts: projectList 12, homeGrid 11, sitemap 12 /projects/ URLs, ItemList numberOfItems 7.
- content-diff exits 0 (no baseline deletion).
</verification>

<success_criteria>
- Full SHIP-01 gate green; AICON-06 grep gate clean; all SEO-02 count assertions hold.
- Owner both-theme smoke approved (evidence-backed).
- 02-04-SUMMARY.md records gate output, counts, grep result, and curl evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/02-append-ai-content/02-04-SUMMARY.md`
</output>
