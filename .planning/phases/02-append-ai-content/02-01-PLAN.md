---
phase: 02-append-ai-content
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/projects.ts
  - src/content/seo.ts
autonomous: true
requirements: [AICON-04, SEO-02, SHIP-01]
must_haves:
  truths:
    - "Visiting /projects/mcp-netsuite-ollama-bridge (and the 4 other new slugs) returns a populated detail page, not a 404"
    - "The home project grid renders 11 cards (6 existing + 5 new AI projects)"
    - "The sitemap lists 12 /projects/ URLs"
    - "The JSON-LD ItemList contains 7 items and numberOfItems === 7"
    - "All 7 pre-existing projects render byte-identical to baseline (content-diff stays green)"
  artifacts:
    - path: "src/content/projects.ts"
      provides: "5 new AI Project entries in the keyed map + their ids appended to projections"
      contains: "mcp-netsuite-ollama-bridge"
    - path: "src/content/seo.ts"
      provides: "numberOfItems bumped 6 -> 7 to match itemListProjects.length"
      contains: "numberOfItems: 7"
  key_links:
    - from: "src/content/projects.ts (keyed map)"
      to: "src/app/projects/[id]/page.tsx generateStaticParams"
      via: "projectList = Object.values(projects)"
      pattern: "mcp-netsuite-ollama-bridge"
    - from: "itemListProjects (1 new flagship appended)"
      to: "seo.ts numberOfItems"
      via: "manual literal bump must equal array length"
      pattern: "numberOfItems: 7"
---

<objective>
Append the 5 new AI projects (AICON-04) to the centralized content source so their detail pages, home-grid cards, sitemap URLs, and JSON-LD ItemList entry all flow automatically from Phase 1's wiring (SEO-02). This is a data-append exercise — zero JSX changes to consumers.

Purpose: Surface "production AI engineer" — the rarest half of the Core Value — in the project list and SEO surfaces.
Output: 5 new Project entries (12-field detail population), grown projections (12 detail / 11 grid / 12 sitemap / 7 ItemList), numberOfItems bumped to 7. content-diff stays green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-append-ai-content/02-RESEARCH.md

# Append targets — READ FIRST before editing
@src/content/projects.ts
@src/content/seo.ts
@src/content/types.ts

<interfaces>
<!-- The Project superset shape (src/content/types.ts). New entries MUST satisfy this. -->
<!-- Detail-page REQUIRED fields (verified against src/app/projects/[id]/page.tsx): -->
<!-- id, title, subtitle, description, longDescription (use \n\n for paragraphs), -->
<!-- tech[] (>=1), category, timeline, team, features[] (>=1), challenges[] (>=1), -->
<!-- results[] (>=1), gradient (Tailwind "from-X to-Y"). liveUrl? is OPTIONAL — omit it -->
<!-- for personal experiments with no public URL. -->
<!-- Grid fields REQUIRED only because all 5 go in homeGridProjects: -->
<!-- gridTitle, gridDescription, gridTech[], gridCategory. -->
<!-- seo* fields (seoName, seoDescription, applicationCategory) REQUIRED only for the -->
<!-- ONE project added to itemListProjects (the MCP flagship). -->
<!-- applicationCategory union: "BusinessApplication" | "DeveloperApplication" | "WebApplication". -->

export interface Project {
  id: string;
  title: string; subtitle: string; description: string; longDescription: string;
  tech: string[]; category: string; timeline: string; team: string;
  features: string[]; challenges: string[]; results: string[];
  gradient: string; liveUrl?: string;
  gridTitle?: string; gridDescription?: string; gridTech?: string[]; gridCategory?: string;
  seoName?: string; seoDescription?: string; applicationCategory?: ApplicationCategory;
  sitemapPriority?: number;
}

<!-- Three projection arrays at the bottom of projects.ts are hand-listed id arrays. -->
<!-- A new map entry does NOT auto-appear in them — you append its id explicitly: -->
<!--   homeGridProjects (currently 6 ids) -> add all 5 -->
<!--   sitemapProjects  (currently 7 {id, priority}) -> add all 5 -->
<!--   itemListProjects (currently 6 ids) -> add ONLY "mcp-netsuite-ollama-bridge" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append the 5 AI Project entries to the keyed map in projects.ts</name>
  <files>src/content/projects.ts</files>
  <action>
    Append 5 new entries INSIDE the `const projects = { ... } satisfies Record<string, Project>` object (after the existing "cloud-infrastructure" entry on line 339, before the closing `}` on line 340). DO NOT touch any of the 7 existing entries (any change to baseline copy fails the content-diff gate as CHANGED).

    Use these exact slug keys (the route URL = the key): `mcp-netsuite-ollama-bridge`, `linkedin-job-auto-apply-agent`, `self-learning-social-media-generator`, `n8n-marketing-research-workflows`, `healthcare-voice-agent`.

    Populate ALL 12 required detail fields per entry (id, title, subtitle, description, longDescription, tech[], category, timeline, team, features[], challenges[], results[], gradient) PLUS the 4 grid fields (gridTitle, gridDescription, gridTech[], gridCategory) for ALL 5. Add `sitemapPriority: 0.8` for production/work projects (MCP bridge, n8n workflows), `0.7` for personal/architecture (LinkedIn agent, social generator, healthcare voice agent). Omit `liveUrl` (no public URLs).

    For the MCP flagship ONLY, also add the 3 seo fields: `seoName`, `seoDescription`, `applicationCategory: "BusinessApplication"`.

    CONTENT — use ONLY these verified facts (do NOT invent metrics; the audience is technical):
    - (1) "mcp-netsuite-ollama-bridge" — title "MCP NetSuite-Ollama Bridge", category "AI Engineering", PRODUCTION @ Ear Science Institute Australia (Perth), team "1 developer (Senior Systems Developer @ ESIA)". An MCP server connecting a local Ollama LLM to NetSuite ERP for the marketing team. Frame longDescription as Problem -> Architecture -> Trade-offs -> Impact. 6-step flow: marketing UI natural-language question ("Show me details for Jim Brown") -> MCP server -> OAuth + NetSuite API auth -> SuiteScript RESTlet queries the DB -> data returned to Ollama LLM -> formatted answer to the UI. v1 = simple query->fetch->answer; v2 = added LangGraph multi-step reasoning + intelligent chunking. challenges[] = the 3 production problems: NetSuite script execution/governance limits on large datasets; LLM conflating leads vs customers; paging through large result sets. results[] = the 3 fixes: 250-records-per-batch batching + query optimization restored throughput within execution limits; explicit system-prompt definitions disambiguated leads vs customers; pagination through batches enabled large-dataset queries. tech: ["MCP","Ollama","NetSuite","SuiteScript","LangGraph","OAuth 2.0","Python"]. gradient "from-blue-500 to-violet-600". seoName "MCP NetSuite-Ollama Bridge", applicationCategory "BusinessApplication".
    - (2) "linkedin-job-auto-apply-agent" — title "LinkedIn Job Auto-Apply Agent", category "AI Agents", PERSONAL project. AI agent that automates job-application workflows on LinkedIn. tech: ["AI Agents","Automation","Python"]. gradient "from-violet-500 to-purple-600".
    - (3) "self-learning-social-media-generator" — title "Self-Learning Social Media Generator", category "AI Agents", PERSONAL. LLM-driven content generator with feedback loops that improve output over time. tech: ["LLM","Feedback Loops","Content Generation","Python"]. gradient "from-emerald-500 to-teal-600".
    - (4) "n8n-marketing-research-workflows" — title "n8n Marketing Research Workflows", category "Automation", WORK project. n8n automation workflows for marketing research. tech: ["n8n","Automation","APIs"]. gradient "from-amber-500 to-orange-600". sitemapPriority 0.8.
    - (5) "healthcare-voice-agent" — title "Healthcare Voice Agent (Architecture)", category "AI Engineering", ARCHITECTURE/design project. Voice-AI agent architecture for healthcare. tech: ["Voice AI","Ollama","FastAPI","n8n"]. gradient "from-cyan-500 to-blue-600".

    For each, write 4-8 features[], 3-4 challenges[], 3-4 results[], a multi-paragraph longDescription (`\n\n` between paragraphs), distinct grid copy, a one-line subtitle, and a 1-2 sentence description. Keep claims grounded in the verified facts; for personal/architecture projects describe capability and design intent rather than fabricated production metrics.

    Match the file's existing style (2-space indent, double quotes for strings, trailing commas).
  </action>
  <verify>
    <automated>cd "C:\Users\Usama.Javed\Desktop\modern-portfolio" && node ./node_modules/typescript/bin/tsc --noEmit src/content/projects.ts 2>&1 | Select-String "projects.ts" ; echo "EXIT:$LASTEXITCODE"</automated>
  </verify>
  <done>5 new entries present in the map; `node ./node_modules/typescript/bin/tsc --noEmit` reports NO errors originating in src/content/projects.ts (pre-existing errors in api/* and expertise/page.tsx are out of scope); all 12 detail fields + 4 grid fields populated per entry; MCP entry has the 3 seo fields; no existing entry altered.</done>
</task>

<task type="auto">
  <name>Task 2: Append ids to the 3 projections and bump numberOfItems 6 -> 7</name>
  <files>src/content/projects.ts, src/content/seo.ts</files>
  <action>
    In src/content/projects.ts, append to the three hand-listed projection arrays at the bottom of the file. DO NOT reorder or remove any existing id (the diff gate checks membership and order of baseline entries).

    1. `homeGridProjects` (currently 6 ids, line ~356): append all 5 new ids -> 11 total. The grid renders these with no `.slice`, so all 5 cards appear.
    2. `sitemapProjects` (currently 7 {id, priority}, line ~379): append all 5 as `{ id, priority }` — use 0.8 for "mcp-netsuite-ollama-bridge" and "n8n-marketing-research-workflows", 0.7 for the other three -> 12 total.
    3. `itemListProjects` (currently 6 ids, line ~367): append ONLY "mcp-netsuite-ollama-bridge" (the production flagship; keep the curated list high-signal per orchestrator decision) -> 7 total. Do NOT add the other 4.

    Then in src/content/seo.ts, change the hardcoded `numberOfItems: 6` (line 21) to `numberOfItems: 7` to match the new itemListProjects.length. This is the manual-discipline bump research Pitfall 1 warns about — the diff gate only enforces `>=`, so it won't catch an under-count; you MUST keep numberOfItems === itemListProjects.length.
  </action>
  <verify>
    <automated>cd "C:\Users\Usama.Javed\Desktop\modern-portfolio" && node scripts/content-diff.mjs ; echo "DIFF_EXIT:$LASTEXITCODE"</automated>
  </verify>
  <done>homeGridProjects has 11 ids, sitemapProjects has 12 entries, itemListProjects has 7 ids; seo.ts numberOfItems is 7 (=== itemListProjects.length); `node scripts/content-diff.mjs` exits 0 (zero deletions — additions allowed).</done>
</task>

</tasks>

<verification>
- `node scripts/content-diff.mjs` exits 0 (no baseline project/skill/ItemList/sitemap entry deleted or changed).
- `node ./node_modules/typescript/bin/tsc --noEmit` surfaces no NEW errors in src/content/projects.ts or src/content/seo.ts (pre-existing api/* + expertise errors deferred).
- numberOfItems (seo.ts) === itemListProjects.length === 7.
- The 5 new slugs exist as map keys (will become detail routes via generateStaticParams in 02-04's build).
</verification>

<success_criteria>
- 5 AI projects appended to the keyed map with full 12-field detail population + 4 grid fields each; MCP flagship additionally has seoName/seoDescription/applicationCategory.
- Projections grown: detail 7->12, grid 6->11, sitemap 7->12, ItemList 6->7; numberOfItems bumped to 7.
- content-diff green; no existing project entry touched (AICON-04 "existing untouched"; SEO-02 propagation).
</success_criteria>

<output>
After completion, create `.planning/phases/02-append-ai-content/02-01-SUMMARY.md`
</output>
