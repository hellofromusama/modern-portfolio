---
phase: 02-append-ai-content
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/skills.ts
  - src/app/layout.tsx
autonomous: true
requirements: [AICON-05, SEO-02, SHIP-01]
must_haves:
  truths:
    - "The home skills section renders the 4 existing groups PLUS new AI skill groups, grouped by domain"
    - "No percentage bars appear — skills stay a dot+text list"
    - "The layout Person JSON-LD knowsAbout array includes AI domain terms (MCP, RAG, LangGraph, vector databases, FastAPI, AWS Bedrock)"
    - "All 4 pre-existing skill groups render byte-identical to baseline (content-diff stays green)"
  artifacts:
    - path: "src/content/skills.ts"
      provides: "AI skill groups appended to the skills array, reusing the 4 closed-union accents"
      contains: "MCP"
    - path: "src/app/layout.tsx"
      provides: "AI domain terms appended to the knowsAbout array"
      contains: "knowsAbout"
  key_links:
    - from: "src/content/skills.ts"
      to: "src/app/page.tsx skills.map + accentMap"
      via: "every new group's accent is one of blue|violet|emerald|amber"
      pattern: "accent: \"(blue|violet|emerald|amber)\""
---

<objective>
Append AI skill domains to the centralized skills source (AICON-05) and strengthen the Person JSON-LD with AI terms (SEO-02). The home skills renderer is already a dot+text list (no bars) and maps whatever groups exist, so new groups appear automatically — provided every new accent reuses the closed 4-value union.

Purpose: Show breadth of production AI capability grouped by domain, tied to the real projects added in 02-01.
Output: New Skill groups in skills.ts (reusing blue|violet|emerald|amber); AI terms appended to layout knowsAbout. content-diff stays green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-append-ai-content/02-RESEARCH.md

# Append targets — READ FIRST before editing
@src/content/skills.ts
@src/content/types.ts

<interfaces>
<!-- Skill shape (src/content/types.ts). accent is a CLOSED 4-value union — -->
<!-- new groups MUST reuse blue|violet|emerald|amber (do NOT invent a 5th colour; -->
<!-- it would ripple into types.ts + accentMap in page.tsx + the gate's tolerance). -->
interface Skill { title: string; accent: "blue" | "violet" | "emerald" | "amber"; items: string[]; }

<!-- Home consumer accentMap (src/app/page.tsx lines 39-44) has exactly these 4 keys. -->
<!-- The renderer (skills.map) does NOT require 4 items per group — any item count is fine. -->
<!-- The content-diff gate only verifies the 4 BASELINE groups/items still present; -->
<!-- it never counts new groups. Keep new groups in the identical -->
<!-- { title, accent, items: [...] } literal form so the extractor stays happy. -->

<!-- layout.tsx: the Person JSON-LD knowsAbout array starts at line 176 -->
<!-- ("knowsAbout: ["). The content-diff gate does NOT track this array, so appending -->
<!-- terms is safe and high-SEO-value. Append, never remove existing terms. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append AI skill groups to skills.ts (reusing the 4 accents)</name>
  <files>src/content/skills.ts</files>
  <action>
    Append new `Skill` groups to the `skills: Skill[]` array (after the existing "Specializations" group on line 9, before the closing `]` on line 10). DO NOT modify the 4 existing groups (any change fails the diff gate).

    Collapse the 10 AI domains from the brief into a sensible set of grouped entries (4 recommended), each reusing one of the 4 accents. Recommended grouping (Claude may refine, but MUST reuse only blue|violet|emerald|amber):
    - { title: "AI Protocols & Agents", accent: "amber", items: ["MCP, A2A, ACP", "LangChain / LangGraph", "CrewAI / AutoGen", "OpenAI Agents SDK", "Semantic Kernel", "LlamaIndex"] }
    - { title: "RAG & Retrieval", accent: "blue", items: ["Full RAG pipelines", "Hybrid search & rerankers", "Vector DBs: Chroma / Pinecone / Milvus", "OpenSearch / Aurora pgvector"] }
    - { title: "LLM Engineering", accent: "violet", items: ["Context / session / long-term memory", "System prompts & few-shot / CoT", "Prompt-injection defense", "Python & FastAPI"] }
    - { title: "AI on AWS & Observability", accent: "emerald", items: ["Bedrock / OpenSearch Serverless", "Lambda / ECS / Fargate / S3", "LangSmith / Langfuse / Phoenix", "Ragas / OpenTelemetry"] }

    These items reference the same technologies used in the projects appended in 02-01, satisfying AICON-05's "tied to real projects". No percentage bars — the renderer is a dot+text list. Match existing style (2-space indent, double quotes, trailing comma after the last new group).
  </action>
  <verify>
    <automated>cd "C:\Users\Usama.Javed\Desktop\modern-portfolio" && node scripts/content-diff.mjs ; echo "DIFF_EXIT:$LASTEXITCODE"</automated>
  </verify>
  <done>New AI skill groups appended; every new group's accent is one of blue|violet|emerald|amber; the 4 baseline groups untouched; `node scripts/content-diff.mjs` exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Append AI domain terms to the layout knowsAbout array</name>
  <files>src/app/layout.tsx</files>
  <action>
    In the Person JSON-LD `knowsAbout: [` array (starts line 176), APPEND AI domain terms to strengthen the skill signal (SEO-02). Do NOT remove or reorder existing terms — additive only.

    Append terms such as: "Model Context Protocol (MCP)", "Retrieval-Augmented Generation (RAG)", "LangChain", "LangGraph", "Vector Databases", "Pinecone", "Chroma", "Ollama", "FastAPI", "AWS Bedrock", "AI Agents", "LLM Observability", "Prompt Engineering", "NetSuite Integration". Match the existing array's string style and indentation. The content-diff gate does not track knowsAbout, so this is gate-safe.

    Touch ONLY the knowsAbout array — do not alter any other JSON-LD block or metadata in layout.tsx.
  </action>
  <verify>
    <automated>cd "C:\Users\Usama.Javed\Desktop\modern-portfolio" && node ./node_modules/typescript/bin/tsc --noEmit src/app/layout.tsx 2>&1 | Select-String "layout.tsx" ; echo "EXIT:$LASTEXITCODE"</automated>
  </verify>
  <done>knowsAbout array contains the appended AI terms; no existing term removed; `tsc --noEmit` reports no NEW error in layout.tsx; content-diff still green (run in 02-04 wave merge).</done>
</task>

</tasks>

<verification>
- `node scripts/content-diff.mjs` exits 0 (4 baseline skill groups intact).
- Every new Skill group uses an accent in {blue, violet, emerald, amber}.
- `tsc --noEmit` surfaces no new error in skills.ts or layout.tsx.
- knowsAbout grew (additive), no existing terms removed.
</verification>

<success_criteria>
- AI skill domains appended to centralized skills.ts, grouped by domain, no percentage bars, tied to the real projects from 02-01 (AICON-05).
- Person JSON-LD knowsAbout enriched with AI terms (SEO-02).
- content-diff green; 4 baseline groups untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/02-append-ai-content/02-02-SUMMARY.md`
</output>
