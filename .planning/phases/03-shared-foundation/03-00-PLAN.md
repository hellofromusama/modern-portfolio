---
phase: 03-shared-foundation
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - scripts/contrast-check.mjs
  - .planning/phases/03-shared-foundation/03-token-usage-audit.md
autonomous: true
requirements: [FOUND-02, FOUND-03]
must_haves:
  truths:
    - "motion@^12.40.0 is installed and resolves clean on React 19.2.4"
    - "A repeatable contrast-check script asserts every text-role token meets WCAG AA in BOTH themes"
    - "An audit document lists every information-bearing use of --text-faint/--text-ghost/--text-muted"
  artifacts:
    - path: "scripts/contrast-check.mjs"
      provides: "WCAG relative-luminance pass/fail table for both theme palettes"
      min_lines: 40
    - path: ".planning/phases/03-shared-foundation/03-token-usage-audit.md"
      provides: "Per-usage Strategy A/B decision table for faint/ghost/muted"
      min_lines: 20
  key_links:
    - from: "package.json"
      to: "node_modules/motion"
      via: "npm install motion@^12.40.0"
      pattern: "\"motion\":"
---

<objective>
Wave 0 enabling work for Phase 3: install the single new dependency (`motion@^12.40.0`), build a repeatable WCAG contrast-check script, and produce the per-usage token audit that decides which `--text-faint`/`--text-ghost`/`--text-muted` usages convey real information (and must move up a tier) versus which are purely decorative.

This plan touches NO source/CSS yet — it establishes the dependency and the two artifacts every later Phase-3 plan depends on. Execution of this entire phase begins ONLY after Phase 2 completes (Phase 3 depends_on Phase 2 per ROADMAP).

Purpose: De-risk FOUND-03 (Open Question 1 from research — exact text-tier usages are unknown) and FOUND-02 (motion install) before any edits.
Output: motion installed, `scripts/contrast-check.mjs`, `03-token-usage-audit.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-shared-foundation/03-RESEARCH.md

<interfaces>
<!-- Final AA-passing token values the contrast-check script must assert against (from research §Failing pairs). -->
<!-- DARK theme over --bg-primary #0a0a0f; LIGHT theme over --bg-primary #f8fafc. -->
<!-- AA thresholds: 4.5 normal text, 3.0 large/UI/decoration. -->

Text-role tokens that MUST be ≥ 4.5 (normal text) after Plan 03-01:
  dark  --text-primary #ffffff (19.75) | --text-secondary white@0.5 (5.36) | --text-muted → white@0.45 (4.49)
  light --text-primary #0f172a (17.06) | --text-secondary #334155 (9.90) | --text-tertiary #475569 (7.24) | --text-muted #64748b (4.55) | --text-faint → #64748b (4.55)
Accent-as-text tokens that MUST be ≥ 4.5 in LIGHT theme after Plan 03-01:
  --accent-blue → #2563eb (4.94) | --accent-violet → #7c3aed (5.45) | --accent-emerald → #047857 (5.24)
Decorative-only tokens (documented contract — NOT asserted for 4.5, may stay below):
  --text-faint (dark, white@0.2), --text-ghost (both themes), --text-tertiary (dark, white@0.4 large-only)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install motion@^12.40.0 (single new dep)</name>
  <files>package.json, package-lock.json</files>
  <action>
    If `node_modules/` is absent, run `npm ci` first (lockfile-faithful baseline — research Environment §, PITFALLS #8).
    Then run exactly: `npm install motion@^12.40.0`
    Do NOT pass `-D`; do NOT install `@types/*` (motion ships its own types); do NOT install `@emotion/is-prop-valid` (optional peer, ignore the warning); do NOT install `three`/`@react-three/fiber`/`@react-three/drei` (Phase 4 — explicitly out of scope per orchestrator decision).
    Confirm `package.json` `dependencies` gained `"motion": "^12.40.0"` and `package-lock.json` updated. React 19.2.4 is the installed line — motion's peer `react: ^18||^19` is satisfied (research verified live 2026-06-12).
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.dependencies.motion) throw new Error('motion missing'); require.resolve('motion/react'); console.log('motion', p.dependencies.motion, 'resolves motion/react OK')"</automated>
  </verify>
  <done>`package.json` lists `motion: ^12.40.0`; `require.resolve('motion/react')` succeeds; no three/R3F added.</done>
</task>

<task type="auto">
  <name>Task 2: Write repeatable WCAG contrast-check script</name>
  <files>scripts/contrast-check.mjs</files>
  <action>
    Create `scripts/contrast-check.mjs` — a dependency-free Node ESM script implementing the WCAG 2.1 relative-luminance contrast formula (same computation used in research §Contrast audit). It must:
    - Define the two backgrounds: DARK `--bg-primary` #0a0a0f, LIGHT `--bg-primary` #f8fafc.
    - Define each text-role token's FINAL intended value per the `<interfaces>` block above (both themes), including alpha-over-background compositing for the rgba() dark tokens (composite white@alpha over #0a0a0f before computing luminance).
    - For each token, compute the contrast ratio and assert it meets its threshold: text-role tokens require ≥ 4.5; explicitly-flagged decorative tokens (--text-faint dark, --text-ghost both, --text-tertiary dark) are printed but asserted only ≥ 3.0 OR labeled DECORATIVE (skip-asserted) — match the research's Strategy-A-plus-B recommendation.
    - Print a pass/fail table for BOTH themes (token | theme | ratio | threshold | PASS/FAIL).
    - `process.exit(1)` if any non-decorative token fails its threshold; exit 0 otherwise.
    This script reads the token values from a constant block at the top of the file (the script is the single source for CI-less re-verification; Plan 03-01 edits globals.css to match these exact values, then re-runs this script).
  </action>
  <verify>
    <automated>node scripts/contrast-check.mjs</automated>
  </verify>
  <done>`node scripts/contrast-check.mjs` prints both-theme tables and exits 0 with the final AA-passing values from the interfaces block; exits 1 if any text-role token is below 4.5.</done>
</task>

<task type="auto">
  <name>Task 3: Grep audit of faint/ghost/muted text usages (Strategy A vs B decision)</name>
  <files>.planning/phases/03-shared-foundation/03-token-usage-audit.md</files>
  <action>
    Resolve research Open Question 1. Grep the codebase for every usage of the three low-contrast tiers and classify each as DECORATIVE (divider, ghost numeral, icon, dot, large display) vs INFORMATION-BEARING (readable label/caption/body text — especially the 10–11px uppercase labels PITFALLS #6 flags).
    Run these and capture results (118 known occurrences across 14 files):
      rg -n "var\(--text-faint\)" src/
      rg -n "var\(--text-ghost\)" src/
      rg -n "var\(--text-muted\)" src/
    For each information-bearing hit, record: file:line, the text it colors, and the prescribed fix (promote to a tier that passes AA — faint→muted, ghost→muted/secondary, or muted already passes after the dark-theme white@0.45 nudge so most muted usages need no per-file change).
    Write `03-token-usage-audit.md` with: (1) a summary count, (2) a table `file:line | token | role (decorative/text) | action`, (3) the final list of file edits Plan 03-01 must make (the per-usage relabels). Known starting points to verify: `Hero3D.tsx` uses `--text-faint` for the 8+-years caption (line ~67, INFORMATION-BEARING → promote) and the `[10px] uppercase` stat labels (lines ~95/105, decorative-display large-tracking → planner's call, lean promote since they convey info).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const t=fs.readFileSync('.planning/phases/03-shared-foundation/03-token-usage-audit.md','utf8'); if(!/file.*line/i.test(t) || t.length < 400) throw new Error('audit incomplete'); console.log('audit present', t.length, 'chars')"</automated>
  </verify>
  <done>`03-token-usage-audit.md` exists with a per-usage table and an explicit list of information-bearing relabels for Plan 03-01 to apply; decorative usages documented as keep-as-is under Strategy A.</done>
</task>

</tasks>

<verification>
- `npm install motion@^12.40.0` succeeded; `motion/react` resolves; no three/R3F added.
- `node scripts/contrast-check.mjs` exits 0 against the final AA values.
- `03-token-usage-audit.md` enumerates information-bearing vs decorative usages with per-file actions.
</verification>

<success_criteria>
- motion@^12.40.0 in package.json + lockfile; React 19.2.4 line clean.
- Repeatable contrast script present and green against final token values.
- Token-usage audit complete — drives Plan 03-01's per-usage relabels.
</success_criteria>

<output>
After completion, create `.planning/phases/03-shared-foundation/03-00-SUMMARY.md`
</output>
