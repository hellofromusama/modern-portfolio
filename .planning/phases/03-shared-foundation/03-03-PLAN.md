---
phase: 03-shared-foundation
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - src/app/error.tsx
  - src/app/global-error.tsx
  - src/components/IslandBoundary.tsx
  - src/components/Hero3DScene.tsx
  - src/components/IdeaNetworkCanvas.tsx
  - src/components/InteractiveGlobe.tsx
  - src/components/Hero3D.tsx
  - src/app/ideas/page.tsx
  - src/app/fund-me/page.tsx
autonomous: true
requirements: [FOUND-05, FOUND-02, PERF-04]
must_haves:
  truths:
    - "error.tsx and global-error.tsx exist; a thrown island render error shows a fallback, not a white screen"
    - "Each of the 3 canvas islands is wrapped in IslandBoundary with a static-poster fallback"
    - "Each canvas's rAF loop stops when off-screen, on tab blur, and under reduced motion (drawing one final static frame)"
  artifacts:
    - path: "src/components/IslandBoundary.tsx"
      provides: "reusable class error boundary (getDerivedStateFromError + componentDidCatch)"
      contains: "getDerivedStateFromError"
    - path: "src/app/error.tsx"
      provides: "route-level client error boundary with reset()"
      contains: "reset"
    - path: "src/app/global-error.tsx"
      provides: "root boundary rendering its own <html><body>"
      contains: "<html"
  key_links:
    - from: "src/app/ideas/page.tsx + src/app/fund-me/page.tsx + src/components/Hero3D.tsx"
      to: "src/components/IslandBoundary.tsx"
      via: "<IslandBoundary fallback={...}>...</IslandBoundary> wrapping each canvas"
      pattern: "IslandBoundary"
    - from: "the 3 canvas rAF closures"
      to: "src/hooks/useAnimationGate.ts"
      via: "gateRef mirror of shouldAnimate read inside requestAnimationFrame"
      pattern: "useAnimationGate"
---

<objective>
Stand up the error-boundary trio (FOUND-05) and wire the three existing Canvas-2D islands into `useAnimationGate` (FOUND-02 consumption + PERF-04 off-screen/tab-blur/reduced-motion pause). After this, a thrown render error in any island degrades to a static poster instead of white-screening the page, and every rAF loop stops when off-screen, on tab blur, and under reduced motion.

Begins only after Phase 2 completes AND after Plan 03-02 (needs `useAnimationGate`). Wave 3 — runs after 03-01/03-02. Owns the canvas-wiring edits to `ideas/page.tsx`, `fund-me/page.tsx`, `Hero3D.tsx` (Plan 03-01 deliberately did NOT touch these to avoid write collision; apply any text-relabels it handed off in its SUMMARY here, surgically).

Purpose: PITFALLS #1 — no error.tsx/global-error.tsx + per-island boundary is a hard prerequisite for shipping 3D in Phase 4/5. The IslandBoundary fallback doubles as the reduced-motion static poster (research Pitfall #4).
Output: error.tsx, global-error.tsx, IslandBoundary.tsx; 3 canvases gated; 3 islands boundary-wrapped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-shared-foundation/03-RESEARCH.md
@.planning/phases/03-shared-foundation/03-02-SUMMARY.md
@.planning/phases/03-shared-foundation/03-01-SUMMARY.md

<interfaces>
<!-- The primitive this plan consumes (built in 03-02): -->
import { useAnimationGate } from '@/hooks/useAnimationGate';
const { shouldAnimate } = useAnimationGate(ref);   // ref = the canvas/container element

<!-- VERIFIED repo anchors for the 3 rAF closures (single rAF each, NO IO/visibility gate today): -->
// Hero3DScene.tsx: 'use client'; effect deps [mouse]; `let animId`; `const draw = () => { ...; animId = requestAnimationFrame(draw); }` line 290; `draw();` line 292; cleanup cancelAnimationFrame(animId) line 295; canvas ref = canvasRef. Wrapper Hero3D.tsx renders <Hero3DScene mouse={mouse}/> inside a `.absolute inset-0 z-0` div (line 34-36).
// IdeaNetworkCanvas.tsx: effect deps [init]; `animId = requestAnimationFrame(draw)` at 307 AND kickoff at 310; cleanup line 313. Rendered at ideas/page.tsx:161 <IdeaNetworkCanvas className="w-full h-full"/>.
// InteractiveGlobe.tsx: effect deps [dimensions, getRadius]; `animFrameRef.current = requestAnimationFrame(render)` at 446 AND kickoff 449; cleanup 452; has containerRef. Rendered at fund-me/page.tsx:181 <InteractiveGlobe className="w-full h-[350px] md:h-[400px]"/>.

<!-- IslandBoundary contract (research §Code Examples): -->
// 'use client'; class extends Component<{children:ReactNode; fallback:ReactNode},{hasError:boolean}>
// static getDerivedStateFromError(){return{hasError:true}}; componentDidCatch(e){console.error(...)}; render → hasError? fallback : children
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the error-boundary trio</name>
  <files>src/app/error.tsx, src/app/global-error.tsx, src/components/IslandBoundary.tsx</files>
  <action>
    Create all three per research §Pattern 2 / §Code Examples:
    1. `src/components/IslandBoundary.tsx` — `'use client'`; default-export React **class** `IslandBoundary extends Component<Props,State>` with `Props { children: ReactNode; fallback: ReactNode }`, `State { hasError: boolean }`, `static getDerivedStateFromError(): State { return { hasError: true }; }`, `componentDidCatch(error: unknown) { console.error('[IslandBoundary] island failed, showing fallback:', error); }`, `render()` returns `this.state.hasError ? this.props.fallback : this.props.children`. This is the only class component in the repo — expected and correct (no hook equivalent).
    2. `src/app/error.tsx` — `'use client'`; default `function Error({ error, reset }: { error: Error & { digest?: string }; reset: () => void })` rendering a themed (var(--bg-primary)/var(--text-primary)/var(--btn-primary-*)) centered "Something went wrong" + a `reset()` button.
    3. `src/app/global-error.tsx` — `'use client'`; renders its OWN `<html lang="en-AU"><body>...</body></html>` shell with LITERAL hex (#0a0a0f/#fff — it replaces the root layout so theme CSS vars aren't reliably scoped; research Pitfall #3) + reset button.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const ib=fs.readFileSync('src/components/IslandBoundary.tsx','utf8'); const e=fs.readFileSync('src/app/error.tsx','utf8'); const ge=fs.readFileSync('src/app/global-error.tsx','utf8'); if(!/getDerivedStateFromError/.test(ib)) throw new Error('IslandBoundary not a class boundary'); if(!/reset/.test(e)) throw new Error('error.tsx missing reset'); if(!/<html/.test(ge)) throw new Error('global-error missing own html/body'); console.log('boundary trio ok')"</automated>
  </verify>
  <done>All three files exist with the correct contracts; `npx tsc --noEmit` clean for them.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the 3 canvases into useAnimationGate (off-screen + tab-blur + reduced-motion pause)</name>
  <files>src/components/Hero3DScene.tsx, src/components/IdeaNetworkCanvas.tsx, src/components/InteractiveGlobe.tsx</files>
  <action>
    For EACH of the three canvas components, consume `useAnimationGate` on the element it already owns and gate its rAF loop. RE-READ each file at execution time (line numbers below are from a verified snapshot but may have shifted).
    Pattern (research §Pattern 1 consumption + Pitfall #2 — stale-closure fix):
    1. `import { useAnimationGate } from '@/hooks/useAnimationGate';`
    2. Call `const { shouldAnimate } = useAnimationGate(REF);` where REF is the existing element ref: Hero3DScene → `canvasRef`; IdeaNetworkCanvas → its canvas ref; InteractiveGlobe → `containerRef`.
    3. Add a `const gateRef = useRef(false)` and an effect `useEffect(() => { gateRef.current = shouldAnimate; }, [shouldAnimate])` so the long-lived rAF closure reads the LATEST value (closures capture by value — Pitfall #2).
    4. In each `draw`/`render` closure, replace the unconditional re-schedule with: render one frame, then `if (gateRef.current) { animId = requestAnimationFrame(draw); }`. Do the same at the kickoff site so the loop also re-arms when the gate flips back true (add a small effect that calls the draw/render kickoff when `shouldAnimate` becomes true and the loop is idle, OR keep a single always-scheduled tick that early-returns its body when `!gateRef.current` after drawing one settle frame — pick the minimal change per file; the REQUIRED observable behavior is: when shouldAnimate is false the canvas shows a correct STATIC frame and schedules no further frames).
    5. CRITICAL (PERF-04): when the gate is false, ensure ONE final static frame is drawn so reduced-motion / off-screen shows a frozen-but-correct image, not a blank canvas. Draw-then-check, never check-then-skip-draw on the first false transition.
    Keep each component's existing rAF/cleanup structure otherwise intact; do not change rendering math, DPR handling, or props.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); for(const f of ['Hero3DScene','IdeaNetworkCanvas','InteractiveGlobe']){const s=fs.readFileSync('src/components/'+f+'.tsx','utf8'); if(!/useAnimationGate/.test(s)) throw new Error(f+' not gated'); if(!/gateRef/.test(s)) throw new Error(f+' missing gateRef stale-closure mirror');} console.log('3 canvases gated')"</automated>
  </verify>
  <done>All 3 canvases import + use useAnimationGate, mirror shouldAnimate into gateRef, stop scheduling frames when false after a final static frame; rendering math unchanged; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Wrap the 3 islands in IslandBoundary with static-poster fallbacks</name>
  <files>src/components/Hero3D.tsx, src/app/ideas/page.tsx, src/app/fund-me/page.tsx</files>
  <action>
    Wrap each dynamically-imported canvas island in `<IslandBoundary fallback={<poster/>}>` so a render throw degrades to a poster instead of bubbling to error.tsx and blanking the route (research Pitfall #4 — fallback is a static poster, NOT an error message, so a failed canvas looks intentional). RE-READ each file first (Phase 2 may have touched fund-me/ideas).
    - `Hero3D.tsx` line ~34-36: wrap `<Hero3DScene mouse={mouse} />` (inside the `.absolute inset-0 z-0` div) with `<IslandBoundary fallback={<div className="w-full h-full" style={{ background: 'var(--bg-card)' }} />}>...</IslandBoundary>`. Import `IslandBoundary from '@/components/IslandBoundary'`.
    - `ideas/page.tsx` line ~161: wrap `<IdeaNetworkCanvas .../>` similarly (full-bleed bg poster matching the page background).
    - `fund-me/page.tsx` line ~181: wrap `<InteractiveGlobe .../>` similarly (poster sized to the same `h-[350px] md:h-[400px]` box).
    Fallback posters use theme tokens (var(--bg-card)/var(--bg-secondary)) so they look intentional in both themes and double as the reduced-motion/no-WebGL static view. Do not alter the dynamic() imports, ssr:false, or any other page content.
    ALSO: apply any text-tier relabels Plan 03-01's SUMMARY explicitly handed off to this plan for these three files (surgical, audit-driven only).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); for(const f of ['src/components/Hero3D.tsx','src/app/ideas/page.tsx','src/app/fund-me/page.tsx']){const s=fs.readFileSync(f,'utf8'); if(!/IslandBoundary/.test(s)) throw new Error(f+' island not wrapped');} console.log('3 islands wrapped')"</automated>
  </verify>
  <done>All 3 canvas islands wrapped in IslandBoundary with themed static-poster fallbacks; dynamic imports untouched; handed-off relabels applied; tsc clean.</done>
</task>

</tasks>

<verification>
- error.tsx + global-error.tsx + IslandBoundary.tsx exist with correct Next.js/React contracts.
- All 3 canvases consume useAnimationGate + gateRef mirror; rAF stops off-screen/tab-blur/reduced-motion after a final static frame.
- All 3 islands wrapped in IslandBoundary with themed posters.
- `npx tsc --noEmit` clean across all touched files.
</verification>

<success_criteria>
- A deliberately thrown island error shows the poster fallback, page chrome survives — no white screen (FOUND-05; verified in Plan 03-04).
- Off-screen / tab-blur / reduced-motion each stop the rAF loop with a correct static frame (FOUND-02 consumption + PERF-04).
</success_criteria>

<output>
After completion, create `.planning/phases/03-shared-foundation/03-03-SUMMARY.md`
</output>
