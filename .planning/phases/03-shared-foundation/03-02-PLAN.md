---
phase: 03-shared-foundation
plan: 02
type: execute
wave: 2
depends_on: ["03-00"]
files_modified:
  - src/hooks/useAnimationGate.ts
  - src/hooks/useThemeColors.ts
  - src/lib/motion.ts
autonomous: true
requirements: [FOUND-02, PERF-04]
must_haves:
  truths:
    - "A single useAnimationGate hook composes reduced-motion + in-view + tab-visibility into one boolean"
    - "useReducedMotion is sourced from motion/react (not a hand-rolled matchMedia)"
    - "A useThemeColors hook reads CSS vars once per data-theme change (MutationObserver, not per-frame)"
    - "A motion preset module exports shared variants/transitions using the existing signature easing"
  artifacts:
    - path: "src/hooks/useAnimationGate.ts"
      provides: "shouldAnimate = !reduced && inView && tabVisible, via IntersectionObserver + visibilitychange"
      contains: "useReducedMotion"
    - path: "src/hooks/useThemeColors.ts"
      provides: "CSS-var → string color bridge cached on data-theme mutation"
      contains: "MutationObserver"
    - path: "src/lib/motion.ts"
      provides: "fadeUp/stagger variants + base/quick transitions with EASE_SIGNATURE"
      contains: "0.16, 1, 0.3, 1"
  key_links:
    - from: "src/hooks/useAnimationGate.ts"
      to: "motion/react useReducedMotion"
      via: "import { useReducedMotion } from 'motion/react'"
      pattern: "from 'motion/react'"
---

<objective>
Build the three shared primitives as standalone, type-clean modules (FOUND-02, PERF-04): `useAnimationGate` (the single off-screen/tab-blur/reduced-motion gate every animated component will consume), `useThemeColors` (the CSS-var→canvas/WebGL color bridge for Phase 4/5), and `src/lib/motion.ts` (shared motion v12 presets). This plan creates the primitives ONLY — wiring them into the three existing canvases is Plan 03-03.

Begins only after Phase 2 completes (phase depends_on Phase 2) and after Plan 03-00 (needs motion installed to import `motion/react`). Runs in parallel with Plan 03-01 — no file overlap (hooks/lib vs globals.css/layout).

Purpose: Build the gate ONCE so every animated component (3 existing canvases + every Phase-4/5 scene) consumes one boolean instead of N hand-rolled matchMedia/IO loops (research Anti-Pattern 4). The theme bridge ships now for Phase 4/5 consumers.
Output: `src/hooks/useAnimationGate.ts`, `src/hooks/useThemeColors.ts`, `src/lib/motion.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-shared-foundation/03-RESEARCH.md

<interfaces>
<!-- CONTRACTS Plan 03-03 and Phase 4/5 implement against. Build these signatures exactly. -->

// src/hooks/useAnimationGate.ts  ('use client')
import { useReducedMotion } from 'motion/react';
interface AnimationGateOptions { rootMargin?: string; threshold?: number; }
export function useAnimationGate<T extends Element>(
  ref: React.RefObject<T | null>,
  opts?: AnimationGateOptions               // defaults: rootMargin '200px', threshold 0
): { shouldAnimate: boolean; prefersReduced: boolean; inView: boolean; tabVisible: boolean };
// shouldAnimate = !prefersReduced && inView && tabVisible

// src/hooks/useThemeColors.ts  ('use client')
export function useThemeColors(varNames: string[]): Record<string, string>;
// reads getComputedStyle(documentElement) once on mount + on data-theme MutationObserver change; never per-frame

// src/lib/motion.ts  (no 'use client' — plain data export)
export const EASE_SIGNATURE = [0.16, 1, 0.3, 1] as const;   // matches existing cubic-bezier(0.16,1,0.3,1)
export const transitions: { base: Transition; quick: Transition };  // base 0.7s, quick 0.4s
export const fadeUp: Variants;                               // hidden {opacity:0,y:40} → visible {opacity:1,y:0}
export const stagger: (gap?: number) => Variants;
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create useAnimationGate hook</name>
  <files>src/hooks/useAnimationGate.ts</files>
  <action>
    Create `src/hooks/useAnimationGate.ts` exactly per the interfaces contract (research §Pattern 1 canonical shape). First line `'use client';`.
    - Import `useReducedMotion` from `motion/react` (NOT a hand-rolled matchMedia — research Don't-Hand-Roll). This is the one source of reduced-motion truth.
    - `inView` state via IntersectionObserver on `ref.current` (default `rootMargin: '200px'`, `threshold: 0`); observe in an effect keyed on `[ref, rootMargin, threshold]`; `io.disconnect()` on cleanup. Guard `if (!el) return`.
    - `tabVisible` state via `document.visibilitychange` listener (`document.visibilityState === 'visible'`); seed once on mount; remove listener on cleanup.
    - Return `{ shouldAnimate: !prefersReduced && inView && tabVisible, prefersReduced, inView, tabVisible }`.
    SSR-safe: all DOM access inside useEffect; motion's useReducedMotion is SSR-safe by design. Match the repo's 2-space/semicolons/double-or-match style; this file is new so use the research's double-quote sample style.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | (grep -i "useAnimationGate" && exit 1 || exit 0)</automated>
  </verify>
  <done>File exists, exports `useAnimationGate` with the contract signature, imports `useReducedMotion` from `motion/react`, and type-checks clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create useThemeColors theme-bridge hook</name>
  <files>src/hooks/useThemeColors.ts</files>
  <action>
    Create `src/hooks/useThemeColors.ts` per the interfaces contract (research §Code Examples + Anti-Pattern 3/per-frame warning). First line `'use client';`.
    - `read()` (useCallback over `varNames`): `getComputedStyle(document.documentElement)` then `Object.fromEntries(varNames.map(v => [v, cs.getPropertyValue(v).trim()]))`.
    - State `colors: Record<string,string>` default `{}`.
    - Effect: set initial `read()` (client-only), then a `MutationObserver` on `document.documentElement` filtered to `attributes`/`attributeFilter: ['data-theme']` that calls `setColors(read())`; `ob.disconnect()` on cleanup.
    - Return `colors`. NEVER read per-frame (cache on data-theme mutation only). Returns raw token strings (work directly as canvas `fillStyle`; Phase 4 parses to THREE.Color at the consumer).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | (grep -i "useThemeColors" && exit 1 || exit 0)</automated>
  </verify>
  <done>File exists, exports `useThemeColors`, uses MutationObserver on data-theme (not per-frame), type-checks clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create motion preset module</name>
  <files>src/lib/motion.ts</files>
  <action>
    Create `src/lib/motion.ts` per the interfaces contract (research §Code Examples). NO 'use client' (plain data export, no hooks/state).
    - `import type { Variants, Transition } from 'motion/react';`
    - `export const EASE_SIGNATURE = [0.16, 1, 0.3, 1] as const;` (preserves the existing signature easing seen in ScrollReveal/ThemeToggle so motion-driven components feel identical).
    - `export const transitions = { base: { duration: 0.7, ease: EASE_SIGNATURE } satisfies Transition, quick: { duration: 0.4, ease: EASE_SIGNATURE } satisfies Transition } as const;`
    - `export const fadeUp: Variants = { hidden: { opacity: 0, y: 40 }, visible: { opacity: 1, y: 0, transition: transitions.base } };`
    - `export const stagger = (gap = 0.1): Variants => ({ visible: { transition: { staggerChildren: gap } } });`
    - Add a comment: consumers honor `useReducedMotion()` — when reduced, swap y:40→y:0 (no transform). This module is consumed by the ScrollReveal rewrite in Phase 5; ships now as the single easing/duration source for the 17 components.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | (grep -iE "lib/motion|src.motion" && exit 1 || exit 0)</automated>
  </verify>
  <done>File exists, exports EASE_SIGNATURE/transitions/fadeUp/stagger, contains the `0.16, 1, 0.3, 1` easing, type-checks clean.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` reports no errors in src/hooks/* or src/lib/motion.ts.
- `useAnimationGate` imports `useReducedMotion` from `motion/react`; returns the four-field object.
- `useThemeColors` uses a data-theme-filtered MutationObserver (no per-frame reads).
- `src/lib/motion.ts` exports presets with the signature easing.
</verification>

<success_criteria>
- Single shared gate primitive exists and is the only reduced-motion/IO/visibility composition (FOUND-02 foundation; wiring in 03-03).
- Theme bridge hook ready for Phase 4/5 (FOUND-02 supporting; PERF-04 reduced-motion branch present).
- Motion preset module ready for Phase 5 ScrollReveal rewrite.
</success_criteria>

<output>
After completion, create `.planning/phases/03-shared-foundation/03-02-SUMMARY.md`
</output>
