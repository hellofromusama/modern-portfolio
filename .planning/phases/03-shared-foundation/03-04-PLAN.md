---
phase: 03-shared-foundation
plan: 04
type: execute
wave: 4
depends_on: ["03-01", "03-02", "03-03"]
files_modified: []
autonomous: false
requirements: [SHIP-01, PERF-04, PERF-03, FOUND-05]
must_haves:
  truths:
    - "npx tsc --noEmit passes on all Phase-3-touched files"
    - "npm run lint passes on all Phase-3-touched files"
    - "npm run build succeeds and curl/build smoke confirms the foundation in BOTH themes"
    - "A deliberate-throw test proves IslandBoundary catches without white-screen, then is removed before ship"
    - "Reduced-motion emulation shows a static, readable experience; no throw test remains in shipped code"
  artifacts: []
  key_links:
    - from: "all Phase-3 artifacts"
      to: "SHIP-01 gate"
      via: "tsc + lint + build + both-theme smoke evidence"
      pattern: "tsc --noEmit"
---

<objective>
Final Phase-3 gate (SHIP-01): prove the foundation is type/lint/build-clean and that its observable behaviors hold in BOTH themes, then verify FOUND-05 (deliberate-throw boundary catch) and PERF-04 (reduced-motion static experience) via the dev-only, must-NOT-ship mechanisms — and confirm no throw/test scaffold ships.

Begins only after Phase 2 completes and after Plans 03-01, 03-02, 03-03. No source edits except a temporary dev-only throw used for the FOUND-05 check, which MUST be removed before the gate closes.

Purpose: `next build` alone passes with errors (`ignoreBuildErrors`/`ignoreDuringBuilds` true) — it proves nothing. Run tsc + lint explicitly (STATE.md blocker). This is the phase's definition of done.
Output: SHIP-01 evidence + checkpoint approval (auto-approved on evidence).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-shared-foundation/03-RESEARCH.md
@.planning/phases/03-shared-foundation/03-VALIDATION.md
@.planning/phases/03-shared-foundation/03-01-SUMMARY.md
@.planning/phases/03-shared-foundation/03-02-SUMMARY.md
@.planning/phases/03-shared-foundation/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SHIP-01 gate — tsc + lint + build + contrast script</name>
  <files></files>
  <action>
    Run the full phase gate from the project root and capture output as evidence:
    1. `node scripts/contrast-check.mjs` → must exit 0 (both palettes AA — FOUND-03).
    2. `npx tsc --noEmit` → must be clean for all Phase-3-touched files (src/hooks/*, src/lib/motion.ts, src/components/IslandBoundary.tsx, error.tsx, global-error.tsx, the 3 canvases, Hero3D.tsx, globals.css consumers, layout.tsx). Pre-existing unrelated errors in `api/expertise` etc. (documented in STATE.md Phase-01 note) may remain — they must NOT be in any file this phase touched.
    3. `npm run lint` → clean on Phase-3 files (no new warnings/errors introduced).
    4. `npm run build` → succeeds; confirm no build-time crash and that the `ideas`/`fund-me`/home routes compile.
    Record exact commands + pass/fail in the SUMMARY. If any Phase-3-touched file errors, STOP and report (do not paper over with the ignore flags).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run build && node scripts/contrast-check.mjs</automated>
  </verify>
  <done>tsc + lint + build + contrast-check all green; no new errors in any Phase-3-touched file.</done>
</task>

<task type="auto">
  <name>Task 2: FOUND-05 deliberate-throw boundary test + PERF-04 reduced-motion verification (dev-only, must-not-ship)</name>
  <files></files>
  <action>
    Per research §Validation "Dev-only reduced-motion + error-boundary verification (must NOT ship)":
    FOUND-05 boundary catch:
    - Temporarily make ONE wrapped canvas throw during render (e.g. add `if (process.env.NODE_ENV !== 'production' && new URLSearchParams(typeof location!=='undefined'?location.search:'').get('boom')) throw new Error('TEST_BOUNDARY')` inside a wrapped island, gated to dev + a query flag).
    - Run dev, load the page with `?boom=1`, confirm: the IslandBoundary poster renders, the rest of the page chrome + below-fold content survive (no white screen, error.tsx NOT triggered for the whole route).
    - REMOVE the throw entirely. Then assert nothing shipped: grep that no `TEST_BOUNDARY` / `throw new Error('TEST` remains anywhere in src/.
    PERF-04 reduced-motion + off-screen/tab-blur:
    - DevTools → Rendering → "Emulate CSS prefers-reduced-motion: reduce": confirm the 3 canvases render a static frame (no animation) and the page is fully readable.
    - DevTools Performance / visual check: scroll a canvas off-screen → its rAF stops; switch browser tab → stops; both resume on return.
    PERF-03 keyboard: Tab from page top → skip link appears and moves focus to #main-content; :focus-visible ring visible in BOTH themes; no keyboard trap.
    Capture results in the SUMMARY.
  </action>
  <verify>
    <automated>node -e "const cp=require('child_process'); const r=cp.execSync('rg -n \"TEST_BOUNDARY|throw new Error\\\\(.TEST\" src/ || true',{encoding:'utf8'}); if(r.trim()) throw new Error('test-throw scaffold still present: '+r); console.log('no throw scaffold in src/')"</automated>
  </verify>
  <done>Boundary catch verified then throw removed (grep-clean); reduced-motion static experience + off-screen/tab-blur pause + keyboard skip-link/focus confirmed in both themes; no test scaffold ships.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Owner evidence checkpoint (auto-approve on evidence)</name>
  <action>Present the SHIP-01 evidence and dev-only verification results below for owner sign-off. Auto-approve when the evidence in the 03-04-SUMMARY confirms all checks green.</action>
  <what-built>
    Phase 3 Shared Foundation: AA-corrected tokens + documented type/spacing scales, skip-link + focus-visible a11y, shared useAnimationGate + useThemeColors hooks + motion presets, error-boundary trio, and the 3 existing canvases gated (off-screen/tab-blur/reduced-motion pause) and IslandBoundary-wrapped. SHIP-01 gate (tsc + lint + build + contrast-check) is green and dev-only boundary/reduced-motion/keyboard checks pass.
  </what-built>
  <how-to-verify>
    Evidence-based (auto-approve on evidence). Confirm from the 03-04-SUMMARY:
    1. `npx tsc --noEmit && npm run lint && npm run build && node scripts/contrast-check.mjs` — all green, no Phase-3-touched file errors.
    2. Both-theme smoke: toggle theme on home, /ideas, /fund-me — text readable, accents AA, canvases render; no white screens.
    3. `?boom=1` boundary test showed a poster (page survived), then the throw was removed (grep-clean for TEST_BOUNDARY).
    4. Reduced-motion emulation → static readable canvases; off-screen/tab-blur pause confirmed; Tab → skip link + focus ring in both themes.
  </how-to-verify>
  <verify><automated>N/A — checkpoint (evidence captured in Task 1/2 automated verifies)</automated></verify>
  <done>Owner approval recorded (auto-approved on evidence) or issues described for revision.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- tsc + lint + build + contrast-check all green; no new errors in Phase-3 files.
- Deliberate-throw boundary catch verified then removed (grep-clean).
- Reduced-motion static experience, off-screen/tab-blur pause, keyboard skip-link/focus all confirmed in both themes.
</verification>

<success_criteria>
- SHIP-01 gate passes for Phase 3 (tsc + lint + build + both-theme smoke).
- FOUND-05 boundary catch and PERF-04 reduced-motion behavior proven via dev-only mechanisms that do NOT ship.
</success_criteria>

<output>
After completion, create `.planning/phases/03-shared-foundation/03-04-SUMMARY.md`
</output>
