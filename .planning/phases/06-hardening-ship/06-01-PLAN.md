---
phase: 06-hardening-ship
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/auto-llm-training/route.ts
  - src/app/api/ai-training/route.ts
  - src/app/api/create-checkout/route.ts
  - src/app/api/budget-estimate/route.ts
autonomous: true
requirements: [SHIP-02]
must_haves:
  truths:
    - "npx tsc --noEmit reports ZERO errors in all four api/* files owned by this plan (down from 11)"
    - "npm run lint reports ZERO problems in all four api/* files owned by this plan"
    - "The Stripe create-checkout route compiles against the installed SDK's pinned apiVersion"
  artifacts:
    - path: "src/app/api/auto-llm-training/route.ts"
      provides: "Typed results object — no TS18047/TS2322; no console key leak"
      contains: "interface"
    - path: "src/app/api/ai-training/route.ts"
      provides: "Typed map param + valid fetch body (TS7006/TS2769 cleared)"
    - path: "src/app/api/create-checkout/route.ts"
      provides: "apiVersion '2025-08-27.basil' matching stripe@18.5.0"
      contains: "2025-08-27.basil"
    - path: "src/app/api/budget-estimate/route.ts"
      provides: "userMessage reference fixed (TS2304 cleared)"
  key_links:
    - from: "src/app/api/create-checkout/route.ts"
      to: "stripe SDK type literal"
      via: "apiVersion string"
      pattern: "apiVersion:\\s*'2025-08-27.basil'"
---

<objective>
Clear the 11 `tsc --noEmit` errors and all lint problems in the four AI/payment API routes — the LOW-to-MEDIUM-difficulty half of the strict-build burndown (SHIP-02). This unblocks the `next.config.ts` strict-flag flip that 06-05 performs.

Purpose: `next build` is currently green ONLY because `typescript.ignoreBuildErrors` + `eslint.ignoreDuringBuilds` are `true`. These flags are flipped to `false` in 06-05. Every owned file must be genuinely clean first, or the strict build fails.
Output: Four api/* route files that pass `npx tsc --noEmit` AND `npm run lint` with zero findings on those paths — functionality byte-identical (no behavior change except the required, intentional Stripe apiVersion bump).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-hardening-ship/06-RESEARCH.md

# The exact error inventory (measured at phase start 2026-06-12, node_modules present):

# `npx tsc --noEmit` errors in this plan's four files (11 total):
#   src/app/api/auto-llm-training/route.ts — 7 errors
#     TS18047 (results.openai etc. possibly null) + TS2322 (object assigned to null-typed field).
#     ROOT CAUSE: the `results` object literal infers each provider field as `null`.
#     FIX: declare an explicit interface (e.g. `interface ProviderResult { success: boolean; ... }`
#          and `interface TrainingResults { openai: ProviderResult | null; ... }`), annotate the
#          `results` const with it, so `.success` access + later assignments type-check.
#   src/app/api/ai-training/route.ts — 2 errors
#     TS7006 (implicit any `example` map param) → type the map param.
#     TS2769 (fetch body is `void`) → REAL BUG: a function returning void is passed as `body`.
#          Pass the actual serialized string (JSON.stringify(...)) not the void return.
#   src/app/api/create-checkout/route.ts — 1 error
#     TS2322 — apiVersion '2024-06-20' not assignable to '2025-08-27.basil'.
#     FIX: bump the literal to '2025-08-27.basil' (matches installed stripe@18.5.0).
#     PITFALL (research): this is a REAL API-version pin, not just a type annoyance —
#          DO NOT charge a live card; type-check only. A test-mode smoke is a 06-05 concern.
#   src/app/api/budget-estimate/route.ts — 1 error
#     TS2304 — `userMessage` not defined (line ~37). Genuine undefined-variable bug; fix the reference.

# `npm run lint` problems in this plan's four files (~33 total — fix these in the SAME pass):
#   api/ai-training/route.ts        — 15 (no-explicit-any, no-unused-vars; overlaps the TS7006 fix)
#   api/auto-llm-training/route.ts  — 12 (no-explicit-any, no-unused-vars; overlaps the TS18047 fix)
#   api/budget-estimate/route.ts    — 4
#   api/create-checkout/route.ts    — 1
#   Also remove the console key-prefix leak at auto-llm-training/route.ts:37
#     (`console.log('🔑 API Key format:', ...substring(0, 7) + '...')`) — stripped in prod by
#     compiler.removeConsole but remove it anyway (FIX-01-adjacent hygiene).

# Project conventions (CLAUDE.md):
# - quote style: API routes use SINGLE quotes — match the file
# - 2-space indent, semicolons, trailing commas in multiline
# - for intentionally-unused catch bindings use `catch {}` (ES2019) or prefix `_error`
# - NO new dependencies; NO behavior change beyond the Stripe apiVersion bump
# - the no-explicit-any lint and the TS7006/TS18047 type errors are the SAME "this data has no
#   type" problem — fix tsc + lint together per file, do not eslint-disable to hit zero.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Type the two training routes (auto-llm-training + ai-training) and remove the console key leak</name>
  <files>src/app/api/auto-llm-training/route.ts, src/app/api/ai-training/route.ts</files>
  <action>
**auto-llm-training/route.ts (7 tsc errors + 12 lint):**
- Declare explicit interfaces for the provider-results shape so the `results` object no longer infers each field as `null`. Example: `interface ProviderResult { success: boolean; [k: string]: unknown }` and a `TrainingResults` type with each provider field typed `ProviderResult | null`. Annotate the `results` const. This clears the TS18047 (possibly-null access) + TS2322 (assigning object to null-typed field) errors.
- Replace every `no-explicit-any` with the real type (reuse the interfaces above; the any's are the same untyped-data problem).
- Remove unused vars/imports flagged by `no-unused-vars`; for unused catch bindings use `catch {}` or prefix `_error`.
- REMOVE the `console.log('🔑 API Key format:', ...substring(0, 7) + '...')` line at ~line 37 entirely (key-prefix leak).
- Do NOT change runtime behavior (same providers, same flow). Single quotes, 2-space indent.

**ai-training/route.ts (2 tsc errors + 15 lint):**
- Type the implicit-any map param (the `example` callback param) — TS7006.
- Fix the TS2769 `void` fetch body: a function returning `void` is being passed as `body`. Pass the actual serialized payload (`JSON.stringify(payload)`), not the void-returning call. This is a real bug — verify the intended body is sent.
- Clear the no-explicit-any / no-unused-vars problems (same typing pass).
- Single quotes, match file style. No behavior change.
  </action>
  <verify>
  <automated>cd "C:\Users\Usama.Javed\Desktop\modern-portfolio" && npx tsc --noEmit 2>&1 | grep -E "auto-llm-training|ai-training/route" | wc -l</automated>
  Expect 0. Also run `npm run lint 2>&1 | grep -E "auto-llm-training|ai-training[\\\\/]route"` → expect no output.
  </verify>
  <done>npx tsc --noEmit reports zero errors in auto-llm-training/route.ts and ai-training/route.ts; npm run lint reports zero problems in both; the console key-prefix leak is gone; runtime behavior unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Fix create-checkout (Stripe apiVersion bump) + budget-estimate (undefined var) + clear their lint</name>
  <files>src/app/api/create-checkout/route.ts, src/app/api/budget-estimate/route.ts</files>
  <action>
**create-checkout/route.ts (1 tsc error + 1 lint):**
- Change the Stripe `apiVersion` literal from `'2024-06-20'` to `'2025-08-27.basil'` (matches installed stripe@18.5.0; required for the TS2322 fix). This is the ONLY behavior-affecting change in this plan — it is intentional and required.
- DO NOT execute a live checkout or charge a card here; type-check only. (06-05 owns any test-mode smoke before deploy.)
- Clear the 1 lint problem on this file (likely no-unused-vars / no-explicit-any).
- Preserve the lazy `getStripe()` null-guard → 503 behavior verbatim. Single quotes.

**budget-estimate/route.ts (1 tsc error + 4 lint):**
- Fix the TS2304 `userMessage` undefined reference (~line 37) — trace where the value should come from (likely the request body field) and reference the correct variable. Genuine bug.
- Clear the 4 lint problems (no-explicit-any / no-unused-vars) in the same pass.
- Preserve the OpenAI → x.ai (Grok) → fallback chain behavior. Single quotes.
  </action>
  <verify>
  <automated>cd "C:\Users\Usama.Javed\Desktop\modern-portfolio" && npx tsc --noEmit 2>&1 | grep -E "create-checkout|budget-estimate" | wc -l</automated>
  Expect 0. Also `npm run lint 2>&1 | grep -E "create-checkout|budget-estimate"` → no output. And confirm `grep -n "2025-08-27.basil" src/app/api/create-checkout/route.ts` matches.
  </verify>
  <done>Both files have zero tsc errors and zero lint problems; create-checkout pins apiVersion '2025-08-27.basil'; budget-estimate's userMessage reference resolves; the 503 null-guard and provider fallback chain are preserved.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit 2>&1 | grep -E "api/(auto-llm-training|ai-training|create-checkout|budget-estimate)"` → no matches (11 → 0 for owned files).
- `npm run lint 2>&1 | grep -E "api[\\/](auto-llm-training|ai-training|create-checkout|budget-estimate)"` → no matches.
- The overall tsc count drops from 29 to 18 (only expertise/page.tsx remains — owned by 06-02).
- No new dependency added; `git diff package.json package-lock.json` empty.
</verification>

<success_criteria>
All four api/* route files pass `npx tsc --noEmit` and `npm run lint` with zero findings on their paths; the Stripe apiVersion is bumped to the SDK-pinned literal; the console key-prefix leak is removed; runtime behavior is otherwise byte-identical. Overall tsc error count: 29 → 18.
</success_criteria>

<output>
After completion, create `.planning/phases/06-hardening-ship/06-01-SUMMARY.md`
</output>
