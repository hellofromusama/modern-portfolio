---
phase: 06-hardening-ship
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/VisitorTracker.tsx
  - src/components/VisitorCounter.tsx
autonomous: true
requirements: [FIX-02]
must_haves:
  truths:
    - "Loading the homepage fires ZERO requests to /api/schedule-training or /api/auto-llm-training from a visitor browser"
    - "The VisitorTracker welcome toast/counter behavior is otherwise unchanged"
    - "Corrupt localStorage no longer silently kills the tracker effect (JSON.parse is guarded)"
    - "VisitorCounter renders correctly in BOTH themes (dark-only slate chrome tokenized)"
  artifacts:
    - path: "src/components/VisitorTracker.tsx"
      provides: "Client component with the paid-endpoint fan-out removed and JSON.parse guarded"
    - path: "src/components/VisitorCounter.tsx"
      provides: "Theme-token chrome (no bg-slate-900/text-slate literals)"
  key_links: []
---

<objective>
FIX-02: Stop visitor browsers from firing the paid AI training endpoints on every visit, and tokenize the deferred VisitorCounter dark-only chrome so it renders in both themes.

Purpose: VisitorTracker.tsx currently fires fetch('/api/schedule-training') (line 79, every 3rd visit) and fetch('/api/auto-llm-training') (line 92, first daily visit) directly from each visitor's browser — paid AI fan-out whose cost scales with traffic. The orchestrator decision (research recommendation) is to REMOVE the client fan-out entirely; the API routes stay (feature preserved server-side behind the FIX-01 gate / future cron), but no visitor browser ever triggers a paid call. Separately, VisitorCounter.tsx was deferred from Phase 5 (05-09 deferred-items.md) — it carries a dark-only bg-slate-900/text-slate literal that breaks light theme.
Output: VisitorTracker with both fetch blocks removed + a guarded JSON.parse; VisitorCounter chrome tokenized to CSS custom properties.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-hardening-ship/06-RESEARCH.md
@.planning/phases/05-component-upgrades/deferred-items.md

Verified source facts (measured 2026-06-12):

VisitorTracker.tsx (full file read):
- Line 29: const parsed = JSON.parse(existingData); — UNGUARDED. Corrupt localStorage throws and silently kills the whole trackVisitor effect (CONCERNS.md fragile-area bug).
- Lines 77-86: `if (data.count % 3 === 1) { fetch('/api/schedule-training', {POST...}).catch(()=>{}) }` — REMOVE this whole block.
- Lines 89-100: `if (data.count === 1) { setTimeout(() => { fetch('/api/auto-llm-training', {POST...}).catch(()=>{}) }, 5000) }` — REMOVE this whole block.
- The visible counter/toast (getVisitMessage/getVisitIcon/the returned JSX at 134-168) is UNAFFECTED by removing the fetches — they are fire-and-forget background calls. Keep all toast behavior verbatim.

ORCHESTRATOR DECISION (resolves research Open Q2): REMOVE the client-side fan-out entirely (do NOT ship the gated sessionStorage opt-in variant). Training runs only via the owner's FIX-01 admin call or a future cron. This is the lowest-risk, zero-dependency path.

VisitorCounter.tsx (separate component, line 35):
- `<div className="bg-slate-900/80 backdrop-blur-md text-slate-300 px-3 py-2 rounded-lg border border-slate-700/50 text-xs">` — dark-only literals.
- Tokenize using the project's CSS custom properties (CLAUDE.md theming - NOT Tailwind dark:):
  bg-slate-900/80 -> var(--bg-card) (or var(--bg-elevated)); text-slate-300 -> var(--text-secondary) (or --text-muted); border-slate-700/50 -> var(--border-subtle). Preserve backdrop-blur-md, rounded-lg, padding, text-xs. Apply via inline style or Tailwind arbitrary value referencing the var. Verify it reads correctly under both :root (dark) and [data-theme="light"].

CONSTRAINTS: both are client components ('use client') - safe to keep handlers. No new deps. No behavior change beyond removing the two fetch blocks and theming. Match each file's quote style (VisitorTracker uses single quotes). This plan is file-disjoint from 06-04 (api routes) - they run in the same wave.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the two paid-endpoint fetch blocks from VisitorTracker + guard JSON.parse</name>
  <files>src/components/VisitorTracker.tsx</files>
  <action>
- DELETE the schedule-training fetch block (the `if (data.count % 3 === 1) { ... fetch('/api/schedule-training' ...) }`, ~lines 77-86) entirely.
- DELETE the auto-llm-training fetch block (the `if (data.count === 1) { setTimeout(() => { fetch('/api/auto-llm-training' ...) }, 5000) }`, ~lines 89-100) entirely.
- Wrap the `JSON.parse(existingData)` at ~line 29 in a try/catch: on parse failure, treat it as a first-ever visit (fall through to the else branch / reset to a fresh VisitorData) rather than throwing and killing the effect. Keep the rest of the existing-data branch logic intact.
- Leave ALL visible behavior unchanged: getVisitMessage, getVisitIcon, shouldShowNotification timing (setTimeout show/hide), the returned toast JSX, localStorage.setItem of the counter. Only the two background fetches and the parse-guard change.
- Single quotes, 2-space indent. No new deps.
  </action>
  <verify>
  <automated>cd "C:\Users\Usama.Javed\Desktop\modern-portfolio" && grep -cE "schedule-training|auto-llm-training" src/components/VisitorTracker.tsx</automated>
  Expect 0 (both fetch targets gone). Then npx tsc --noEmit and npm run lint must be clean on this file.
  </verify>
  <done>VisitorTracker.tsx contains zero references to /api/schedule-training or /api/auto-llm-training; JSON.parse is guarded; the toast/counter behavior is byte-identical; file is tsc + lint clean.</done>
</task>

<task type="auto">
  <name>Task 2: Tokenize VisitorCounter chrome for both-theme correctness</name>
  <files>src/components/VisitorCounter.tsx</files>
  <action>
- Replace the dark-only literals at line 35 (bg-slate-900/80, text-slate-300, border-slate-700/50) with the project's CSS custom properties so the counter renders in both themes:
  - background -> var(--bg-card) or var(--bg-elevated)
  - text color -> var(--text-secondary) (or --text-muted if too prominent)
  - border -> var(--border-subtle)
- Preserve backdrop-blur-md, rounded-lg, px-3 py-2, text-xs and all surrounding structure/content.
- Apply via inline style={{ background: 'var(--bg-card)', color: 'var(--text-secondary)', borderColor: 'var(--border-subtle)' }} or Tailwind arbitrary-value classes referencing the vars — match whatever the rest of the file/codebase uses.
- No content/behavior change; theming only. Verify the swept classes are gone.
  </action>
  <verify>
  <automated>cd "C:\Users\Usama.Javed\Desktop\modern-portfolio" && grep -cE "bg-slate-900|text-slate-300|border-slate-700" src/components/VisitorCounter.tsx</automated>
  Expect 0 (dark-only literals replaced with tokens). tsc + lint clean on this file.
  </verify>
  <done>VisitorCounter.tsx has no bg-slate-900/text-slate-300/border-slate-700 literals; chrome uses CSS custom properties readable in both :root and [data-theme="light"]; content unchanged; file tsc + lint clean.</done>
</task>

</tasks>

<verification>
- grep -cE "schedule-training|auto-llm-training" src/components/VisitorTracker.tsx returns 0.
- grep -cE "bg-slate-900|text-slate" src/components/VisitorCounter.tsx returns 0.
- npx tsc --noEmit + npm run lint clean on both files.
- Functional: 06-07 confirms (DevTools/curl) a fresh homepage visit fires no /api/auto-llm-training or /api/schedule-training calls.
</verification>

<success_criteria>
FIX-02 complete: the client-side paid-endpoint fan-out is removed from VisitorTracker (no visitor browser triggers training), JSON.parse is crash-guarded, and VisitorCounter's deferred dark-only chrome is tokenized for both themes — all with zero new deps and no other behavior change.
</success_criteria>

<output>
After completion, create `.planning/phases/06-hardening-ship/06-05-SUMMARY.md`
</output>
