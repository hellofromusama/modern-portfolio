---
phase: 06-hardening-ship
plan: 04
type: execute
wave: 2
depends_on: [06-01, 06-03]
files_modified:
  - src/lib/admin-guard.ts
  - src/app/api/test-openai/route.ts
  - src/app/api/test-free-llm/route.ts
  - src/app/api/ai-training/route.ts
  - src/app/api/auto-llm-training/route.ts
  - src/app/api/schedule-training/route.ts
autonomous: true
requirements: [FIX-01]
user_setup:
  - service: vercel-env
    why: "Admin token to authorize the owner's calls to the gated test/training routes"
    env_vars:
      - name: ADMIN_API_TOKEN
        source: "Owner-chosen strong random secret; set in Vercel project 'modern-portfolio' Settings -> Environment Variables (and .env.local for local dev)"
must_haves:
  truths:
    - "An unauthenticated request to /api/test-openai returns 401/403 and contains NO key bytes"
    - "A request with the correct x-admin-token header reaches the route's existing logic"
    - "When ADMIN_API_TOKEN is unset the routes fail closed (403 disabled), never exposing a token or key"
    - "The test-openai response no longer contains a keyUsed field or any key substring"
    - "Every exported handler in every dual-handler route (GET AND POST) is gated — no handler left ungated"
  artifacts:
    - path: "src/lib/admin-guard.ts"
      provides: "requireAdmin(request) shared-secret gate returning NextResponse or null"
      exports: ["requireAdmin"]
      min_lines: 10
    - path: "src/app/api/test-openai/route.ts"
      provides: "Gated GET with keyUsed removed (returns ok boolean instead)"
      contains: "requireAdmin"
  key_links:
    - from: "src/app/api/test-openai/route.ts"
      to: "src/lib/admin-guard.ts"
      via: "requireAdmin(request) guard at handler top"
      pattern: "requireAdmin\\(request\\)"
    - from: "src/app/api/auto-llm-training/route.ts"
      to: "src/lib/admin-guard.ts"
      via: "requireAdmin(request) guard"
      pattern: "requireAdmin"
---

<objective>
FIX-01: Gate the unauthenticated paid-API/test/training routes behind a shared-secret admin header plus env flag (fail-closed), and remove the keyUsed key-prefix leak from test-openai. The feature is PRESERVED for the owner (callable with the x-admin-token header), never deleted, per the owner's guard-not-delete mandate.

Purpose: test-openai is currently a public GET that calls OpenAI on every hit (paid) and returns the first 10 bytes of the API key. test-free-llm, ai-training, auto-llm-training, schedule-training are unguarded or env-flag-only (no auth). A single ~12-line shared guard closes all of them with zero new dependencies.
Output: A src/lib/admin-guard.ts helper applied at the top of all five handlers; the keyUsed field removed; fail-closed behavior when the token is unset.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-hardening-ship/06-RESEARCH.md

Verified route facts (measured 2026-06-12):
- test-openai/route.ts: GET(request: NextRequest) ONLY (1 handler); line 38 leaks keyUsed = process.env.OPENAI_API_KEY.substring(0,10) + '...' (REMOVE). Also returns raw OpenAI error bodies (lines 41-47) - return a generic message instead.
- test-free-llm/route.ts: POST(request: NextRequest) ONLY (1 handler, no GET).
- ai-training/route.ts: POST(request: NextRequest) + GET() with NO param (2 handlers) - add (request: NextRequest) to GET.
- auto-llm-training/route.ts: POST(request: NextRequest) + GET() with NO param (2 handlers) - add (request: NextRequest) to GET. Gated only by AI_TRAINING_ENABLED env flag, NO auth currently.
- schedule-training/route.ts: POST(request: NextRequest) + GET() with NO param (2 handlers) - add (request: NextRequest) to GET.

HANDLER-COUNT MAP (drives the per-file guard-count verify):
- test-openai/route.ts        -> 1 handler  -> requireAdmin count >= 1
- test-free-llm/route.ts       -> 1 handler  -> requireAdmin count >= 1
- ai-training/route.ts         -> 2 handlers -> requireAdmin count == 2 (one per GET and POST)
- auto-llm-training/route.ts   -> 2 handlers -> requireAdmin count == 2 (one per GET and POST)
- schedule-training/route.ts   -> 2 handlers -> requireAdmin count == 2 (one per GET and POST)

ORCHESTRATOR DECISION (resolves research Open Q3): fail-closed. Do NOT ship ADMIN_API_TOKEN to any client/UI page. The /test-apis + /llm-training-dashboard UIs are owner-local; they call with the header manually (or remain non-functional in prod, which is acceptable - owner-only tools).

SHARED GUARD (research-verified pattern, src/lib/admin-guard.ts - new ~12-line file, single quotes):
  import { NextRequest, NextResponse } from 'next/server';
  export function requireAdmin(request: NextRequest): NextResponse | null {
    const expected = process.env.ADMIN_API_TOKEN;
    if (!expected) return NextResponse.json({ status: 'disabled' }, { status: 403 }); // fail closed
    const provided = request.headers.get('x-admin-token');
    if (provided !== expected) return NextResponse.json({ status: 'unauthorized' }, { status: 401 });
    return null; // authorized -> proceed
  }

Apply as the FIRST statement of EVERY exported handler (the guard must run before any route logic):
  export async function GET(request: NextRequest) {
    const denied = requireAdmin(request);
    if (denied) return denied;
    // ...existing logic...
  }

DEPENDS ON 06-01 + 06-03: those plans type-fix/de-lint these same api/* files. This plan runs in Wave 2 AFTER both, so the files are already tsc+lint clean and this plan only ADDS the guard + removes keyUsed - keeping THIS plan's edits lint-clean too (no re-introduced unused vars).

CONSTRAINTS: zero new deps; single quotes (api routes); preserve each route's existing logic verbatim below the guard; document ADMIN_API_TOKEN in the SUMMARY (no .env.local.example exists - do NOT create a file containing a real secret; just document the required var name + where to set it).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the requireAdmin shared guard</name>
  <files>src/lib/admin-guard.ts</files>
  <action>
Create src/lib/admin-guard.ts exactly as the research-verified pattern in context:
- export function requireAdmin(request: NextRequest): NextResponse | null
- Reads process.env.ADMIN_API_TOKEN. If unset, return NextResponse.json({ status: 'disabled' }, { status: 403 }) (fail closed - route disabled in prod when no token configured).
- If the x-admin-token request header does not equal the expected token, return NextResponse.json({ status: 'unauthorized' }, { status: 401 }).
- Otherwise return null (authorized, proceed).
- Single quotes, 2-space indent, semicolons. No 'use client'. This is the ONLY new file.
  </action>
  <verify>
  <automated>cd "C:\Users\Usama.Javed\Desktop\modern-portfolio" && npx tsc --noEmit 2>&1 | grep -c "admin-guard"</automated>
  Expect 0 (file type-checks). Also confirm grep -c "export function requireAdmin" src/lib/admin-guard.ts returns 1.
  </verify>
  <done>src/lib/admin-guard.ts exists, exports requireAdmin with the fail-closed + 401 + null-proceed contract, type-checks clean.</done>
</task>

<task type="auto">
  <name>Task 2: Gate all five routes + remove the test-openai keyUsed leak</name>
  <files>src/app/api/test-openai/route.ts, src/app/api/test-free-llm/route.ts, src/app/api/ai-training/route.ts, src/app/api/auto-llm-training/route.ts, src/app/api/schedule-training/route.ts</files>
  <action>
For EACH of the five routes, import requireAdmin from @/lib/admin-guard and add the guard as the FIRST statement of EVERY exported handler (GET and POST): "const denied = requireAdmin(request); if (denied) return denied;". It must run before ANY route logic — nothing else may execute ahead of it.
- CRITICAL (dual-handler routes): ai-training, auto-llm-training, and schedule-training EACH have BOTH a GET and a POST handler. BOTH must be gated independently. A common failure is gating POST and leaving GET (or vice versa) open — gate every exported function.
- For handlers currently declared with NO param (ai-training GET(), auto-llm-training GET(), schedule-training GET()), change the signature to GET(request: NextRequest) so the guard can read the header. NextRequest is already imported in these files.
- Keep each route's existing logic verbatim below the guard (same providers, same env-flag checks like AI_TRAINING_ENABLED - the guard is ADDITIVE, in front of those checks).

For test-openai/route.ts specifically, additionally:
- REMOVE the keyUsed field (line 38) entirely. Replace the success payload's key-exposing field with a boolean (e.g. ok: true) - return NO key bytes.
- Replace the raw OpenAI error-body passthrough (lines 41-47, error: error) with a generic message (e.g. message: 'OpenAI API request failed' + statusCode) - do not leak upstream error bodies.

Single quotes, match file style. No unused-var / any re-introduced (these files are lint-clean post 06-01/06-03; keep them clean).
  </action>
  <verify>
  <automated>cd "C:\Users\Usama.Javed\Desktop\modern-portfolio" && echo "single-handler (expect >=1 each):" && grep -c requireAdmin src/app/api/test-openai/route.ts && grep -c requireAdmin src/app/api/test-free-llm/route.ts && echo "dual-handler (expect EXACTLY 2 each):" && grep -c requireAdmin src/app/api/ai-training/route.ts && grep -c requireAdmin src/app/api/auto-llm-training/route.ts && grep -c requireAdmin src/app/api/schedule-training/route.ts && echo "guard-is-first-statement (each export must show requireAdmin within 2 lines):" && grep -A2 -nE "export async function (GET|POST)" src/app/api/test-openai/route.ts src/app/api/test-free-llm/route.ts src/app/api/ai-training/route.ts src/app/api/auto-llm-training/route.ts src/app/api/schedule-training/route.ts | grep -c "const denied = requireAdmin" && echo "keyUsed (expect 0):" && grep -c keyUsed src/app/api/test-openai/route.ts</automated>
  Acceptance: test-openai >= 1, test-free-llm >= 1; ai-training == 2, auto-llm-training == 2, schedule-training == 2 (per-file counts match the handler count — a dual-handler file showing 1 means one handler is ungated → FAIL). The grep -A2 count of "const denied = requireAdmin" must equal the total handler count across all five files (8: 1+1+2+2+2) — proving the guard is the FIRST statement of each exported handler, not buried below other logic. keyUsed count is 0.
  </verify>
  <done>All five routes call requireAdmin as the FIRST statement of every exported handler; the three dual-handler routes (ai-training, auto-llm-training, schedule-training) each show a requireAdmin count of exactly 2 (GET AND POST gated); the three GET() handlers now take request: NextRequest; test-openai returns no key bytes (keyUsed removed, generic error message); files remain tsc + lint clean.</done>
</task>

</tasks>

<verification>
- Per-file requireAdmin counts match handler counts: test-openai >=1, test-free-llm >=1, ai-training ==2, auto-llm-training ==2, schedule-training ==2 (no dual-handler file left with a single guard).
- The guard is the FIRST statement of every exported handler (grep -A2 on each export shows "const denied = requireAdmin"); total == 8 across the five files.
- grep -c keyUsed src/app/api/test-openai/route.ts returns 0.
- npx tsc --noEmit and npm run lint clean on all six owned files (admin-guard + five routes).
- ADMIN_API_TOKEN is documented in the SUMMARY (var name + Vercel/.env.local location); no secret-bearing file created.
- Functional smoke (06-07 runs the curl gate post-build): unauth GET /api/test-openai returns 401/403 with no key bytes; with x-admin-token header it reaches the route logic.
</verification>

<success_criteria>
FIX-01 complete: a fail-closed shared-secret guard gates EVERY exported handler of all five test/training routes (both GET and POST on the three dual-handler routes, verified by per-file guard counts and a first-statement assertion); the test-openai key-prefix leak (keyUsed) and raw-error-body passthrough are removed; the feature is preserved for the owner via the x-admin-token header; zero new dependencies; all owned files tsc + lint clean.
</success_criteria>

<output>
After completion, create `.planning/phases/06-hardening-ship/06-04-SUMMARY.md`
</output>
</output>
