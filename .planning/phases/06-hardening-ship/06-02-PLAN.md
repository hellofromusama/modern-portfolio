---
phase: 06-hardening-ship
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/expertise/page.tsx
autonomous: true
requirements: [SHIP-02]
must_haves:
  truths:
    - "npx tsc --noEmit reports ZERO errors in src/app/expertise/page.tsx (down from 18)"
    - "npm run lint reports ZERO problems in src/app/expertise/page.tsx"
    - "The /expertise page renders byte-identical content + JSON-LD in both themes (no content/SEO change)"
  artifacts:
    - path: "src/app/expertise/page.tsx"
      provides: "Discriminated/typed shape for the technicalExpertise data so .technologies/.sectors narrow"
      contains: "interface"
  key_links:
    - from: "src/app/expertise/page.tsx"
      to: "technicalExpertise data object"
      via: "explicit typed shape replacing the inferred union"
      pattern: "interface|type "
---

<objective>
Clear the 18 `tsc --noEmit` errors and 1 lint problem in `src/app/expertise/page.tsx` — the single largest type-debt file and the MEDIUM-difficulty half of the strict-build burndown (SHIP-02). This is the last tsc blocker before 06-05 flips the strict flags.

Purpose: The errors all stem from one root cause — the `technicalExpertise` data object has no explicit type, so its inferred union has no `.technologies`/`.sectors` members (TS2339) and the map callback params are implicit-any (TS7006). A discriminated union or per-category typed shape fixes all 18 at once.
Output: `src/app/expertise/page.tsx` passes `npx tsc --noEmit` AND `npm run lint` with zero findings — content, metadata, and the expertise JSON-LD (line ~431) byte-identical.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-hardening-ship/06-RESEARCH.md

# Exact error inventory (measured 2026-06-12):
# src/app/expertise/page.tsx — 18 tsc errors + 1 lint
#   TS2339: union member has no `technologies` / `sectors` property
#   TS7006: implicit-any map callback params (sector, sectorIndex, app, appIndex, achievement, achIndex, ...)
#
# ROOT CAUSE (research, HIGH confidence): the `technicalExpertise` object literal (declared ~line 20)
#   is rendered by mapping over its categories, and different categories carry DIFFERENT shapes —
#   some have `.technologies`, some have `.sectors`. With no declared type, TS infers a union where
#   neither property is common, so every `.technologies`/`.sectors` access fails (TS2339) and every
#   map param is implicit-any (TS7006).
#
# FIX APPROACH:
#   1. Declare an explicit type for the data. Either:
#      (a) a discriminated union (e.g. a `kind: 'tech' | 'sectors'` discriminant per category) and
#          narrow with the discriminant in the render map, OR
#      (b) a single interface where both `technologies?` and `sectors?` are optional members, and
#          guard each access (`category.technologies?.map(...)` / `category.sectors?.map(...)`).
#      Approach (b) is lower-risk if the render JSX already conditionally renders one or the other.
#   2. Once the data is typed, the map callback params (sector, app, achievement, the *Index params)
#      narrow automatically — the TS7006 errors disappear. Annotate any that don't.
#   3. Clear the 1 remaining lint problem (likely no-unused-vars / no-explicit-any) in the same pass.
#
# HARD CONSTRAINT — SEO / content preservation (this is a SERVER page exporting metadata + JSON-LD):
#   - DO NOT change any rendered text, the `metadata` export, the `certifications` data (~line 397),
#     or the expertise JSON-LD `<script>` (~line 431). This is a TYPE-ONLY refactor.
#   - `expertise` was confirmed to have NO percentage skill bars (anti-feature) — keep it that way.
#   - The page is server-rendered (no "use client"); do not add event handlers (the /blog Rule-1
#     prerender-crash lesson from 05-09 — server components cannot pass event handlers to DOM props).
#
# Conventions: 2-space indent, semicolons, trailing commas; match the file's quote style.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declare an explicit type for technicalExpertise and narrow .technologies/.sectors accesses</name>
  <files>src/app/expertise/page.tsx</files>
  <action>
- Read the `technicalExpertise` object (declared ~line 20) and the JSX that maps over it to understand which categories carry `.technologies` vs `.sectors`.
- Declare an explicit type. Preferred: a category interface with BOTH `technologies?: string[]` (or the real element shape) and `sectors?: ...` as OPTIONAL members, plus the nested `applications`/`achievements` shapes the inner maps consume. If the categories are genuinely disjoint, use a discriminated union and narrow on the discriminant.
- Annotate the `technicalExpertise` const with the type. This clears the TS2339 union-property errors.
- In the render map, guard the optional accesses (`category.technologies?.map(...)`, `category.sectors?.map(...)`) so they match the existing conditional-render structure. The inner map params (sector, sectorIndex, app, appIndex, achievement, achIndex) then narrow automatically — confirm all TS7006 errors clear; annotate any straggler explicitly.
- This is TYPE-ONLY: do not alter rendered text, the metadata export, certifications data, or the JSON-LD script. Do not add "use client" or any event handlers.
  </action>
  <verify>
  <automated>cd "C:\Users\Usama.Javed\Desktop\modern-portfolio" && npx tsc --noEmit 2>&1 | grep "expertise/page" | wc -l</automated>
  Expect 0. Then `npm run lint 2>&1 | grep "expertise[\\\\/]page"` → no output.
  </verify>
  <done>npx tsc --noEmit reports zero errors in expertise/page.tsx (18 → 0); npm run lint reports zero problems; the technicalExpertise data is explicitly typed; no rendered text, metadata, certifications, or JSON-LD changed.</done>
</task>

<task type="auto">
  <name>Task 2: Verify content + JSON-LD preservation via build-render diff</name>
  <files>src/app/expertise/page.tsx</files>
  <action>
Confirm the type refactor changed ZERO user-visible output:
- Run a git diff against HEAD and confirm only type annotations / optional-chaining guards changed — NO text strings, NO metadata fields, NO JSON-LD object values were touched. If the diff shows any content/SEO string change, revert that hunk and re-do as type-only.
- Build the project and confirm `/expertise` still prerenders (it is a server + metadata route).
- This task adds no new code; it is the content-preservation gate for the type refactor.
  </action>
  <verify>
  <automated>cd "C:\Users\Usama.Javed\Desktop\modern-portfolio" && git diff HEAD -- src/app/expertise/page.tsx | grep -E "^[+-]" | grep -iE "(metadata|application/ld\+json|<h1|<h2|<title|description:)" | grep -vE "interface|type |:\s*string|:\s*number" || echo "NO_CONTENT_CHANGE"</automated>
  Expect "NO_CONTENT_CHANGE" (the grep finds no content/SEO lines among the diff additions/deletions).
  </verify>
  <done>git diff confirms expertise/page.tsx changed only type annotations and access guards — no rendered text, metadata, certifications, or JSON-LD values; /expertise prerenders.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit 2>&1 | grep expertise` → no matches (18 → 0).
- `npm run lint 2>&1 | grep expertise` → no matches.
- Combined with 06-01, the overall tsc count reaches 0 (29 = 11 api + 18 expertise, both cleared).
- git diff shows type-only changes; no content/SEO drift.
</verification>

<success_criteria>
`src/app/expertise/page.tsx` passes `npx tsc --noEmit` and `npm run lint` with zero findings; the technicalExpertise data has an explicit type; the page's rendered content, metadata, and JSON-LD are byte-identical (type-only refactor). Overall tsc error count after 06-01 + 06-02: 0.
</success_criteria>

<output>
After completion, create `.planning/phases/06-hardening-ship/06-02-SUMMARY.md`
</output>
