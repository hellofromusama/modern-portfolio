---
phase: 06-hardening-ship
plan: 07
type: execute
wave: 3
depends_on: [06-01, 06-02, 06-03, 06-04, 06-05, 06-06]
files_modified:
  - next.config.ts
  - scripts/verify-prod.sh
autonomous: true
requirements: [SHIP-02, SHIP-01]
must_haves:
  truths:
    - "next.config.ts no longer ignores TS or ESLint errors during build (both flags removed/false)"
    - "npm run build passes a GENUINELY strict build (tsc + lint enforced) with zero errors"
    - "An unauth curl to /api/test-openai returns 401/403 with no key bytes (FIX-01 live)"
    - "A fresh homepage load fires no /api/auto-llm-training or /api/schedule-training (FIX-02 live)"
    - "All routes serve HTTP 200 in both themes on the strict prod build"
  artifacts:
    - path: "next.config.ts"
      provides: "Strict build config — ignoreBuildErrors + ignoreDuringBuilds removed/false"
    - path: "scripts/verify-prod.sh"
      provides: "Reusable post-deploy curl checklist (also used pre-deploy against local prod build)"
      min_lines: 15
  key_links:
    - from: "next.config.ts"
      to: "next build type/lint enforcement"
      via: "removed eslint.ignoreDuringBuilds + typescript.ignoreBuildErrors"
      pattern: "ignoreBuildErrors|ignoreDuringBuilds"
---

<objective>
SHIP-02 strict-build flip + the full Phase-6 automated gate. After every debt-burndown and fix plan has landed (Waves 1-2), remove the two `ignore*` flags from next.config.ts so the build genuinely enforces tsc + lint, prove a strict-green build, and run the complete verification: FIX-01 gate (unauth 401/403 + no key bytes), FIX-02 (no visitor-triggered paid fetch), both-theme route smoke, and a reusable post-deploy verify script.

Purpose: This is the moment Phase 6 ends verification theater. The flags can ONLY be flipped after Waves 1-2 clear all 29 tsc errors + 93 lint problems and add the FIX-01/FIX-02/VIS-08 changes cleanly. If anything regressed, the strict build fails here — that is the gate working.
Output: A strict next.config.ts, a green strict build, recorded gate evidence, and scripts/verify-prod.sh for the 06-08 deploy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-hardening-ship/06-RESEARCH.md

CURRENT next.config.ts (verified 2026-06-12, lines 7-12):
  eslint: { ignoreDuringBuilds: true },
  typescript: { ignoreBuildErrors: true },
-> Remove BOTH (or set false). Leave the rest of next.config.ts untouched (images.domains, headers, redirects, rewrites, compiler, experimental — research flags images.domains as a pre-existing warning that is OUT OF SCOPE; do not fix it here).

ORDER (research, the SHIP-02 completion gate) — STOP-on-dirty-tree is ENFORCED via explicit exit codes:
  1. Confirm gates clean FIRST, capturing exit codes verbatim BEFORE touching next.config.ts:
       npx tsc --noEmit; echo tsc-exit:$?
       npm run lint;     echo lint-exit:$?
     Acceptance to proceed: BOTH "tsc-exit:0" AND "lint-exit:0" must appear in the output. If EITHER is non-zero, a Wave-1/2 plan regressed — STOP, report which file, and do NOT flip the flags over a dirty tree (flipping would just move the failure into next build = re-created theater).
  2. ONLY after both exit:0 lines are confirmed, flip the flags in next.config.ts.
  3. THEN run: npm run build; echo build-exit:$? — must print "build-exit:0". This is SHIP-02 done.

FIX-01 / FIX-02 functional verification (post strict build, against a local prod server):
  - Start the prod build (npm start). Pick the served port.
  - FIX-01: `curl -s -o /dev/null -w "%{http_code}" http://localhost:PORT/api/test-openai` -> expect 401 or 403 (ADMIN_API_TOKEN unset locally => fail-closed 403). And `curl -s http://localhost:PORT/api/test-openai | grep -c keyUsed` -> 0 (no key bytes).
  - FIX-02: curl the homepage HTML and confirm it references VisitorTracker but the served JS no longer contains '/api/schedule-training' or '/api/auto-llm-training' fetch calls in the VisitorTracker chunk. (Definitive Network-tab check is the owner's browser pass at 06-08; the grep of the built chunk is the automatable proxy.)

POST-DEPLOY SCRIPT (scripts/verify-prod.sh) — research's checklist, parameterized by a BASE_URL arg (default https://www.usamajaved.com.au) so it runs against BOTH the local prod build AND the deployed preview/prod:
  - curl -I {BASE}/ -> 200
  - {BASE}/sitemap.xml -> valid XML, expected routes
  - {BASE}/robots.txt -> bot tiers intact
  - {BASE}/llms.txt -> Content-Type text/markdown
  - {BASE}/ | grep 'application/ld+json' -> JSON-LD blocks present (research says ~7)
  - {BASE}/api/test-openai -> 401/403, no keyUsed in body
  - {BASE}/usamajaved-indexnow-key-2026.txt -> 200 (IndexNow key file served)
  Make it bash, exit non-zero on any failed check, echo a clear PASS/FAIL line per check (one line starting with "PASS" or "FAIL" per item — this PASS/FAIL line format is what 06-08's log-grep asserts against).

CONSTRAINTS: zero new deps; do NOT touch unrelated next.config.ts (images.domains warning is out of scope); both-theme smoke is structural (served CSS carries both [data-theme] palettes — 05-09 pattern).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm gates clean, then flip the strict-build flags and prove a strict-green build</name>
  <files>next.config.ts</files>
  <action>
- FIRST, BEFORE editing next.config.ts, run the two gates capturing exit codes explicitly:
    `npx tsc --noEmit; echo tsc-exit:$?`
    `npm run lint; echo lint-exit:$?`
  Both MUST print exit code 0 ("tsc-exit:0" and "lint-exit:0"). If EITHER is non-zero, identify the offending file and STOP — report it as a regression and do NOT proceed to flip. Do not flip flags over a dirty tree.
- ONLY once both "tsc-exit:0" and "lint-exit:0" are confirmed, edit next.config.ts: remove the `eslint: { ignoreDuringBuilds: true }` block and the `typescript: { ignoreBuildErrors: true }` block (or set both to false). Leave ALL other config keys byte-identical (images, headers, redirects, rewrites, compiler, experimental).
- THEN run `npm run build; echo build-exit:$?`. It must print "build-exit:0" with the strict flags gone (next build now enforces tsc + lint). This is the SHIP-02 completion proof.
  </action>
  <verify>
  <automated>cd "C:\Users\Usama.Javed\Desktop\modern-portfolio" && echo "=== pre-flip gates (must show tsc-exit:0 AND lint-exit:0) ===" && (npx tsc --noEmit; echo tsc-exit:$?) && (npm run lint; echo lint-exit:$?) && echo "=== flags removed (expect 0) ===" && grep -cE "ignoreBuildErrors:\s*true|ignoreDuringBuilds:\s*true" next.config.ts && echo "=== no-ignore-flags sanity (expect 0) ===" && grep -cE "ignoreBuildErrors|ignoreDuringBuilds" next.config.ts | grep -v "false" ; echo "=== strict build ===" && (npm run build; echo build-exit:$?)</automated>
  Acceptance criteria (exact strings must appear verbatim in output, in this order): "tsc-exit:0" and "lint-exit:0" BEFORE the flip; then the flag grep returns 0 (no `: true` ignore flags remain); then "build-exit:0" AFTER the flip. If "tsc-exit:0"/"lint-exit:0" are NOT both present, the executor must STOP before flipping — a non-zero pre-flip exit is a regression to report, not to bury in next build.
  </verify>
  <done>Pre-flip output shows "tsc-exit:0" and "lint-exit:0" verbatim; next.config.ts then has no ignoreBuildErrors:true / ignoreDuringBuilds:true; post-flip output shows "build-exit:0" (strict build passes). SHIP-02 strict-flip proven over a verified-clean tree.</done>
</task>

<task type="auto">
  <name>Task 2: Author scripts/verify-prod.sh and run the FIX-01/FIX-02 + both-theme gate against the local prod build</name>
  <files>scripts/verify-prod.sh</files>
  <action>
- Create scripts/verify-prod.sh: a bash script taking BASE_URL as $1 (default https://www.usamajaved.com.au). It curls each checklist item (/, /sitemap.xml, /robots.txt, /llms.txt, / for JSON-LD count, /api/test-openai for 401/403 + no keyUsed, /usamajaved-indexnow-key-2026.txt) and echoes a "PASS"/"FAIL" line per check (each line must START with PASS or FAIL so a log-grep can count them), exiting non-zero on any failure. Reuse the research checklist verbatim.
- Run the gate against a LOCAL prod build: `npm start` (from the strict build in Task 1), then:
  - FIX-01: curl /api/test-openai -> 401/403 (ADMIN_API_TOKEN unset locally => fail-closed 403); body has no keyUsed.
  - FIX-02: confirm the built VisitorTracker chunk no longer contains '/api/schedule-training' or '/api/auto-llm-training' (grep the .next chunks or the served homepage JS).
  - Both-theme smoke: confirm the served global CSS carries both [data-theme=light] and :root (dark) palettes (05-09 structural pattern); spot-check 4-5 routes return HTTP 200.
- Record the results in the SUMMARY (these are the SHIP-01 evidence for the phase). The script itself is the reusable artifact for 06-08's post-deploy run.
  </action>
  <verify>
  <automated>cd "C:\Users\Usama.Javed\Desktop\modern-portfolio" && test -f scripts/verify-prod.sh && grep -cE "test-openai|sitemap|robots|ld\+json|indexnow" scripts/verify-prod.sh && grep -cE "PASS|FAIL" scripts/verify-prod.sh</automated>
  Expect the file to exist, the checklist-item grep >=4, and the PASS|FAIL grep >=1 (the script emits PASS/FAIL lines — required so 06-08 can grep its log). Functional curl results recorded in the SUMMARY.
  </verify>
  <done>scripts/verify-prod.sh exists, covers the research checklist, and emits PASS/FAIL lines per check; against the local strict prod build, /api/test-openai returns 401/403 with no keyUsed, the VisitorTracker chunk has no paid-endpoint fetch, both [data-theme] palettes ship in the served CSS, and spot-checked routes return 200 — all recorded in the SUMMARY.</done>
</task>

</tasks>

<verification>
- Pre-flip "tsc-exit:0" and "lint-exit:0" both present BEFORE next.config.ts is edited; "build-exit:0" present AFTER.
- next.config.ts: zero ignoreBuildErrors:true / ignoreDuringBuilds:true.
- curl /api/test-openai -> 401/403, no keyUsed in body.
- Built VisitorTracker chunk has no /api/schedule-training or /api/auto-llm-training reference.
- scripts/verify-prod.sh present + covers the checklist + emits PASS/FAIL lines per check.
- Both [data-theme] palettes in served CSS; spot-checked routes 200.
</verification>

<success_criteria>
SHIP-02 strict build proven (flags flipped ONLY after a verified-clean tree — "tsc-exit:0" + "lint-exit:0" captured before the flip, "build-exit:0" after); FIX-01 + FIX-02 confirmed live against the local prod build; a reusable PASS/FAIL-emitting post-deploy verify script exists for 06-08; both-theme structural smoke green. No unrelated config touched, no new deps.

NOTE (SHIP-01 a11y deferral): this plan proves the STRUCTURAL both-theme smoke (served CSS carries both [data-theme] palettes) and the automated FIX-01/FIX-02 gates. The LIVE both-theme VISUAL pass and the a11y KEYBOARD pass (Tab-through, focus rings, no traps) are deferred to 06-08's human-action owner checkpoint against the preview URL — they are browser-only and not automatable here. SHIP-01 is fully closed only after that 06-08 owner pass.
</success_criteria>

<output>
After completion, create `.planning/phases/06-hardening-ship/06-07-SUMMARY.md`
</output>
