---
phase: 06-hardening-ship
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/llm-training-dashboard/page.tsx
  - src/app/budget/page.tsx
  - src/app/ideas/page.tsx
  - src/app/fund-me/success/page.tsx
  - src/app/api/schedule-training/route.ts
  - src/components/TeamSection.tsx
  - src/components/Hero3DScene.tsx
  - src/components/AISeoContent.tsx
  - src/app/test-apis/page.tsx
  - src/app/fund-me/page.tsx
  - src/app/blog/page.tsx
  - src/app/api/test-free-llm/route.ts
  - src/app/team/page.tsx
  - src/app/contact/page.tsx
  - src/app/api/test-openai/route.ts
  - src/app/api/ai-verification/route.ts
  - src/components/InteractiveGlobe.tsx
autonomous: true
requirements: [SHIP-02]
must_haves:
  truths:
    - "npm run lint reports ZERO problems across all files owned by this plan (the remaining ~59)"
    - "The prefer-const auto-fixables are cleared (Hero3DScene + InteractiveGlobe)"
    - "The two <img> elements are resolved (next/image or justified disable) — no-img-element gone"
    - "No content/SEO/JSON-LD text changed by entity escaping (only the escape sequences differ)"
  artifacts:
    - path: "src/components/TeamSection.tsx"
      provides: "next/image (or justified disable) replacing <img> — no-img-element cleared"
  key_links: []
---

<objective>
Clear ALL remaining `npm run lint` problems (~59) in the pages and components NOT owned by 06-01 (api type-routes) or 06-02 (expertise) — the mechanical-but-spread-out half of the lint burndown (SHIP-02). These files have NO tsc errors, only lint findings.

Purpose: 06-05 flips `eslint.ignoreDuringBuilds` to `false`. Every file in the repo must be lint-clean or the strict build fails. 06-01 owns api type-route lint, 06-02 owns expertise lint; this plan owns everything else.
Output: The 17 owned files pass `npm run lint` with zero findings — content, copy, JSON-LD, and SEO text byte-identical (entity escaping changes only the escape sequence, never the rendered glyph).

EXECUTION NOTE (Fix-8): if Task 2's 11-file sweep stalls on non-trivial typing (a no-explicit-any that needs a real shape, not a mechanical swap), the executor MAY split it mid-execution into a components group (TeamSection, AISeoContent, test-apis/team/contact/fund-me/blog pages) and an api group (schedule-training, test-free-llm, test-openai, ai-verification) and clear them in two passes - executor discretion, document the split in the SUMMARY. Workflow aid only; the owned-file set and zero-finding bar are unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-hardening-ship/06-RESEARCH.md

# Lint problems owned by this plan (measured 2026-06-12) — by file:
#   llm-training-dashboard/page.tsx  — 13
#   budget/page.tsx                  — 12
#   ideas/page.tsx                   — 7
#   fund-me/success/page.tsx         — 4
#   api/schedule-training/route.ts   — 4
#   TeamSection.tsx                  — 3 (includes 1 no-img-element at ~line 165)
#   Hero3DScene.tsx                  — 3 (includes prefer-const at ~lines 185-187: x, z, y — AUTO-FIXABLE)
#   AISeoContent.tsx                 — 3
#   test-apis/page.tsx               — 3
#   fund-me/page.tsx                 — 2
#   blog/page.tsx                    — 2
#   api/test-free-llm/route.ts       — 2
#   team/page.tsx                    — 1 (may be the 2nd no-img-element)
#   contact/page.tsx                 — 1
#   api/test-openai/route.ts         — 1
#   api/ai-verification/route.ts     — 1
#   InteractiveGlobe.tsx             — prefer-const at ~line 265 (startTime — AUTO-FIXABLE)
#
# By rule (research + re-measured): no-unescaped-entities 34, no-unused-vars 28,
#   no-explicit-any 26, no-img-element 2, prefer-const 3 (auto-fixable).
#
# FIX APPROACH per rule:
#   - prefer-const (3, auto-fixable): run `npx eslint --fix` FIRST — clears Hero3DScene + InteractiveGlobe
#     for free. (Re-run on only the owned files if --fix touches others; this plan owns those two.)
#   - react/no-unescaped-entities: escape '/"/> in JSX copy (&apos; &quot; &gt;). The rule enumerates
#     each location — fix at source. CRITICAL: this changes the ESCAPE SEQUENCE only, never the rendered
#     glyph or the SEO/copy text. Do not reword anything.
#   - @typescript-eslint/no-unused-vars: remove unused vars/imports; unused catch bindings → `catch {}`
#     or prefix `_error`. Many are `catch (error)` that never use `error`.
#   - @typescript-eslint/no-explicit-any: replace any with the real type. Prefer real types; reserve
#     `eslint-disable` for genuine false positives only (research anti-pattern: disabling to hit zero).
#   - @next/next/no-img-element (TeamSection ~165, + 1 more): swap `<img>` for `next/image` with proper
#     width/height/alt, OR a targeted, justified disable if the image is genuinely dynamic/external and
#     next/image is impractical. Default to next/image for the team photos in public/team/.
#
# HARD CONSTRAINTS:
#   - blog/page.tsx, AISeoContent.tsx, and the server pages export metadata / JSON-LD / sr-only SEO copy —
#     entity escaping must NOT alter rendered text. Verify with git diff.
#   - blog/page.tsx is a SERVER component — do NOT add event handlers (05-09's /blog prerender-crash
#     lesson). Lint fixes here are escaping/unused-var only.
#   - api/test-openai + api/schedule-training + api/test-free-llm: lint-ONLY here (no security gating —
#     that's 06-04's job, file-disjoint by wave). Just clear unused-vars/any. Do NOT remove the keyUsed
#     leak here (06-04 owns test-openai's security edit; touching it in both waves would collide).
#   - NO behavior change anywhere; NO new dependencies.
#   - Match each file's existing quote style.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auto-fix prefer-const, then clear lint in the 4 heavy pages (llm-training-dashboard, budget, ideas, fund-me/success)</name>
  <files>src/components/Hero3DScene.tsx, src/components/InteractiveGlobe.tsx, src/app/llm-training-dashboard/page.tsx, src/app/budget/page.tsx, src/app/ideas/page.tsx, src/app/fund-me/success/page.tsx</files>
  <action>
- FIRST run `npx eslint --fix src/components/Hero3DScene.tsx src/components/InteractiveGlobe.tsx` to clear the 3 prefer-const auto-fixables (Hero3DScene x/z/y ~185-187, InteractiveGlobe startTime ~265). Confirm those two files are now lint-clean (they had only prefer-const).
- Then clear the heavy pages by hand:
  - llm-training-dashboard/page.tsx (13): replace any with real types, remove unused vars, escape JSX entities.
  - budget/page.tsx (12): same — no-explicit-any → typed shapes (the budget request/response), unused-vars, entity escaping.
  - ideas/page.tsx (7): unused-vars + any + entity escaping. This is a client page with IdeaNetworkCanvas — do not touch the canvas wiring, only lint.
  - fund-me/success/page.tsx (4): unused-vars / any.
- Escaping changes the escape sequence only; do not reword copy. No behavior change.
  </action>
  <verify>
  <automated>cd "C:\Users\Usama.Javed\Desktop\modern-portfolio" && npm run lint 2>&1 | grep -E "Hero3DScene|InteractiveGlobe|llm-training-dashboard|budget[\\\\/]page|ideas[\\\\/]page|fund-me[\\\\/]success" | wc -l</automated>
  Expect 0.
  </verify>
  <done>Hero3DScene, InteractiveGlobe, llm-training-dashboard, budget, ideas, and fund-me/success all report zero lint problems; prefer-const auto-fixed; no behavior change.</done>
</task>

<task type="auto">
  <name>Task 2: Clear lint in the remaining components, pages, and api/* (test/schedule routes — lint only) + resolve the 2 no-img-element</name>
  <files>src/app/api/schedule-training/route.ts, src/components/TeamSection.tsx, src/components/AISeoContent.tsx, src/app/test-apis/page.tsx, src/app/fund-me/page.tsx, src/app/blog/page.tsx, src/app/api/test-free-llm/route.ts, src/app/team/page.tsx, src/app/contact/page.tsx, src/app/api/test-openai/route.ts, src/app/api/ai-verification/route.ts</files>
  <action>
- TeamSection.tsx (3, incl. no-img-element ~165): swap the `<img>` for `next/image` with explicit width/height/alt (team photos live in public/team/, so next/image works). Clear the other 2 (unused-vars/any).
- team/page.tsx (1): likely the 2nd no-img-element OR an unused-var — resolve accordingly (next/image if img).
- AISeoContent.tsx (3): SERVER sr-only SEO component — escape entities WITHOUT changing the semantic copy; remove unused vars. Verify rendered text unchanged.
- test-apis/page.tsx (3), contact/page.tsx (1), fund-me/page.tsx (2), blog/page.tsx (2): unused-vars + any + entity escaping. blog is a server component — escaping/unused-var ONLY, no event handlers.
- api/schedule-training/route.ts (4), api/test-free-llm/route.ts (2), api/test-openai/route.ts (1), api/ai-verification/route.ts (1): lint-ONLY - clear unused-vars/any. DO NOT add the admin guard or remove keyUsed here (06-04 owns those security edits; this plan only de-lints). Use `catch {}` for unused catch bindings. NOTE: if test-openai's single lint finding is a no-unused-vars on the keyUsed line, do NOT fix it here - 06-04 REMOVES that keyUsed variable as part of its security edit, which clears the finding. Leave it for 06-04 rather than touching test-openai's security surface in this wave.
- Match each file's quote style; no behavior change; no new deps.
  </action>
  <verify>
  <automated>cd "C:\Users\Usama.Javed\Desktop\modern-portfolio" && npm run lint 2>&1 | tail -2</automated>
  Expect zero problems for ALL owned files. Combined with 06-01 + 06-02, the whole tree should be `0 problems` — but since those run in parallel, here just confirm none of THIS plan's 17 files appear in the lint output.
  </verify>
  <done>All remaining owned files report zero lint problems; both no-img-element findings resolved (next/image or justified disable); api test/schedule routes de-linted without touching their security surface; no content/SEO text changed (git-diff verified).</done>
</task>

</tasks>

<verification>
- `npm run lint 2>&1 | grep -vE "expertise|api[\\/](auto-llm-training|ai-training|create-checkout|budget-estimate)"` → no problem lines from this plan's files (the excluded patterns belong to 06-01/06-02).
- `git diff HEAD -- src/app/blog/page.tsx src/components/AISeoContent.tsx | grep -iE "<h[12]|metadata|ld\+json" ` shows only escape-sequence changes, no reworded copy.
- No new dependency; `git diff package.json` empty.
</verification>

<success_criteria>
All 17 owned files pass `npm run lint` with zero findings; prefer-const auto-fixed; both `<img>` elements resolved; entity escaping preserved all rendered/SEO text verbatim; the api test/schedule routes are de-linted but their security surface is untouched (06-04 owns that). No behavior change, no new deps.
</success_criteria>

<output>
After completion, create `.planning/phases/06-hardening-ship/06-03-SUMMARY.md`
</output>
