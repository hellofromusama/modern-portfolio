---
phase: 06-hardening-ship
plan: 08
type: execute
wave: 4
depends_on: [06-07]
files_modified: []
autonomous: false
requirements: [SHIP-02, SHIP-01]
user_setup:
  - service: vercel
    why: "Preview + production deploy of the modern-portfolio project; production promote requires owner approval"
    env_vars:
      - name: ADMIN_API_TOKEN
        source: "Set in Vercel project 'modern-portfolio' Settings -> Environment Variables before deploy so the FIX-01 gate is live (and the owner can call the gated routes)"
    dashboard_config:
      - task: "Confirm the modern-portfolio project link + that apex (usamajaved.com.au) and www are mapped at the project level (they persist across prod deploys)"
        location: "Vercel dashboard -> modern-portfolio -> Settings -> Domains"
must_haves:
  truths:
    - "A Vercel PREVIEW deploy (not --prod) is produced and its URL passes the verify-prod checklist"
    - "The owner explicitly approves production promotion before any vercel --prod runs"
    - "After production deploy, all routes/SEO assets are intact and the domain alias is preserved"
  artifacts: []
  key_links: []
---

<objective>
SHIP-02 final: ship a Vercel PREVIEW deploy of `modern-portfolio`, run the full post-deploy verification checklist against the preview URL, then a BLOCKING owner checkpoint for visual review and production approval — and ONLY after explicit owner approval, promote to production with `vercel --prod`.

Purpose: Production deploy is contractually the owner's decision (PROJECT.md: "production deploy gated on owner approval"). The preview-first flow (research Pitfall 5) avoids clobbering the usamajaved.com.au / www domain alias. The owner-approval checkpoint is `checkpoint:human-action` so the auto-chain CANNOT auto-approve it — the production promote happens only on the owner's explicit word.
Output: A verified preview deploy, owner sign-off, and (post-approval) a production deploy with the domain alias intact and the full SEO surface confirmed live.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-hardening-ship/06-RESEARCH.md

VERIFIED (research, 2026-06-12): Vercel CLI 48.1.6 present. Project: modern-portfolio (serves usamajaved.com.au + www). Domains are attached at the project level and PERSIST across prod deploys. scripts/verify-prod.sh (created in 06-07) is the post-deploy checklist runner, parameterized by BASE_URL.

DEPLOY FLOW (research, Pitfall 5 — do NOT clobber the domain):
  1. `vercel link` — confirm linked to project modern-portfolio (skip if already linked).
  2. Ensure ADMIN_API_TOKEN is set in Vercel env so the FIX-01 gate is live in the deploy.
  3. `vercel` (NO --prod) — produce a PREVIEW deploy. Capture the preview URL.
  4. Run `bash scripts/verify-prod.sh <preview-url>` — all checklist items must pass against the preview:
     / 200, sitemap.xml, robots.txt, llms.txt content-type, JSON-LD present, /api/test-openai 401/403 (no keyUsed), indexnow key file 200. ALSO confirm View Transitions animate in Chrome + instant under reduced-motion, and DevTools Network shows no /api/auto-llm-training or /api/schedule-training on a fresh visit (FIX-02).
  5. BLOCKING owner checkpoint (human-action — NOT auto-approvable): owner does the live visual both-theme / reduced-motion / keyboard pass on the preview URL and explicitly approves (or rejects with notes).
  6. ONLY on explicit approval: `vercel --prod` to promote. Then run `bash scripts/verify-prod.sh https://www.usamajaved.com.au` to confirm prod + domain alias intact.

If the owner rejects: capture the notes, do NOT promote; the gaps route back to a follow-up plan (do not force the deploy).

AUTH NOTE: if `vercel` errors with an auth prompt, that surfaces as a dynamic auth gate (owner logs into Vercel CLI, then retry) — do not pre-plan it; handle on occurrence.

CONSTRAINTS: NEVER run vercel --prod before the owner checkpoint approval. Preserve every route/slug/SEO asset (additive mandate). No code changes in this plan — deploy + verify + approval only.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Produce a Vercel preview deploy and verify it against the post-deploy checklist</name>
  <files></files>
  <action>
- Confirm `vercel link` points at project modern-portfolio (run it if not linked; if it prompts for auth, surface that as an auth gate for the owner and retry after).
- Confirm ADMIN_API_TOKEN is set in the Vercel project env (so the FIX-01 gate is live in the preview). If you cannot confirm, note it for the owner in the checkpoint.
- Run `vercel` (NO --prod) to produce a PREVIEW deploy. Capture the preview URL.
- Run `bash scripts/verify-prod.sh <preview-url>` (the script from 06-07). All checklist items must pass: / 200, sitemap.xml valid, robots.txt tiers, llms.txt content-type, JSON-LD present, /api/test-openai 401/403 with no keyUsed, indexnow key file 200.
- Record the preview URL + the checklist results for the checkpoint. Do NOT promote to production.
  </action>
  <verify>
  <automated>cd "C:\Users\Usama.Javed\Desktop\modern-portfolio" && test -f scripts/verify-prod.sh && echo "verify script present — run: bash scripts/verify-prod.sh <preview-url>"</automated>
  The preview deploy + checklist run is executed live; the automatable proxy is that scripts/verify-prod.sh exists and the preview URL + per-line PASS results are recorded in the SUMMARY (deploy actions are inherently interactive — the owner checkpoint gates correctness).
  </verify>
  <done>A Vercel preview deploy exists; scripts/verify-prod.sh passes against the preview URL (all checklist items green); the preview URL + results are recorded; production has NOT been promoted.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Owner production-approval gate (non-bypassable)</name>
  <action>Checkpoint — no code change. The executor presents the verified preview deploy for the owner to review per how-to-verify below; BLOCKS for explicit production approval (human-action, not auto-approvable).</action>
  <what-built>A verified Vercel PREVIEW deploy of modern-portfolio (NOT production). Strict build, FIX-01 gate live, FIX-02 fan-out removed, VIS-08 transitions, all SHIP-01 gates green. The post-deploy checklist passed against the preview URL.</what-built>
  <how-to-verify>
  OWNER PRODUCTION-APPROVAL GATE (this is NOT auto-approvable — production deploy is the owner's contractual decision):
  1. Open the preview URL (recorded above) in a browser.
  2. Do a live visual pass in BOTH themes (toggle dark/light): hero, projects grid, AI engineering section, fund-me, blog — confirm everything renders correctly and nothing is broken.
  3. Confirm View Transitions feel subtle in Chrome/Edge; enable OS reduced-motion and confirm navigation is instant.
  4. Keyboard pass: Tab through nav, theme toggle, cards, buttons — visible focus rings, no traps.
  5. Spot-check a couple of routes return real content (not 404/500).
  6. If all good, approve production promotion. If not, describe what to fix (it routes to a follow-up plan; production is NOT promoted).
  AFTER approval, the executor runs `vercel --prod` then `bash scripts/verify-prod.sh https://www.usamajaved.com.au` to confirm prod + domain alias intact.
  </how-to-verify>
  <resume-signal>Type "approve production" to promote to production, or describe the issues to fix first (no promotion until explicit approval).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: On owner approval — promote to production and verify the live domain</name>
  <files></files>
  <action>
- ONLY proceed if the owner explicitly approved in the checkpoint above. If they did not, STOP — do not run vercel --prod; capture the rejection notes for a follow-up plan.
- On approval: run `vercel --prod` to promote the approved build to production.
- Run `bash scripts/verify-prod.sh https://www.usamajaved.com.au` against the live domain. Confirm all checklist items pass on production: apex + www serve, sitemap/robots/llms intact, JSON-LD present, /api/test-openai 401/403 (no keyUsed), indexnow key file 200, domain alias preserved.
- Record the production URL + final checklist results in the SUMMARY. This completes SHIP-02.
  </action>
  <verify>
  <automated>cd "C:\Users\Usama.Javed\Desktop\modern-portfolio" && echo "On approval: vercel --prod then bash scripts/verify-prod.sh https://www.usamajaved.com.au — record results in SUMMARY"</automated>
  Production promote is gated on the checkpoint; the SUMMARY must record the prod URL + per-line checklist PASS results + confirmation the domain alias persisted.
  </verify>
  <done>If approved: production deploy live at usamajaved.com.au (+ www), verify-prod.sh all-green against the live domain, every route/SEO asset intact, domain alias preserved. If not approved: no promote, rejection notes captured.</done>
</task>

</tasks>

<verification>
- A preview deploy URL exists and passed scripts/verify-prod.sh (recorded).
- The owner checkpoint was reached and explicitly resolved (approve/reject) — NOT auto-approved.
- If approved: vercel --prod ran, verify-prod.sh green against https://www.usamajaved.com.au, domain alias intact.
- No route/slug/SEO asset removed (additive mandate held).
</verification>

<success_criteria>
SHIP-02 complete: a verified preview deploy, an explicit owner production-approval gate (human-action, non-bypassable), and — only on approval — a production deploy to usamajaved.com.au with the full SEO surface and domain alias intact. No code changes; deploy + verify + approval only.
</success_criteria>

<output>
After completion, create `.planning/phases/06-hardening-ship/06-08-SUMMARY.md`
</output>
