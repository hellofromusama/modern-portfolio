---
phase: 05-component-upgrades
plan: 08
type: execute
wave: 5
depends_on: ["05-02", "05-03", "05-06"]
files_modified:
  - src/app/blog/page.tsx
  - src/app/budget/page.tsx
  - src/app/team/page.tsx
  - src/app/ideas/page.tsx
  - src/app/contact/page.tsx
  - src/app/fund-me/page.tsx
autonomous: true
requirements: [VIS-07]
user_setup: []
must_haves:
  truths:
    - "Cards and typography on the blog, budget, team, ideas, contact, and fund-me routes conform to the tightened tokens"
    - "Hardcoded glass/color literals on these pages are replaced with tokens; both themes render correctly"
    - "The /ideas IdeaNetworkCanvas and /fund-me InteractiveGlobe are confirmed gated/DPR-correct/theme-reactive in context (from 05-02)"
    - "Blog JSON-LD is preserved verbatim; form logic (budget/contact/ideas) and Stripe/checkout (fund-me) are untouched"
  artifacts:
    - path: "src/app/ideas/page.tsx"
      provides: "form + cards over IdeaNetworkCanvas on token cards; canvas gate confirmed in context"
      contains: "var(--"
    - path: "src/app/fund-me/page.tsx"
      provides: "donation cards on tokens; Globe gate/DPR/theme confirmed; Stripe untouched"
      contains: "var(--"
  key_links:
    - from: "src/app/ideas/page.tsx"
      to: "src/components/IdeaNetworkCanvas.tsx"
      via: "background canvas (upgraded in 05-02) — verify gate fires on this page"
      pattern: "IdeaNetworkCanvas"
    - from: "src/app/fund-me/page.tsx"
      to: "src/components/InteractiveGlobe.tsx"
      via: "Globe (upgraded in 05-02) — verify DPR/theme/gate on this page"
      pattern: "InteractiveGlobe"
---

<objective>
Token + typography conformance pass (VIS-07) over the remaining 6 of the 12 routes: `/blog` (+ articles), `/budget`, `/team`, `/ideas`, `/contact`, `/fund-me`. Same convention as 05-07 — token cards, replaced glass/color literals, unified hover, both themes correct. Two of these compose the canvases upgraded in 05-02 (IdeaNetworkCanvas on `/ideas`, InteractiveGlobe on `/fund-me`) so this plan depends on 05-02 and confirms the gate/DPR/theme in context. SEO guard on the server `/blog` pages; never touch form/Stripe/email logic. File-disjoint from 05-07.

Purpose: finish the 12-page token pass with the canvas-bearing routes verified in context.
Output: edited 6 page files (visual surfaces only; canvas behavior verified, not re-authored).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-component-upgrades/05-RESEARCH.md
@.planning/phases/05-component-upgrades/05-02-SUMMARY.md

<interfaces>
<!-- Same card/type token convention as 05-07 (research §Typography & Cards Pass). -->
// Card: bg-card + 1px border-subtle + shadow-card + rounded-xl/2xl; hover lift -1..-2px + border-hover + bg-card-hover.
// Headings: space-grotesk var + tightened scale + two-tone --text-primary/secondary. Replace bg-white/[0.0x], border-white/[0.1], raw hex/rgba → tokens.
// /ideas uses IdeaNetworkCanvas (upgraded 05-02); /fund-me uses InteractiveGlobe (upgraded 05-02) + AnimatedIcons + FundMeWidget — those component upgrades are DONE; here just verify the gate/DPR/theme fire in context and tokenize the PAGE cards.
// SEO GUARD: /blog + /blog/* are server pages with Blog JSON-LD — do NOT alter metadata/JSON-LD/slugs/copy.
// DO NOT touch: budget/contact/ideas form submit logic, fund-me Stripe/checkout. Visual surfaces only.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: blog + budget + contact — token cards & type</name>
  <files>src/app/blog/page.tsx, src/app/budget/page.tsx, src/app/contact/page.tsx</files>
  <action>
    For EACH: **read first**, apply the card/type token convention to visual surfaces ONLY.
    - **`blog/page.tsx` (server):** article/list cards + prose type scale → token cards + tightened headings. **Preserve the Blog JSON-LD + metadata + copy verbatim** (SEO-01). (The two `/blog/*` article pages: if they share inline card styling, apply the same token treatment to their visual surfaces too — but only if trivially in-scope; otherwise note them for 05-09 smoke. Keep their metadata/JSON-LD untouched.)
    - **`budget/page.tsx` (client):** calculator UI cards/inputs → token cards + inputs styled via tokens. **DO NOT touch** the `/api/budget-estimate` submit logic or the estimate state machine.
    - **`contact/page.tsx` (client):** form fields/cards → token cards + token-styled inputs/focus states. **DO NOT touch** the `/api/send-email`/emailjs submit logic.
    Replace residual glass/color literals with tokens for correct light theme.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | (grep -iE "blog/page|budget/page|contact/page" && exit 1 || exit 0)</automated>
  </verify>
  <done>blog/budget/contact cards + type + inputs use tokens with unified hover, no hardcoded glass/color literals, Blog JSON-LD + form/API logic untouched. Type-checks clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: team + ideas + fund-me — token cards & in-context canvas verify</name>
  <files>src/app/team/page.tsx, src/app/ideas/page.tsx, src/app/fund-me/page.tsx</files>
  <action>
    For EACH (all client pages): **read first**, apply the token card/type convention to visual surfaces ONLY, AND confirm the 05-02 canvas upgrades behave in context.
    - **`team/page.tsx`:** team cards (mirrors TeamSection) → token cards + headings. Keep team data + image handling.
    - **`ideas/page.tsx`:** the idea-submission form + cards layered over `IdeaNetworkCanvas` → token cards. Confirm `IdeaNetworkCanvas` (upgraded in 05-02) is mounted with a container the gate can observe — verify the rAF pauses off-screen here (note in summary). DO NOT touch the idea-submit/emailjs logic.
    - **`fund-me/page.tsx`:** donation cards → token cards + headings. Confirm `InteractiveGlobe` (upgraded in 05-02) renders DPR-correct + theme-reactive + gated on this page, and `AnimatedIcons`/`FundMeWidget` (05-06) read correctly. **DO NOT touch** the Stripe `/api/create-checkout` flow.
    Replace residual glass/color literals with tokens.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | (grep -iE "team/page|ideas/page|fund-me/page" && exit 1 || exit 0)</automated>
  </verify>
  <done>team/ideas/fund-me cards + type use tokens; IdeaNetworkCanvas + InteractiveGlobe confirmed gated/DPR/theme-correct in context; form/Stripe logic untouched. Type-checks clean.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` + `npm run lint` clean; `npm run build` succeeds (all routes incl. /blog/* prerender).
- `rg "bg-white/\[0|border-white/\[0" src/app/blog/page.tsx src/app/budget/page.tsx src/app/team/page.tsx src/app/ideas/page.tsx src/app/contact/page.tsx src/app/fund-me/page.tsx` → no residual glass literals.
- `rg "application/ld\+json|export const metadata" src/app/blog/page.tsx` → unchanged from baseline (Blog JSON-LD preserved).
- `rg "create-checkout" src/app/fund-me/page.tsx` and `rg "send-email|budget-estimate" src/app/contact/page.tsx src/app/budget/page.tsx` → submit logic intact.
- Manual (05-09 gate): each route both themes; /ideas canvas pauses off-screen; /fund-me globe crisp on DPR-2 both themes.
</verification>

<success_criteria>
- Cards + typography on blog, budget, team, ideas, contact, fund-me conform to tightened tokens, both themes (VIS-07) — completing the 12-page pass.
- Canvas-bearing routes verified gated/DPR/theme-correct in context; all form/Stripe/email logic preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/05-component-upgrades/05-08-SUMMARY.md`. Record: whether the two /blog/* article pages needed token edits, and the in-context gate confirmation for /ideas + /fund-me.
</output>
