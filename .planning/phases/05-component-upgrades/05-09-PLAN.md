---
phase: 05-component-upgrades
plan: 09
type: execute
wave: 6
depends_on: ["05-01", "05-02", "05-03", "05-04", "05-05", "05-06", "05-07", "05-08"]
files_modified: []
autonomous: false
requirements: [PERF-02, SHIP-01]
user_setup: []
must_haves:
  truths:
    - "The full automated gate is green: npx tsc --noEmit + npm run lint + npm run build + node scripts/check-bundle.mjs"
    - "three is route-split — absent from shared/text/SEO routes — even with the homepage hero mounted (bundle gate)"
    - "Hero LCP ≤ 2.5s and CLS ~0 on a mobile, throttled, PRODUCTION build with the LCP element = the <h1>/poster (not <canvas>)"
    - "Every upgraded surface passes both-theme + reduced-motion + WebGL-disabled + off-screen-pause smoke on a hard refresh across all 12 routes"
    - "No residual hardcoded color/glass literals remain in any upgraded component or page (grep gate)"
  artifacts: []
  key_links: []
---

<objective>
The Phase-5 SHIP-01 verification gate (and final PERF-02 confirmation). No source edits — this plan runs the full automated suite, the Phase-4 bundle re-run with the hero mounted, the Lighthouse LCP/CLS measurement on a production build, a residual-literal grep sweep, and a structured human smoke (both themes + reduced-motion + WebGL-disabled + off-screen pause + hard-refresh) across all 12 routes. Gated behind a blocking human checkpoint because the LCP/CLS/both-theme/fallback judgments are inherently manual.

Purpose: prove the phase is genuinely done (not `next build`-theater — `ignoreBuildErrors`/`ignoreDuringBuilds` are true, so tsc + lint must run explicitly).
Output: a green gate + recorded evidence in the SUMMARY; no file changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-component-upgrades/05-RESEARCH.md
@.planning/phases/05-component-upgrades/05-VALIDATION.md
@.planning/phases/05-component-upgrades/05-00-SUMMARY.md
@.planning/phases/05-component-upgrades/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Full automated gate + bundle re-run + residual-literal grep sweep</name>
  <files>(verification only — no files modified)</files>
  <action>
    Run the full automated suite from the repo root and capture results (research §Validation Architecture):
    1. `npx tsc --noEmit` → MUST be clean on all phase-touched files. (Pre-existing unrelated errors in `api/expertise` etc. noted in Phase-1 history are out of scope — confirm no NEW errors in Phase-5 files.)
    2. `npm run lint` → clean on phase-touched files.
    3. `npm run build` → succeeds; all 12 routes + projects/[id] slugs + /blog/* prerender.
    4. `node scripts/check-bundle.mjs` → PASS: `three`/R3F/drei/postprocessing absent from the shared/text/SEO chunks, present only in the hero scene chunk (PERF-01 carried; the homepage route was added to CANVAS_ROUTES in 05-00).
    5. **Residual-literal grep sweep (VIS-06/07):** across the upgraded components + pages, confirm NO hardcoded color/glass literals remain — e.g. `rg "slate-900|pink-500|purple-500|bg-white/\[0|border-white/\[0" src/components src/app` should return nothing in upgraded files (FundMeWidget especially), and `rg "rgba\(96, ?165, ?250|rgba\(139, ?92, ?246" src/components/InteractiveGlobe.tsx src/components/IdeaNetworkCanvas.tsx` → nothing. Record any intentional exceptions.
    If any check fails → STOP, list failures with file:line, do NOT proceed to the human checkpoint.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json && npm run build && node scripts/check-bundle.mjs</automated>
  </verify>
  <done>tsc clean (no new Phase-5 errors), lint clean, build succeeds with all routes prerendering, bundle gate PASS (three route-split with hero mounted), no residual hardcoded literals in upgraded files. Failures (if any) listed and blocking.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Phase-5 both-theme / reduced-motion / WebGL / LCP / off-screen smoke (12 routes)</name>
  <files>(verification only — no source files modified)</files>
  <action>Blocking human-verify checkpoint. After Task 1's automated gate is green, run a PRODUCTION build (`npm run build && npm start`) and walk the user through the full smoke matrix in `how-to-verify` across all 12 routes. Do NOT mark Phase 5 complete until the user types "approved".</action>
  <what-built>The complete Phase-5 visual upgrade: WebGL hero, DPR-correct theme-reactive globe + idea-network, motion-v12 ScrollReveal/InteractiveButton, accessible Navigation + ThemeToggle, elevated Footer/FAQ/TeamSection/AnimatedIcons/FundMeWidget, and token-conformant cards/typography across all 12 routes.</what-built>
  <how-to-verify>
    Run a PRODUCTION build (`npm run build && npm start`) — dev-mode LCP is not representative. Then:
    1. **Hero LCP/CLS (mobile):** Lighthouse (CLI if 05-00 found it, else DevTools panel, mobile + throttled) on `/` → LCP ≤ 2.5s, CLS ≤ 0.1 (target 0), LCP element = `<h1>`/poster, NOT `<canvas>`. Record the numbers.
    2. **Both themes, all 12 routes (HARD REFRESH, not client nav):** `/`, `/projects/[id]`, `/expertise`, `/services`, `/blog` (+ `/blog/*`), `/budget`, `/team`, `/tech-stack`, `/ideas`, `/contact`, `/developer-australia`, `/fund-me` → cards/type correct in BOTH themes; light mode never broken (no dark-only surfaces). FundMeWidget + Globe + IdeaNetwork read correctly in light.
    3. **Reduced-motion:** OS/DevTools emulate reduce → hero = static poster; ScrollReveal/Team/FAQ/FundMeWidget animations static; every page fully readable.
    4. **WebGL-disabled:** disable WebGL → hero falls back to poster, no white-screen.
    5. **Off-screen + tab-blur pause (PERF-02):** DevTools Performance — on `/` (hero), `/ideas` (network), `/fund-me` (globe): scroll the animation off-screen AND switch tabs → the loops stop rendering. Confirm for all three.
    6. **Navigation a11y:** Tab through nav (focus-visible rings), open/close the mobile menu (Esc closes, focus trapped + restored, `aria-expanded` toggles).
    7. **No regressions:** every pre-existing feature/route/SEO asset still present (additive mandate).
  </how-to-verify>
  <verify>Human approval after the smoke matrix passes on a production build. No automated command — LCP/CLS/both-theme/reduced-motion/WebGL/pause/a11y judgments are inherently manual (the suite is gated automated in Task 1).</verify>
  <done>User typed "approved": LCP/CLS within budget, both themes correct on all 12 routes (hard refresh), reduced-motion + WebGL-disabled fallbacks work, all three canvas loops pause off-screen + tab-blur, nav keyboard-accessible, zero regressions.</done>
  <resume-signal>Type "approved" to mark Phase 5 complete, or list any LCP/CLS/theme/reduced-motion/fallback/a11y/pause issue → route to `/gsd:plan-phase --gaps`.</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` + `npm run lint` + `npm run build` + `node scripts/check-bundle.mjs` all green.
- Lighthouse (mobile, prod build) on `/`: LCP ≤ 2.5s, CLS ≤ 0.1, LCP element = text/poster.
- Both-theme + reduced-motion + WebGL-disabled + off-screen-pause + hard-refresh smoke passes on all 12 routes.
- Grep sweep: no residual hardcoded color/glass literals in upgraded files.
- Human checkpoint approved.
</verification>

<success_criteria>
- Phase passes `npx tsc --noEmit` + `npm run lint` + `npm run build` + both-theme smoke (SHIP-01).
- All animation loops pause off-screen + on tab-blur; mobile DPR caps + capability-appropriate scenes confirmed (PERF-02).
- three route-split preserved; LCP/CLS within budget; zero regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/05-component-upgrades/05-09-SUMMARY.md`. Record: measured LCP/CLS, bundle-gate result, any deferred/known issues, and the both-theme/reduced-motion/WebGL/pause smoke outcome per route.
</output>
