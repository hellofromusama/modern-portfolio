---
phase: 05-component-upgrades
plan: 05
type: execute
wave: 4
depends_on: ["05-03"]
files_modified:
  - src/components/Footer.tsx
  - src/components/FAQ.tsx
  - src/components/TeamSection.tsx
autonomous: true
requirements: [VIS-06, VIS-07]
user_setup: []
must_haves:
  truths:
    - "Footer, FAQ, and TeamSection conform to the tightened tokens and inherit the motion-v12 ScrollReveal upgrade — every existing feature preserved"
    - "FAQ accordion expand/collapse is smoothed via motion while the FAQPage JSON-LD block is preserved VERBATIM (SEO)"
    - "TeamSection member cross-fade + progress dots are smoothed via motion while auto-cycle + hover-pin logic is preserved"
    - "No hardcoded color/glass literals remain in these three components — all colors are tokens"
  artifacts:
    - path: "src/components/FAQ.tsx"
      provides: "motion accordion (height auto / AnimatePresence) + token colors; JSON-LD + category filter preserved"
      contains: "useReducedMotion"
    - path: "src/components/TeamSection.tsx"
      provides: "motion member cross-fade + dots; auto-cycle + hover-pin preserved; tokens"
      contains: "useThemeColors|--accent|token"
    - path: "src/components/Footer.tsx"
      provides: "token conformance + consistent hover-elevation; inherits ScrollReveal upgrade"
      contains: "var(--"
  key_links:
    - from: "src/components/FAQ.tsx"
      to: "motion/react"
      via: "AnimatePresence / animate height for the accordion"
      pattern: "motion/react"
---

<objective>
Token + motion-preset elevation pass over three self-contained components (VIS-06/VIS-07): Footer, FAQ, TeamSection. Each preserves its identity and every feature; the work is token conformance (replace any residual hardcoded color/glass literals), consistent hover-elevation, and smoothing key animations on motion v12. Runs in Wave 4 after the ScrollReveal/motion-primitive rewrite (05-03) so these inherit it. File-disjoint from 05-04 and 05-06.

Purpose: elevate three of the remaining-17 components to the new design language without feature loss.
Output: edited `Footer.tsx`, `FAQ.tsx`, `TeamSection.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-component-upgrades/05-RESEARCH.md
@.planning/phases/05-component-upgrades/05-03-SUMMARY.md

<interfaces>
// motion v12: import { motion, AnimatePresence, useReducedMotion } from 'motion/react';
// Phase-3 tokens (globals.css): --bg-card/--bg-card-hover/--bg-elevated, --border-subtle/default/hover, --text-primary/secondary/tertiary, --shadow-card, --accent-blue. Card surface convention (research §Typography & Cards): bg-card + 1px border-subtle + shadow-card + rounded-xl/2xl; hover lift translateY(-1px..-2px) + border-hover + bg-card-hover.
// ScrollReveal (05-03): same prop API — these components already wrap it; the upgrade is automatic.

<!-- Read at execution (PRESERVE): -->
// Footer.tsx — 4-col grid, ScrollReveal-wrapped, token colors, hover translate-x. Light touch: verify tokens + unify hover-elevation.
// FAQ.tsx — accordion (max-height CSS transition), category filter, ScrollReveal, FAQPage JSON-LD block, token colors. PRESERVE the JSON-LD VERBATIM (SEO-01 locked).
// TeamSection.tsx — auto-cycling featured member, hover-to-pin, grayscale→color photo, progress dots, --accent-blue. PRESERVE auto-cycle + hover-pin.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: FAQ — motion accordion + token pass (JSON-LD preserved verbatim)</name>
  <files>src/components/FAQ.tsx</files>
  <action>
    **Read first:** the WHOLE current `FAQ.tsx` (locate the FAQPage JSON-LD `<script type="application/ld+json">` block and the category filter).
    1. **Accordion (research §Per-Component → FAQ):** replace the `max-height` CSS transition with a motion expand — `AnimatePresence` + animate `height: auto` (or motion `layout`) for a smoother, content-accurate open/collapse. Honor `useReducedMotion()` → instant toggle. Keep the open/close state + category filter behavior unchanged.
    2. **Tokens (VIS-07):** ensure every surface uses card tokens (`--bg-card`, `--border-subtle`, `--shadow-card`, `--text-*`); replace any residual hardcoded glass literals (`bg-white/[0.0x]`, `border-white/[0.1]`) or raw colors with tokens. Unify hover-elevation to the standard lift.
    3. **PRESERVE VERBATIM:** the FAQPage JSON-LD block (do NOT alter its content/structure — SEO-01 is locked/complete). Touch only className/style/motion around it.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | (grep -i "FAQ\.tsx" && exit 1 || exit 0)</automated>
  </verify>
  <done>FAQ accordion animates via motion (reduced-motion → instant), all surfaces use tokens (no hardcoded glass/color literals), category filter intact, FAQPage JSON-LD preserved verbatim. Type-checks clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: TeamSection + Footer — motion smoothing + token conformance</name>
  <files>src/components/TeamSection.tsx, src/components/Footer.tsx</files>
  <action>
    **Read first:** both files.
    **TeamSection (research §Per-Component):** smooth the featured-member cross-fade and the progress-dot transitions via motion (`AnimatePresence`/`motion` opacity+transform), honoring `useReducedMotion()`. PRESERVE the auto-cycle timer, hover-to-pin logic, grayscale→color photo effect, and progress dots. Tokenize any residual literals (use `--accent-blue` etc.). Optional: consider `next/image` for the photos if it doesn't change layout (CLS) — only if trivial; otherwise leave the `<img>` as-is.
    **Footer (research §Per-Component — light touch):** it's already ScrollReveal-wrapped (inherits 05-03) and mostly tokenized — verify token conformance across the 4-col grid, replace any stray hardcoded color/glass literal with tokens, and unify the link hover to the standard hover treatment (keep the translate-x identity if desired, but make colors token-driven). No feature changes.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | (grep -iE "TeamSection|Footer\.tsx" && exit 1 || exit 0)</automated>
  </verify>
  <done>TeamSection member cross-fade + dots animate via motion (reduced-motion safe) with auto-cycle/hover-pin/photo effects preserved; Footer fully token-conformant with unified hover. Both type-check clean.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` + `npm run lint` clean; `npm run build` succeeds.
- `rg "application/ld\+json" src/components/FAQ.tsx` → still present (JSON-LD preserved).
- `rg "bg-white/\[0|border-white/\[0|rgba\(" src/components/Footer.tsx src/components/FAQ.tsx src/components/TeamSection.tsx` → no residual hardcoded glass/color literals (all tokens).
- `rg "useReducedMotion" src/components/FAQ.tsx src/components/TeamSection.tsx` → both match.
- Manual (05-09 gate): both themes — FAQ expand/collapse smooth, Team cross-fade smooth, Footer correct in light mode.
</verification>

<success_criteria>
- Footer, FAQ, TeamSection elevated to the new design language, token-conformant, motion-smoothed (VIS-06, VIS-07).
- Every existing feature (filter, JSON-LD, auto-cycle, hover-pin) preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/05-component-upgrades/05-05-SUMMARY.md`.
</output>
