---
phase: 05-component-upgrades
plan: 03
type: execute
wave: 3
depends_on: ["05-01"]
files_modified:
  - src/components/ScrollReveal.tsx
  - src/components/InteractiveButton.tsx
autonomous: true
requirements: [VIS-04]
user_setup: []
must_haves:
  truths:
    - "ScrollReveal (+ StaggerReveal, AnimatedCounter, MagneticHover, TextReveal, ParallaxSection) are re-implemented on motion v12 with the EXACT same prop surface and named exports — call sites do not churn"
    - "Reduced-motion short-circuits every reveal/interaction to the static end-state (parity with the current early-return)"
    - "InteractiveButton hover/lift/glow/tap run on motion v12 with restraint and keep the variant/size/href API + token styling + a focus-visible ring"
    - "The signature easing [0.16,1,0.3,1] (from lib/motion.ts) is preserved so motion-driven components feel identical"
  artifacts:
    - path: "src/components/ScrollReveal.tsx"
      provides: "motion v12 whileInView/variants/stagger internals; identical prop API + all 6 named exports"
      contains: "whileInView"
      min_lines: 60
    - path: "src/components/InteractiveButton.tsx"
      provides: "motion whileHover/whileTap on the polymorphic button; variant/size/href + token styling + focus-visible preserved"
      contains: "whileHover"
  key_links:
    - from: "src/components/ScrollReveal.tsx"
      to: "src/lib/motion.ts"
      via: "imports EASE_SIGNATURE/transitions/variants for shared easing"
      pattern: "lib/motion"
    - from: "src/components/ScrollReveal.tsx"
      to: "motion/react"
      via: "motion, useReducedMotion from 'motion/react'"
      pattern: "from 'motion/react'"
---

<objective>
Re-implement the widely-imported motion primitives on motion v12 (VIS-04) with senior restraint, keeping their EXACT prop surfaces and named exports so the ~every-page call sites do not churn: `ScrollReveal` (default) + `StaggerReveal`, `AnimatedCounter`, `MagneticHover`, `TextReveal`, `ParallaxSection` (all in `ScrollReveal.tsx`), and `InteractiveButton`. These are consumed across pages, Footer, FAQ, TeamSection, Navigation — so they land in Wave 3 (after the hero), BEFORE the component/page waves verify against them. Reduced-motion short-circuits to the static end-state; the signature easing comes from Phase-3 `lib/motion.ts`.

Purpose: one declarative motion-v12 API replacing per-component IntersectionObserver state machines, with zero call-site breakage.
Output: rewritten internals of `ScrollReveal.tsx` + `InteractiveButton.tsx` (APIs unchanged).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-component-upgrades/05-RESEARCH.md
@.planning/phases/05-component-upgrades/05-00-SUMMARY.md

<interfaces>
<!-- Phase 3 presets (lib/motion.ts) — import, don't re-derive: -->
// EASE_SIGNATURE = [0.16,1,0.3,1]; transitions {base:0.7s, quick:0.4s}; fadeUp: Variants; stagger(gap?): Variants
// motion v12: import { motion, useReducedMotion, AnimatePresence, useSpring } from 'motion/react';  (NOT framer-motion)
// whileInView + viewport={{ once, amount }} replaces the IntersectionObserver; variants + staggerChildren for stagger.

<!-- PRESERVE these exact prop surfaces (read the current file to copy them verbatim): -->
// ScrollReveal: { children, className?, delay=0, direction='up'|'down'|'left'|'right'|'none', distance=40, duration=700(ms), once=true, threshold=0.1 }
// StaggerReveal: maps children into ScrollReveal with incremental delay  (keep prop shape)
// AnimatedCounter: rAF count-up, ease-out-cubic, threshold:0.5  (keep prop shape)
// MagneticHover: { children, strength?, className? } mousemove translate toward cursor, springs back
// TextReveal: per-character stagger  | ParallaxSection: { speed } translateY on scroll
// InteractiveButton: polymorphic <a>/<button>; variants primary|secondary|ghost; sizes sm|md|lg; href?; token styling; ripple + hover glow + 1px lift
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: ScrollReveal.tsx — motion v12 rewrite (all 6 exports, same API)</name>
  <files>src/components/ScrollReveal.tsx</files>
  <action>
    **Read first:** the WHOLE current `ScrollReveal.tsx` (copy every prop name + default + named export signature verbatim) and `src/lib/motion.ts` (presets/easing).
    Rewrite internals on motion v12 (research §Pattern 3 + §Code Examples), KEEPING the default export + all named exports (`StaggerReveal`, `AnimatedCounter`, `MagneticHover`, `TextReveal`, `ParallaxSection`) with identical prop surfaces:
    - `import { motion, useReducedMotion, useScroll, useTransform, useSpring } from 'motion/react';` and easing/transitions from `@/lib/motion`.
    - **ScrollReveal:** map `direction`→offset (`up:{y:distance}`, `down:{y:-distance}`, `left:{x:distance}`, `right:{x:-distance}`, `none:{}`). `const reduce = useReducedMotion(); if (reduce) return <div className={className}>{children}</div>;` (static end-state — parity with current early-return). Else `<motion.div initial={{opacity:0,...offset}} whileInView={{opacity:1,x:0,y:0}} viewport={{ once, amount: threshold }} transition={{ duration: duration/1000, delay: delay/1000, ease: EASE_SIGNATURE }}>`.
    - **StaggerReveal:** use parent/child `variants` with `staggerChildren` (use `stagger()` from lib/motion) OR keep the incremental-delay mapping — but preserve its prop shape and `once` semantics.
    - **AnimatedCounter:** keep the rAF count-up + ease-out-cubic (it's fine), but GATE it — trigger via motion `whileInView`/`onViewportEnter` (or keep IO but ensure reduced-motion shows the final number immediately) and don't run before in-view.
    - **MagneticHover:** re-implement with `useSpring` toward the cursor offset (cap strength — restraint); desktop-only (skip on touch / when `reduce`), spring back to 0 on leave.
    - **TextReveal:** per-character stagger via variants; reduced-motion → plain text.
    - **ParallaxSection:** keep subtle `translateY` via `useScroll`+`useTransform` (or the existing passive listener); respect `reduce` (no transform). Keep it gentle (anti-feature: no deep stacked parallax).
    Match the file's existing double-quote/2-space style. Do NOT change any exported name or prop — downstream waves verify against these.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | (grep -i "ScrollReveal" && exit 1 || exit 0)</automated>
  </verify>
  <done>`ScrollReveal.tsx` exports the same default + 5 named components with identical prop surfaces, internals on motion v12 (`whileInView`/`variants`/`useSpring`), reduced-motion → static end-state, signature easing from lib/motion. Type-checks clean; no call site needs editing.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: InteractiveButton.tsx — motion hover/tap on motion v12 (same API)</name>
  <files>src/components/InteractiveButton.tsx</files>
  <action>
    **Read first:** the WHOLE current `InteractiveButton.tsx` (copy variant/size/href/polymorphic API verbatim).
    Re-implement the interaction layer on motion (research §Per-Component → InteractiveButton):
    - Render `motion.button` / `motion.a` (polymorphic — `<a>` when `href` given) keeping the 3 token-styled variants (`primary`/`secondary`/`ghost` via `--btn-*`) and sizes (`sm`/`md`/`lg`).
    - `whileHover` = subtle lift (`y: -1`/`-2`) + the existing box-shadow glow; `whileTap` = `scale: 0.98` (restraint). Keep the cursor-follow radial glow OR replace the styled-jsx ripple with a clean motion tap-scale — either is fine; do NOT make it gaudy.
    - `transition` from `transitions.quick` (lib/motion) so it matches the signature easing.
    - Respect `useReducedMotion()` → drop hover/tap transforms (keep color/focus states).
    - **a11y (VIS-04 floor):** add a visible `focus-visible` ring (token-based, e.g. `--accent-blue`) on both the button and anchor forms — net-new, required.
    Keep all props, token styling, and the polymorphic href behavior identical so every CTA call site is unaffected.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | (grep -i "InteractiveButton" && exit 1 || exit 0)</automated>
  </verify>
  <done>`InteractiveButton` runs hover/lift/glow/tap on motion v12 with restraint, keeps the variant/size/href API + `--btn-*` token styling, adds a focus-visible ring, and honors reduced-motion. Type-checks clean; call sites unchanged.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` + `npm run lint` clean; `npm run build` succeeds (proves no call-site break across pages).
- `rg "export (default function|function|const) (ScrollReveal|StaggerReveal|AnimatedCounter|MagneticHover|TextReveal|ParallaxSection)" src/components/ScrollReveal.tsx` → all 6 still exported.
- `rg "from 'motion/react'|from \"motion/react\"" src/components/ScrollReveal.tsx src/components/InteractiveButton.tsx` → both match (motion v12, not framer-motion).
- `rg "useReducedMotion" src/components/ScrollReveal.tsx src/components/InteractiveButton.tsx` → both match (reduced-motion parity).
- `rg "focus-visible" src/components/InteractiveButton.tsx` → match (a11y floor).
</verification>

<success_criteria>
- ScrollReveal + micro-interactions (InteractiveButton, hover, magnetic) re-implemented on motion v12 with restraint and reduced-motion parity (VIS-04).
- Exact prop surfaces + named exports preserved → zero call-site churn for downstream waves.
</success_criteria>

<output>
After completion, create `.planning/phases/05-component-upgrades/05-03-SUMMARY.md`. Record: whether StaggerReveal uses variants-stagger or delay-mapping, and any prop additions (should be none beyond focus-visible styling).
</output>
