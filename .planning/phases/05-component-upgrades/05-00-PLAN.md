---
phase: 05-component-upgrades
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - scripts/check-bundle.mjs
  - .planning/phases/05-component-upgrades/05-00-SPIKE.md
autonomous: true
requirements: [PERF-02, SHIP-01]
user_setup: []
must_haves:
  truths:
    - "All Phase 3 + Phase 4 primitives consumed by Phase 5 are confirmed present before any scene code is written (hard BLOCK if absent)"
    - "The actual hook/component signatures are read from source and recorded — scene code aligns to the real API, not the plan sketch"
    - "maath is installed (+ r3f-perf dev, + postprocessing only if bloom is kept) at registry-verified versions"
    - "A device/throttled perf spike picks the hero particle count and the bloom on/off decision from measured fps, not theory"
    - "The Phase-4 bundle gate is updated so the homepage scene mount is asserted to keep three route-split"
  artifacts:
    - path: ".planning/phases/05-component-upgrades/05-00-SPIKE.md"
      provides: "Recorded primitive signatures, chosen particle count, bloom decision, installed versions, Lighthouse availability"
      contains: "particle count"
    - path: "scripts/check-bundle.mjs"
      provides: "Phase-4 bundle gate updated so CANVAS_ROUTES includes the homepage route now mounting the scene"
      contains: "CANVAS_ROUTES"
  key_links:
    - from: ".planning/phases/05-component-upgrades/05-00-SPIKE.md"
      to: "src/hooks/useAnimationGate.ts"
      via: "recorded actual return shape read at execution"
      pattern: "useAnimationGate"
---

<objective>
De-risk the flagship before any production scene code. Three jobs: (1) a HARD prerequisite guard that BLOCKS the phase if any Phase-3/Phase-4 primitive Phase 5 consumes is absent, plus reading the ACTUAL hook/component signatures from source (not the sketches in research); (2) install the Phase-5-only deps (`maath`, dev `r3f-perf`, and `@react-three/postprocessing` only if bloom is kept) at registry-verified versions; (3) a throttled perf spike that picks the hero particle count (2,000–3,000 band) and the bloom on/off decision from MEASURED fps, and updates the Phase-4 bundle gate to cover the homepage scene mount.

Purpose: Research Open Questions 1 + 3 resolved before committing the hero; everything downstream aligns to real APIs and measured numbers.
Output: `05-00-SPIKE.md` (recorded signatures + decisions), installed deps, updated `scripts/check-bundle.mjs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-component-upgrades/05-RESEARCH.md

<interfaces>
<!-- Contracts Phase 5 CONSUMES — confirm these against the real files in Task 1; do NOT re-author them. -->

// src/hooks/useAnimationGate.ts  ('use client')  — Phase 3 (03-02)
export function useAnimationGate<T extends Element>(
  ref: React.RefObject<T | null>,
  opts?: { rootMargin?: string; threshold?: number }
): { shouldAnimate: boolean; prefersReduced: boolean; inView: boolean; tabVisible: boolean };

// src/hooks/useThemeColors.ts  ('use client')  — Phase 3 (03-02)
export function useThemeColors(varNames: string[]): Record<string, string>; // raw hex tokens; re-reads on data-theme

// src/lib/motion.ts  — Phase 3 (03-02): EASE_SIGNATURE [0.16,1,0.3,1], transitions{base,quick}, fadeUp, stagger(gap)
// src/components/IslandBoundary.tsx — Phase 3: class boundary; CONFIRM the exact fallback prop name + export style
// src/components/three/{SceneCanvas,ClientScene,ScenePoster,ThemedScene}.tsx — Phase 4 (04-02)
//   ClientScene: dynamic(ssr:false) + IslandBoundary + ScenePoster — the ONLY public import surface
//   SceneCanvas: dpr={[1,2]}, frameloop from useAnimationGate, context-loss → poster
// src/lib/webgl.ts — Phase 4: isWebGLAvailable()
// scripts/check-bundle.mjs — Phase 4: bundle gate (CANVAS_ROUTES). CONFIRM it exists and read its current shape.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Prereq HARD-guard + read actual primitive signatures</name>
  <files>.planning/phases/05-component-upgrades/05-00-SPIKE.md</files>
  <action>
    **BLOCK gate (research §Wave 0 Gaps + Environment).** Verify EVERY Phase-3/4 primitive exists before any scene code is authored this phase. Run an existence check on all of:
    `src/hooks/useAnimationGate.ts`, `src/hooks/useThemeColors.ts`, `src/lib/motion.ts`, `src/components/IslandBoundary.tsx`, `src/components/three/SceneCanvas.tsx`, `src/components/three/ClientScene.tsx`, `src/components/three/ScenePoster.tsx`, `src/components/three/ThemedScene.tsx`, `src/lib/webgl.ts`, `scripts/check-bundle.mjs`.
    Use a shell test that exits non-zero if ANY is missing, e.g. `for f in <paths>; do test -f "$f" || { echo "MISSING $f"; exit 1; }; done`. If any file is missing → STOP and report "Phase 3/4 not merged — BLOCK" (do NOT re-author the primitive; the orchestrator must resolve).

    Then **read the real files** (Read tool) for: `useAnimationGate.ts`, `useThemeColors.ts`, `lib/motion.ts`, `IslandBoundary.tsx` (exact `fallback` prop name + default-vs-named export), `ClientScene.tsx` (props it accepts + how it accepts a scene — does it import a hardcoded `SceneCanvas`/`ThemedScene`, or take a render prop / scene prop? This determines how the hero scene is mounted in 05-01). Record the ACTUAL signatures verbatim in `05-00-SPIKE.md` under a "Confirmed Primitive Signatures" heading (Research Open Q 3). If `ClientScene` hardcodes `ThemedScene` with no scene-injection prop, note that 05-01 must parameterize `ClientScene`/`SceneCanvas` to accept a `scene` (and record the cleanest injection approach).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p=['src/hooks/useAnimationGate.ts','src/hooks/useThemeColors.ts','src/lib/motion.ts','src/components/IslandBoundary.tsx','src/components/three/SceneCanvas.tsx','src/components/three/ClientScene.tsx','src/components/three/ScenePoster.tsx','src/lib/webgl.ts','scripts/check-bundle.mjs'];const m=p.filter(f=>!fs.existsSync(f));if(m.length){console.error('MISSING',m);process.exit(1)}console.log('all present')"</automated>
  </verify>
  <done>All 10 primitives confirmed present (guard passes). Actual signatures of useAnimationGate/useThemeColors/IslandBoundary/ClientScene recorded in 05-00-SPIKE.md, including how ClientScene mounts a scene (and whether 05-01 must parameterize it).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Install Phase-5 deps at registry-verified versions</name>
  <files>package.json, package-lock.json, .planning/phases/05-component-upgrades/05-00-SPIKE.md</files>
  <action>
    First confirm Phase 4 actually installed the core 3D stack: `node -e "const p=require('./package.json');console.log(p.dependencies['three'],p.dependencies['@react-three/fiber'],p.dependencies['@react-three/drei'],p.dependencies['motion'])"`. If any are absent → BLOCK (Phase 4 not merged).

    Verify registry versions (training data is stale — research §Standard Stack): `npm view maath version @react-three/postprocessing version r3f-perf version`. Then install:
    - `npm install maath@^0.10.8` (particle in-sphere distribution + damped easing — replaces hand-rolled math).
    - `npm install -D r3f-perf@^7.2.3` (dev-only FPS/draw-call HUD for the spike; NEVER ships — gate its usage behind `process.env.NODE_ENV !== 'production'`).
    - `@react-three/postprocessing@^3.0.4` — install ONLY if Task 3's spike keeps bloom. If the spike drops bloom, do NOT install it (record the decision in the spike doc).
    Pin to the registry-confirmed latest in the `^0.10.x` / `^3.0.x` / `^7.2.x` lines if they differ from the sketch. Record installed exact versions in `05-00-SPIKE.md`.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json');const d={...p.dependencies,...p.devDependencies};if(!d['maath']){console.error('maath missing');process.exit(1)}if(!d['r3f-perf']){console.error('r3f-perf missing');process.exit(1)}console.log('maath',d['maath'],'r3f-perf',d['r3f-perf'],'postproc',d['@react-three/postprocessing']||'(omitted)')"</automated>
  </verify>
  <done>`maath` in dependencies, `r3f-perf` in devDependencies, both at registry-verified versions. `@react-three/postprocessing` installed iff bloom is kept (Task 3). Exact versions recorded in spike doc; type-check still clean (`npx tsc --noEmit`).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Hero perf spike (particle count + bloom decision) + update bundle gate</name>
  <files>scripts/check-bundle.mjs, .planning/phases/05-component-upgrades/05-00-SPIKE.md</files>
  <action>
    **Spike (research Open Q 1 — pick numbers from measurement, throwaway code only — do NOT commit a production route).** Mount a temporary local scene (e.g. a scratch `/_hero-spike` route or reuse the Phase-4 `/_scene-harness`) rendering a `<points>` field via `maath/random.inSphere` + drei `<Points>`/`<PointMaterial>` at 2000 / 2500 / 3000, with `r3f-perf` HUD on, under CPU throttling (DevTools 4x–6x). Confirm draw calls stay < 10 and fps stays ~60 (research budget: < 100 draw calls → 60fps). Toggle a gated `<Bloom>` and observe fps cost. **Decide:** the production particle count (conservative end of the band if no real mid-tier device is available) and bloom ON/OFF. Record both with the measured fps in `05-00-SPIKE.md`. DELETE the scratch route before finishing — it must NOT remain in the build.

    Also probe Lighthouse CLI availability: `npx lighthouse --version` (record present/absent + the DevTools-panel fallback note) for the 05-09 LCP gate.

    **Update the Phase-4 bundle gate (`scripts/check-bundle.mjs`):** read its current `CANVAS_ROUTES` array (or equivalent) and add the homepage route (the route that mounts the hero scene in 05-01 — likely `/` / `app/page`) so the gate asserts `three`/R3F lands only in the scene chunk and stays ABSENT from shared/text routes once the hero is wired. Keep the existing routes. If the script's matcher needs tuning against the real `.next` manifest, run `npm run build` then `node scripts/check-bundle.mjs` and adjust until it passes on the current (pre-hero) build.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('scripts/check-bundle.mjs','utf8');if(!/CANVAS_ROUTES|canvasRoutes|three/i.test(s)){console.error('bundle gate not recognizable');process.exit(1)}console.log('bundle gate present')" && node -e "const d=require('fs').readFileSync('.planning/phases/05-component-upgrades/05-00-SPIKE.md','utf8');if(!/particle count/i.test(d)||!/bloom/i.test(d)){console.error('spike decisions not recorded');process.exit(1)}console.log('spike recorded')"</automated>
  </verify>
  <done>Spike measured fps at 2000/2500/3000 particles under throttling; production particle count + bloom decision recorded in 05-00-SPIKE.md; scratch spike route deleted. Lighthouse CLI availability recorded. `scripts/check-bundle.mjs` updated to include the homepage route and passes on the current build.</done>
</task>

</tasks>

<verification>
- The Task-1 existence guard exits 0 (all Phase-3/4 primitives present); if it ever exits non-zero the phase is BLOCKED.
- `package.json` lists `maath` + dev `r3f-perf`; `@react-three/postprocessing` present iff bloom kept.
- `05-00-SPIKE.md` records: confirmed primitive signatures (incl. how ClientScene mounts a scene), chosen particle count, bloom on/off with measured fps, installed versions, Lighthouse availability.
- `scripts/check-bundle.mjs` includes the homepage route and passes `node scripts/check-bundle.mjs` after `npm run build`.
- No scratch spike route remains in `src/app/`.
</verification>

<success_criteria>
- Hard prereq guard green; real primitive signatures recorded so all downstream scene/component code aligns to the actual API (PERF-02 foundation; SHIP-01 gate readiness).
- Phase-5 deps installed at verified versions; bloom decision made from measurement.
- Bundle gate ready to prove three stays route-split once the hero mounts on the homepage.
</success_criteria>

<output>
After completion, create `.planning/phases/05-component-upgrades/05-00-SUMMARY.md`. Record: the confirmed signatures (esp. ClientScene scene-injection shape), chosen particle count, bloom on/off, installed versions, Lighthouse CLI present/absent.
</output>
