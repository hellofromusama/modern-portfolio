---
phase: 05-component-upgrades
plan: 01
type: execute
wave: 2
depends_on: ["05-00"]
files_modified:
  - src/components/three/HeroScene.tsx
  - src/components/three/HeroParticles.tsx
  - src/components/three/HeroEffects.tsx
  - src/components/three/ScenePoster.tsx
  - src/components/Hero3D.tsx
autonomous: false
requirements: [VIS-01, PERF-02]
user_setup: []
must_haves:
  truths:
    - "The homepage hero renders a WebGL icosahedron (the brand motif, now real depth + crisp edges) suspended in a GPU particle field"
    - "Particle motion is GPU-driven (group/field rotation or a uTime uniform) — NO per-frame CPU position writes"
    - "Icosahedron edge + particle colors come from useThemeColors → THREE.Color and visibly change on data-theme toggle (no remount)"
    - "The hero <h1> text overlay stays the LCP element; a ScenePoster paints instantly behind it; the scene fades in on top with zero CLS"
    - "Reduced-motion / WebGL-unavailable / low-power all degrade to the static poster; the scene frameloop pauses off-screen and on tab-blur"
  artifacts:
    - path: "src/components/three/HeroParticles.tsx"
      provides: "Single <points> GPU particle field (maath inSphere), one draw call, theme-tinted, paused-aware"
      contains: "inSphere"
      min_lines: 25
    - path: "src/components/three/HeroScene.tsx"
      provides: "Concept A scene graph: drei Icosahedron + Edges + HeroParticles, theme colors, slow rotation gated by paused"
      contains: "Icosahedron"
      min_lines: 30
    - path: "src/components/Hero3D.tsx"
      provides: "Canvas layer swapped from Hero3DScene to the Phase-4 ClientScene mounting HeroScene; text overlay preserved verbatim"
      contains: "ClientScene"
  key_links:
    - from: "src/components/three/HeroScene.tsx"
      to: "src/hooks/useThemeColors.ts"
      via: "useThemeColors(['--accent-blue','--accent-violet']) → new THREE.Color()"
      pattern: "useThemeColors"
    - from: "src/components/three/HeroScene.tsx"
      to: "src/components/three/HeroParticles.tsx"
      via: "renders <HeroParticles count={...} paused={paused} />"
      pattern: "HeroParticles"
    - from: "src/components/Hero3D.tsx"
      to: "src/components/three/ClientScene.tsx"
      via: "renders ClientScene mounting HeroScene behind the z-10 text overlay"
      pattern: "ClientScene"
---

<objective>
Build the signature WebGL hero (VIS-01) — Concept A "Icosahedron Evolution + Particle Field" — on the Phase-4 boundary, and swap the homepage hero's canvas layer to it while preserving the `<h1>` text overlay verbatim. The icosahedron is the existing brand motif raised to real WebGL (crisp `<Edges>`, true depth, optional gated bloom), suspended in a single GPU `<points>` field (count + bloom decided by 05-00's spike). Colors flow through `useThemeColors`; the frameloop is gated by the Phase-4 `SceneCanvas`/`useAnimationGate`; the poster is the instant LCP backdrop with zero CLS. This is the highest-leverage, highest-CWV-risk surface and validates the entire Phase-4 infra end-to-end — it is gated behind a human verification checkpoint (LCP/CLS/both-theme) before any other component proceeds.

Purpose: deliver the WebGL "wow" within the mobile budget while preserving brand identity and the LCP path.
Output: `HeroScene.tsx`, `HeroParticles.tsx`, (opt) `HeroEffects.tsx`, hero ScenePoster variant, edited `Hero3D.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-component-upgrades/05-RESEARCH.md
@.planning/phases/05-component-upgrades/05-00-SUMMARY.md

<interfaces>
<!-- Consume these EXACTLY as recorded in 05-00-SUMMARY.md (read it first — it has the REAL signatures, not these sketches). -->

// Phase 3:  useThemeColors(['--accent-blue','--accent-violet']) → Record<string,string> (raw hex; re-reads on data-theme)
//   tokens: dark --accent-blue #60a5fa / --accent-violet #a78bfa ; light #3b82f6 / #8b5cf6. THREE.Color accepts hex directly.
// Phase 4:  SceneCanvas (dpr [1,2], frameloop = useAnimationGate, context-loss→poster), passes `paused = !shouldAnimate || prefersReduced` to its scene.
// Phase 4:  ClientScene — dynamic(ssr:false) + IslandBoundary + ScenePoster. The ONLY public import surface.
//   *** 05-00 recorded whether ClientScene hardcodes ThemedScene or accepts a scene-injection prop. Mount HeroScene via that recorded mechanism. ***
//   If ClientScene/SceneCanvas hardcode ThemedScene, parameterize them to accept a `scene` prop (default = ThemedScene to preserve the Phase-4 harness) and pass HeroScene from Hero3D — do NOT fork a second Canvas/boundary.
// Phase 4:  isWebGLAvailable() from src/lib/webgl.ts ; ScenePoster (token gradient, full-size, the LCP element).
// drei (named imports only): Icosahedron, Edges, Float, Points, PointMaterial.  maath/random: inSphere.
</interfaces>

<precedent>
<!-- Read at execution: -->
// src/components/Hero3D.tsx — KEEP the z-10 text overlay (<h1> "Usama / Javed"), the mouse ref, min-h-[92vh], var(--canvas-opacity).
//   Today: const Hero3DScene = dynamic(() => import('./Hero3DScene'), { ssr:false, loading: ... }). Replace ONLY the canvas layer with ClientScene mounting HeroScene.
// src/components/Hero3DScene.tsx — KEEP as a fallback reference (its setTransform DPR pattern is correct); do NOT delete it (additive mandate).
// Research §Code Examples — HeroScene.tsx + HeroParticles.tsx reference implementations (Concept A).
// Particle count + bloom on/off = the values 05-00-SPIKE.md decided. HeroEffects.tsx only exists if bloom kept.
</precedent>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: HeroParticles — single GPU points field (one draw call)</name>
  <files>src/components/three/HeroParticles.tsx</files>
  <action>
    Create `src/components/three/HeroParticles.tsx` (research §Code Examples + Pattern 2). `'use client'`. Props `{ count?: number; paused?: boolean; color?: THREE.Color | string }`.
    - `const positions = useMemo(() => inSphere(new Float32Array(count * 3), { radius: 3 }) as Float32Array, [count]);` — import `{ inSphere } from 'maath/random'`. Correct in-sphere distribution; positions buffer is STATIC.
    - Render a single drei `<Points positions={positions} frustumCulled>` with `<PointMaterial transparent size={0.012} sizeAttenuation depthWrite={false} color={color} />` (additive feel via small size + no depth write). ONE `<points>` = ONE draw call.
    - Animate the WHOLE field cheaply: `useFrame((_, delta) => { if (paused || !ref.current) return; ref.current.rotation.y += delta * 0.02; })`. **NEVER loop over particles mutating positions per frame** (research Pitfall 4 / Anti-Pattern). For richer motion a `uTime` uniform in a shaderMaterial is allowed — still one draw call, still no CPU position writes.
    - `count` defaults to the value 05-00 chose; the caller (HeroScene) passes the spike-decided count.
    Named drei imports only (research Pitfall 7). 2-space, semicolons, double-quote style for new three files (match HeroScene).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | (grep -i "three/HeroParticles" && exit 1 || exit 0)</automated>
  </verify>
  <done>`HeroParticles` renders ONE `<Points>` from a static `inSphere` buffer, animates only the field rotation when not paused (no per-particle CPU writes), accepts a theme `color`, type-checks clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: HeroScene (Concept A) + optional gated HeroEffects</name>
  <files>src/components/three/HeroScene.tsx, src/components/three/HeroEffects.tsx</files>
  <action>
    Create `src/components/three/HeroScene.tsx` (research §Hero Scene Design Concept A + §Code Examples). `'use client'`. Props `{ paused: boolean }` (provided by SceneCanvas as `!shouldAnimate || prefersReduced`).
    - `const t = useThemeColors(['--accent-blue', '--accent-violet']);` then `useMemo` THREE.Colors: `edge = new THREE.Color(t['--accent-blue'] || '#60a5fa')`, `accent = new THREE.Color(t['--accent-violet'] || '#a78bfa')`, each keyed on the resolved token (no remount on theme toggle — research Pattern 3).
    - Root `<group ref={group}>` rotating slowly in `useFrame` ONLY when `!paused` (`rotation.y += delta * 0.15`).
    - Brand motif: `<Float speed={1} rotationIntensity={0.4} floatIntensity={0.6} enabled={!paused}><Icosahedron args={[1.4, 0]}><meshBasicMaterial transparent opacity={0.04} color={accent} /><Edges color={edge} /></Icosahedron></Float>` (faint inner solid in the second accent + crisp edges in the first — both theme-reactive).
    - `<HeroParticles count={SPIKE_COUNT} paused={paused} color={edge} />` (use the count 05-00 chose).
    - **Mouse parallax (restraint):** lerp the camera or the `group` toward normalized mouse using `maath/easing.damp` (frame-rate-independent). If `Hero3D` exposes a mouse ref, thread it through; otherwise read a normalized pointer via a window listener set up once (NO DOM reads per frame). Keep it subtle (research Concept A).
    - **Bloom (only if 05-00 kept it):** create `src/components/three/HeroEffects.tsx` (`'use client'`) exporting a gated `<EffectComposer><Bloom mipmapBlur intensity={...} /></EffectComposer>` from `@react-three/postprocessing`; render it from HeroScene ONLY when `!paused` (so reduced-motion/low-power skip it). If 05-00 dropped bloom, do NOT create HeroEffects.tsx (and remove it from `files_modified` mentally — leave it uncreated).
    Named drei imports only. All colors via tokens; the only hardcoded hex are the hydration fallbacks.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | (grep -iE "three/HeroScene|three/HeroEffects" && exit 1 || exit 0)</automated>
  </verify>
  <done>`HeroScene` renders the icosahedron (`<Edges>` + faint fill) + `HeroParticles`, colors via `useThemeColors` THREE.Colors keyed on resolved tokens, rotates/parallaxes only when `!paused`. Bloom present iff 05-00 kept it, gated off when paused. Type-checks clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Hero ScenePoster variant + swap Hero3D canvas layer</name>
  <files>src/components/three/ScenePoster.tsx, src/components/Hero3D.tsx</files>
  <action>
    **Read first:** `src/components/three/ScenePoster.tsx` (Phase-4 poster), `src/components/three/ClientScene.tsx` (the recorded scene-injection mechanism), `src/components/Hero3D.tsx` (text overlay, mouse ref, `min-h-[92vh]`, `var(--canvas-opacity)`).

    Part A — `ScenePoster.tsx`: extend (additively, no breaking change) to support a hero-tuned variant — either a `variant?: 'default' | 'hero'` prop or an extra accent glow — that matches the hero's icosahedron-glow backdrop using ONLY tokens (`--bg-secondary`→`--bg-primary` gradient + a faint `--accent-blue` radial glow), sized to fill the `min-h-[92vh]` container so the canvas mounts on top with ZERO CLS (research Pitfall 2). No animation/timers — it is the instant LCP backdrop and the reduced-motion/no-WebGL view. Keep the existing Phase-4 default behavior intact.

    Part B — `Hero3D.tsx`: swap ONLY the canvas layer. Replace `const Hero3DScene = dynamic(() => import('./Hero3DScene'), { ssr:false, ... })` usage with the Phase-4 `ClientScene` mounting `HeroScene` (via the injection mechanism 05-00 recorded — pass `HeroScene` as the scene, hero ScenePoster as the loading/fallback). PRESERVE verbatim: the z-10 `<h1>` text overlay (the LCP element), the CTAs/`InteractiveButton`s, the stats, the mouse ref, `min-h-[92vh]`, and `style={{ opacity: 'var(--canvas-opacity)' }}` on the canvas layer (1 dark / 0.6 light). The text overlay must NOT move into the canvas — it stays the DOM LCP element painting immediately. Do NOT delete `Hero3DScene.tsx` (additive mandate — it remains as reference/fallback). Thread the existing mouse ref into the scene if HeroScene consumes it.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | (grep -iE "Hero3D\.tsx|three/ScenePoster" && exit 1 || exit 0)</automated>
  </verify>
  <done>`Hero3D.tsx` mounts `HeroScene` via `ClientScene` behind the unchanged z-10 text overlay (LCP preserved), `var(--canvas-opacity)` kept, `Hero3DScene.tsx` not deleted. Hero ScenePoster variant fills the container (zero CLS) using only tokens. Type-checks clean; `npm run build` succeeds; `node scripts/check-bundle.mjs` confirms `three` is still absent from shared/text routes after the homepage mount.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Hero LCP / CLS / both-theme / fallback verification</name>
  <files>(verification only — no source files modified)</files>
  <action>Blocking human-verify checkpoint. Run a PRODUCTION build (`npm run build && npm start`) and walk the user through the verification steps in `how-to-verify`. Do NOT mark complete or unlock downstream waves until the user types "approved".</action>
  <what-built>The signature WebGL hero (icosahedron + GPU particle field) mounted on the homepage via the Phase-4 boundary, behind the unchanged text overlay, with poster-first LCP, theme-reactive colors, and reduced-motion/no-WebGL/off-screen gating.</what-built>
  <how-to-verify>
    Run a PRODUCTION build (not dev — dev misleads LCP): `npm run build && npm start`, then on `http://localhost:3000/`:
    1. **LCP/CLS (mobile):** Lighthouse (CLI if 05-00 found it, else DevTools panel, mobile + throttled) → confirm LCP ≤ 2.5s, CLS ≤ 0.1 (target 0), and the LCP element is the hero `<h1>`/poster — NOT `<canvas>`.
    2. **Both themes:** toggle theme → the icosahedron edge color visibly changes (dark #60a5fa → light #3b82f6) and the scene reads correctly in light mode (not a dark-only scene).
    3. **Reduced motion:** OS/DevTools "Emulate prefers-reduced-motion: reduce" → the static poster shows, no animation, page fully readable.
    4. **No WebGL:** disable WebGL (DevTools/flag) → poster fallback, no crash/white-screen.
    5. **Gating:** DevTools Performance — scroll the hero off-screen and switch tabs → the frameloop stops (no render).
    6. **Brand continuity:** the scene still reads as the icosahedron motif (an evolution, not a rebrand) and the text/CTAs/stats are unchanged.
  </how-to-verify>
  <verify>Human approval after the 6 checks pass on a production build. No automated command — the LCP/CLS/theme/fallback judgments are inherently manual (the build itself is gated automated in Task 3).</verify>
  <done>User typed "approved": LCP ≤ 2.5s, CLS ~0, LCP element = text/poster, both themes correct, reduced-motion + no-WebGL fallback to poster, off-screen/tab-blur pause confirmed, brand motif preserved.</done>
  <resume-signal>Type "approved" to unlock the downstream component waves, or describe any LCP/CLS/theme/fallback issue to fix.</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` + `npm run lint` clean on `src/components/three/Hero*` and `Hero3D.tsx`.
- `npm run build` succeeds; `node scripts/check-bundle.mjs` passes (three absent from shared/text routes with the homepage scene mounted).
- `rg "HeroParticles|Icosahedron|useThemeColors" src/components/three/HeroScene.tsx` → all match.
- `rg "ClientScene" src/components/Hero3D.tsx` → match; `rg "var\(--canvas-opacity\)" src/components/Hero3D.tsx` → still present.
- No per-frame CPU position writes in HeroParticles (`rg "positions\[" src/components/three/HeroParticles.tsx` → no per-index frame mutation).
- Checkpoint Task 4 approved (LCP ≤ 2.5s, CLS ~0, both themes, reduced-motion + no-WebGL fallback, off-screen pause).
</verification>

<success_criteria>
- Hero is a signature WebGL scene themed via CSS vars, with poster LCP element (≤2.5s, ~0 CLS), reduced-motion static fallback, and capability/WebGL fallback (VIS-01).
- All hero loops pause off-screen + tab-blur + under reduced-motion; DPR clamped [1,2] via Phase-4 SceneCanvas (PERF-02).
- three stays route-split; brand motif preserved as an evolution; text overlay unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/05-component-upgrades/05-01-SUMMARY.md`. Record: final particle count, bloom on/off, how ClientScene mounted HeroScene (injection mechanism / parameterization), measured LCP/CLS from the checkpoint, and whether the mouse-parallax ref was threaded from Hero3D.
</output>
