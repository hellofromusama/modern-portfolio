---
phase: 05-component-upgrades
plan: 06
type: execute
wave: 4
depends_on: ["05-03"]
files_modified:
  - src/components/AnimatedIcons.tsx
  - src/components/FundMeWidget.tsx
autonomous: true
requirements: [VIS-06, VIS-07]
user_setup: []
must_haves:
  truths:
    - "AnimatedIcons keeps its efficient CSS-keyframe cross-fade and respects prefers-reduced-motion; stroke colors are tokenized (currentColor/tokens)"
    - "FundMeWidget is de-hacked from hardcoded pink/purple/slate to theme tokens while preserving its visual identity (still reads as the fund-me widget)"
    - "FundMeWidget's always-on ping/pulse/bounce/spin animations are tamed and reduced-motion-respecting; the navigation-to-/fund-me behavior is preserved"
  artifacts:
    - path: "src/components/FundMeWidget.tsx"
      provides: "token-driven accent treatment (no pink/purple/slate literals) + gated/reduced-motion animations; fund-me feature intact"
      contains: "var(--"
    - path: "src/components/AnimatedIcons.tsx"
      provides: "CSS-keyframe cross-fade preserved + reduced-motion guard + tokenized strokes"
      contains: "prefers-reduced-motion"
  key_links:
    - from: "src/components/FundMeWidget.tsx"
      to: "globals.css tokens"
      via: "replaces hardcoded pink/purple/blue/slate-900 with --accent-*/--bg-* tokens"
      pattern: "var\\(--"
---

<objective>
Two remaining-17 components (VIS-06/VIS-07). AnimatedIcons is a low-effort elevation (preserve its efficient CSS-keyframe cross-fade, add a reduced-motion guard if missing, tokenize strokes). FundMeWidget is the BIGGEST single token win — it is the least token-conformant component (hardcoded pink/purple/blue gradients + `slate-900` + always-on `animate-ping`/`pulse`/`bounce`/`spin`). De-hack it toward tokens and restraint while PRESERVING its visual identity (it must still read as the playful fund-me widget) and the navigation-to-`/fund-me` behavior. File-disjoint from 05-04 and 05-05.

Purpose: close the two highest-leverage VIS-06/07 component gaps, especially the FundMeWidget de-hack.
Output: edited `AnimatedIcons.tsx`, `FundMeWidget.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-component-upgrades/05-RESEARCH.md
@.planning/phases/05-component-upgrades/05-03-SUMMARY.md

<interfaces>
// motion v12: import { motion, useReducedMotion } from 'motion/react';  (optional for FundMeWidget panel)
// Tokens: --accent-blue/violet/emerald, --bg-card/elevated/secondary/primary, --btn-primary-*, --text-*, --border-*. NOT Tailwind dark:; NOT hardcoded pink/purple/slate.
// Reduced-motion: globals.css has a global kill switch; components additionally check window.matchMedia('(prefers-reduced-motion: reduce)') or useReducedMotion().

<!-- Read at execution (PRESERVE identity): -->
// AnimatedIcons.tsx — 12 MorphIcons via injected CSS keyframe cross-fade between two SVG <g> groups (ai-fade-in/out, 3000ms, alternate); keyframes injected once (ensureKeyframes guarded by element id + typeof document). Only fund-me uses HeartBroken/LoadingSuccess/LockUnlock. KEEP the keyframe approach (already efficient, no JS loop).
// FundMeWidget.tsx — floating 💖 button, pulse-ring/glow, expand panel, HARDCODED pink/purple/blue gradients + slate-900/95 + border-pink-500, imperative DOM heart-rain on click, emoji sparkles, always-on animate-ping/pulse/bounce/spin. Navigates to /fund-me. The least token-conformant component.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: AnimatedIcons — reduced-motion guard + tokenized strokes</name>
  <files>src/components/AnimatedIcons.tsx</files>
  <action>
    **Read first:** the WHOLE current `AnimatedIcons.tsx` (the `ensureKeyframes` injection + the 12 MorphIcons).
    KEEP the CSS-keyframe cross-fade approach verbatim (it's already efficient and gate-free — research §Per-Component). Two light changes:
    1. **Reduced-motion (PERF-04 floor):** ensure the cross-fade respects `prefers-reduced-motion` — add a `@media (prefers-reduced-motion: reduce)` rule into the injected keyframe CSS (or the wrapper) that pauses/freezes the alternate cross-fade to a single static state, so reduced-motion users get a still icon. If the global kill switch already covers it, verify and note it.
    2. **Tokenize strokes (VIS-06):** make the SVG strokes use `currentColor` / token-driven color rather than any hardcoded hex, so icons inherit the surrounding `--text-*`/`--accent-*` context in both themes. Preserve all 12 exported icons + `AnimatedIconShowcase` + the existing consumers (fund-me's 3).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | (grep -i "AnimatedIcons" && exit 1 || exit 0)</automated>
  </verify>
  <done>AnimatedIcons keeps the keyframe cross-fade, adds a `prefers-reduced-motion` freeze, tokenizes strokes to `currentColor`/tokens, preserves all 12 icons + showcase. Type-checks clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: FundMeWidget — token de-hack + tamed/gated animations (identity preserved)</name>
  <files>src/components/FundMeWidget.tsx</files>
  <action>
    **Read first:** the WHOLE current `FundMeWidget.tsx`.
    De-hack toward tokens + restraint while KEEPING the fund-me identity (research §Per-Component → FundMeWidget — "biggest single VIS-06/07 win"):
    1. **Tokens (VIS-07):** replace the hardcoded `slate-900/95` panel bg with `--bg-elevated`/`--bg-card`; replace the `pink/purple/blue` gradients + `border-pink-500` with a token-driven accent treatment (`--accent-violet`/`--accent-blue` gradient, `--border-*`). It should still feel warm/playful and visually distinct as the fund-me widget — preserve the 💖 button, expand panel, glow, and sparkle character — but theme-correct in BOTH themes (today it's dark-hardcoded).
    2. **Tame the motion (PERF-02/PERF-04):** the always-on `animate-ping`/`animate-pulse`/`animate-bounce`/`spin` should be calmed (slower/subtler) and reduced-motion-respecting — gate them behind `useReducedMotion()`/the matchMedia check so reduced-motion users get a still widget. The imperative DOM heart-rain on click + emoji sparkles can stay as a delight on the explicit click action, but should also no-op under reduced motion.
    3. **PRESERVE:** the navigation-to-`/fund-me` behavior, the floating-button placement, and the widget's playful identity. Do NOT touch Stripe/checkout/email logic (out of scope — visual polish only).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | (grep -i "FundMeWidget" && exit 1 || exit 0)</automated>
  </verify>
  <done>FundMeWidget uses tokens (no pink/purple/slate literals), animations tamed + reduced-motion-gated, fund-me identity + /fund-me navigation preserved, Stripe/checkout untouched. Reads correctly in both themes. Type-checks clean.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` + `npm run lint` clean; `npm run build` succeeds.
- `rg "slate-900|pink-500|purple-500|border-pink|from-pink|to-purple" src/components/FundMeWidget.tsx` → no residual hardcoded color literals (all tokens).
- `rg "prefers-reduced-motion|useReducedMotion" src/components/FundMeWidget.tsx src/components/AnimatedIcons.tsx` → both match.
- `rg "/fund-me" src/components/FundMeWidget.tsx` → navigation preserved.
- Manual (05-09 gate): both themes — FundMeWidget reads correctly in light mode (not dark-only), animations calm, reduced-motion → still.
</verification>

<success_criteria>
- AnimatedIcons + FundMeWidget elevated to the new design language, token-conformant, reduced-motion-respecting (VIS-06, VIS-07).
- FundMeWidget de-hacked to tokens with identity + fund-me feature preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/05-component-upgrades/05-06-SUMMARY.md`. Record: the token mapping chosen for the FundMeWidget gradient/panel.
</output>
