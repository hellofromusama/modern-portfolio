---
phase: 05-component-upgrades
plan: 07
type: execute
wave: 5
depends_on: ["05-03", "05-05", "05-06"]
files_modified:
  - src/app/page.tsx
  - src/app/projects/[id]/page.tsx
  - src/app/expertise/page.tsx
  - src/app/services/page.tsx
  - src/app/tech-stack/page.tsx
  - src/app/developer-australia/page.tsx
autonomous: true
requirements: [VIS-07]
user_setup: []
must_haves:
  truths:
    - "Cards and typography on the home, projects/[id], expertise, services, tech-stack, and developer-australia routes conform to the tightened tokens"
    - "Hardcoded glass/color literals (bg-white/[0.0x], border-white/[0.1], raw hex/rgba) on these pages are replaced with tokens"
    - "Card hover-elevation is unified to the standard lift; both themes render correctly (not dark-only)"
    - "On SERVER pages, metadata/JSON-LD/slugs/copy are untouched — only className/style/token surfaces change (SEO-01 locked)"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "home grid cards + skills + stats on token cards, unified hover, type scale (inherits ScrollReveal upgrade)"
      contains: "var(--"
    - path: "src/app/expertise/page.tsx"
      provides: "technicalExpertise + certs as token cards (no % bars); expertise JSON-LD preserved verbatim"
      contains: "var(--"
  key_links:
    - from: "src/app/page.tsx"
      to: "globals.css tokens"
      via: "card surface = bg-card + border-subtle + shadow-card; hover lift via tokens"
      pattern: "var\\(--"
---

<objective>
Token + typography conformance pass (VIS-07) over the first 6 of the 12 routes: `/` (home), `/projects/[id]`, `/expertise`, `/services`, `/tech-stack`, `/developer-australia`. Standardize card surfaces and headings on the Phase-3 tightened tokens, replace residual hardcoded glass/color literals, and unify hover-elevation — both themes correct. SEO guard: on server pages touch ONLY visual surfaces (className/style/token), never metadata/JSON-LD/slugs/copy. Runs in Wave 5 after the components it composes are final (05-03 ScrollReveal, 05-05/06 components). File-disjoint from 05-08 (the other 6 routes).

Purpose: pages conform to the finished component design language; light theme is correct everywhere.
Output: edited 6 page files (visual surfaces only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-component-upgrades/05-RESEARCH.md

<interfaces>
<!-- Card/type convention (research §Typography & Cards Pass): -->
// Card surface: background var(--bg-card); border 1px solid var(--border-subtle); box-shadow var(--shadow-card) (none dark / soft light); rounded-xl/2xl.
// Hover: translateY(-1px..-2px) + border-hover + bg-card-hover (tokens exist). Unify — some cards use hover:translate-x-0.5, some none.
// Headings: font-[family-name:var(--font-space-grotesk)] at tightened type-scale steps; --text-primary/--text-secondary two-tone pattern. Hero clamp text-6xl..9xl preserved.
// Replace: bg-white/[0.0x], border-white/[0.1], raw hex/rgba → tokens.
// SEO GUARD: server pages (projects/[id], expertise, services, tech-stack, developer-australia) carry metadata/JSON-LD — DO NOT alter metadata, JSON-LD, slugs, or copy. Touch className/style only. Home (page.tsx) is a client page.
// Anti-feature: expertise must NOT use percentage skill bars.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Home + projects/[id] + expertise — token cards & type</name>
  <files>src/app/page.tsx, src/app/projects/[id]/page.tsx, src/app/expertise/page.tsx</files>
  <action>
    For EACH file: **read first**, then apply the card/type token convention (research §Typography & Cards) to className/style surfaces ONLY.
    - **`page.tsx` (home, client):** project grid cards + skills + stats → standard token card surface (`--bg-card`/`--border-subtle`/`--shadow-card`/rounded-xl-2xl), unified hover lift, headings on the tightened type scale + two-tone `--text-primary/secondary`. It inherits the ScrollReveal/InteractiveButton upgrades automatically — verify nothing broke. Keep the `accentMap` and all content/grid logic.
    - **`projects/[id]/page.tsx` (server):** detail typography + any cards → tokens. **DO NOT touch** `generateStaticParams`, `generateMetadata`, JSON-LD, slugs, or copy (SEO-01 locked). Visual className/style only.
    - **`expertise/page.tsx` (server):** `technicalExpertise` cards + certifications → token cards; ensure NO percentage skill bars (anti-feature). **Preserve the expertise JSON-LD block verbatim** and all copy. Visual surfaces only.
    Replace any `bg-white/[0.0x]`/`border-white/[0.1]`/raw hex on these pages with tokens so light theme is correct.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | (grep -iE "app/page\.tsx|projects/\[id\]|expertise/page" && exit 1 || exit 0)</automated>
  </verify>
  <done>Home/projects-detail/expertise cards + headings use tokens with unified hover; no hardcoded glass/color literals; expertise has no % bars; all server-page metadata/JSON-LD/slugs/copy untouched. Type-checks clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: services + tech-stack + developer-australia — token cards & type</name>
  <files>src/app/services/page.tsx, src/app/tech-stack/page.tsx, src/app/developer-australia/page.tsx</files>
  <action>
    For EACH (all server pages): **read first**, apply the same card/type token convention to visual surfaces ONLY.
    - **`services/page.tsx`:** service catalog cards → token cards + unified hover + tightened headings.
    - **`tech-stack/page.tsx`:** `techStack`/`features` cards → token cards + headings.
    - **`developer-australia/page.tsx`:** SEO landing cards/type → tokens.
    **SEO GUARD (all three are server pages):** do NOT alter any `export const metadata`, JSON-LD, slugs, or copy. Only className/style/token changes. Replace residual hardcoded glass/color literals with tokens for correct light-theme rendering.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | (grep -iE "services/page|tech-stack/page|developer-australia/page" && exit 1 || exit 0)</automated>
  </verify>
  <done>services/tech-stack/developer-australia cards + headings use tokens with unified hover, no hardcoded literals, metadata/JSON-LD/copy untouched. Type-checks clean.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` + `npm run lint` clean; `npm run build` succeeds (all 6 routes prerender; projects/[id] still generates its slugs).
- `rg "bg-white/\[0|border-white/\[0" src/app/page.tsx src/app/projects/'[id]'/page.tsx src/app/expertise/page.tsx src/app/services/page.tsx src/app/tech-stack/page.tsx src/app/developer-australia/page.tsx` → no residual glass literals.
- `rg "export const metadata|application/ld\+json|generateMetadata|generateStaticParams" src/app/projects/'[id]'/page.tsx src/app/expertise/page.tsx` → unchanged from baseline (SEO preserved — diff shows only className/style lines changed).
- Manual (05-09 gate): visit each route in BOTH themes → cards/type correct, light mode not broken.
</verification>

<success_criteria>
- Cards + typography on home, projects/[id], expertise, services, tech-stack, developer-australia conform to tightened tokens, both themes (VIS-07).
- All SEO assets (metadata, JSON-LD, slugs, copy) preserved on server pages.
</success_criteria>

<output>
After completion, create `.planning/phases/05-component-upgrades/05-07-SUMMARY.md`.
</output>
