---
phase: 05-component-upgrades
plan: 04
type: execute
wave: 4
depends_on: ["05-03"]
files_modified:
  - src/components/Navigation.tsx
  - src/components/ThemeToggle.tsx
autonomous: true
requirements: [VIS-05, VIS-06]
user_setup: []
must_haves:
  truths:
    - "The mobile menu animates open AND close via AnimatePresence (today it mounts/unmounts with no exit)"
    - "Navigation is keyboard-accessible: aria-expanded/aria-controls on the toggle, focus-visible rings, Escape closes, focus trapped while open, background inert/aria-hidden"
    - "Per-link underline is driven by motion variants/layout — NOT direct DOM style mutation"
    - "ThemeToggle knob/icon swap is motion-smoothed while ALL localStorage + data-theme logic is preserved verbatim, with a focus-visible ring"
  artifacts:
    - path: "src/components/Navigation.tsx"
      provides: "scroll-aware glass (kept) + AnimatePresence mobile menu + a11y (aria-expanded/controls, Esc, focus-trap, focus-visible) + motion underline"
      contains: "AnimatePresence"
    - path: "src/components/ThemeToggle.tsx"
      provides: "motion-spring knob/icon; localStorage+data-theme logic intact; focus-visible"
      contains: "data-theme"
  key_links:
    - from: "src/components/Navigation.tsx"
      to: "motion/react"
      via: "AnimatePresence + motion for mobile menu choreography"
      pattern: "AnimatePresence"
    - from: "src/components/ThemeToggle.tsx"
      to: "localStorage / document.documentElement data-theme"
      via: "setAttribute('data-theme', ...) + localStorage.theme preserved"
      pattern: "data-theme"
---

<objective>
Upgrade Navigation (VIS-05) and ThemeToggle (VIS-06) on motion v12. Navigation imports `InteractiveButton` + `ThemeToggle`, so this runs in Wave 4 after the motion primitives (05-03) finalize. Keep the scroll-aware glass; replace the mount/unmount mobile menu with `AnimatePresence` open/close choreography; replace the per-link DOM-mutation underline with motion variants; and add the NET-NEW keyboard accessibility the success criterion requires (aria-expanded/aria-controls, focus-visible, Escape-to-close, focus trap, background inert). ThemeToggle: motion-smooth the knob/icon while preserving ALL localStorage/`data-theme` logic verbatim.

Purpose: VIS-05 polished + accessible navigation; VIS-06 ThemeToggle elevation — both feature-preserving.
Output: edited `Navigation.tsx`, `ThemeToggle.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-component-upgrades/05-RESEARCH.md
@.planning/phases/05-component-upgrades/05-03-SUMMARY.md

<interfaces>
// motion v12: import { motion, AnimatePresence, useReducedMotion } from 'motion/react';
// InteractiveButton (05-03): variant/size/href API unchanged — Nav's Hire-Me CTA uses it as-is.
// ThemeToggle: PRESERVE setAttribute('data-theme', theme) on document.documentElement + localStorage.theme + the `mounted` guard (returns null pre-hydration).
// Tokens: --bg-nav, --border-subtle/default/hover, --text-*, --accent-blue (focus ring). NOT Tailwind dark:.

<!-- Read at execution (PRESERVE behavior): -->
// Navigation.tsx — fixed nav; scroll-aware bg/blur/border at >20px; navItems data (desktop+mobile, incl. the AI Engineering link from Phase 2); active-page detection (isActivePage); per-link hover underline via e.currentTarget.style mutation (REPLACE); mobile menu via isMobileMenuOpen mount/unmount (WRAP in AnimatePresence); renders ThemeToggle + Hire-Me InteractiveButton.
// ThemeToggle.tsx — CSS-transition knob slide (cubic-bezier(0.16,1,0.3,1), 500ms) + sun/moon cross-fade; mounted guard.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Navigation — AnimatePresence mobile menu + a11y + motion underline</name>
  <files>src/components/Navigation.tsx</files>
  <action>
    **Read first:** the WHOLE current `Navigation.tsx` (keep navItems, isActivePage, scroll-aware glass thresholds, ThemeToggle/Hire-Me placement).
    1. **Mobile menu (research §Pattern 4):** wrap the conditional mobile-menu block in `<AnimatePresence>` so it animates OPEN and CLOSE (`initial`/`animate`/`exit` with the signature easing — e.g. height/opacity or slide). Keep `isMobileMenuOpen` state + the nav-item data.
    2. **Accessibility (NET-NEW — VIS-05 + PERF-03 floor, required):** on the hamburger toggle add `aria-expanded={isMobileMenuOpen}` + `aria-controls="mobile-menu"` (give the menu `id="mobile-menu"`); add `Escape`-to-close (keydown listener while open); trap focus within the open menu (focus first item on open, cycle within, restore focus to the toggle on close); set the background content `inert`/`aria-hidden` while the menu is open; add `focus-visible` rings (token `--accent-blue`) to EVERY nav link/button.
    3. **Underline (research):** replace the per-link `onMouseEnter/onMouseLeave` DOM `style.color`/underline mutation with a motion approach — `whileHover`/`variants` for the underline (e.g. a `motion.span` scaleX underline) and token color changes via className/data-state, NOT imperative `e.currentTarget.style`. Keep active-page indication.
    4. Keep the scroll-aware glass (already good) — optionally smooth the bg/blur transition with motion, but don't change the >20px behavior. Honor `useReducedMotion()` (menu still opens/closes, just without the motion flourish).
    PRESERVE all nav items (incl. AI Engineering), the ThemeToggle, and the Hire-Me InteractiveButton CTA.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | (grep -i "Navigation\.tsx" && exit 1 || exit 0)</automated>
  </verify>
  <done>Mobile menu uses `AnimatePresence` (open+close), toggle has `aria-expanded`/`aria-controls`, Escape closes, focus is trapped + restored, background inert while open, focus-visible rings on all links; underline is motion-driven (no `e.currentTarget.style` mutation); all nav items + ThemeToggle + Hire-Me CTA preserved. Type-checks clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: ThemeToggle — motion-spring knob/icon, logic preserved</name>
  <files>src/components/ThemeToggle.tsx</files>
  <action>
    **Read first:** the WHOLE current `ThemeToggle.tsx`.
    Smooth the knob slide + sun/moon swap with motion (research §Per-Component → ThemeToggle): use `motion` for the knob translate (spring or the signature easing) and the icon cross-fade (`AnimatePresence` or animate opacity/rotate/scale between sun and moon). Honor `useReducedMotion()` → instant swap.
    **PRESERVE VERBATIM:** the `mounted` guard (`if (!mounted) return null`), `localStorage.theme` read/write, and `document.documentElement.setAttribute('data-theme', theme)` — the theming contract must not change. Add a `focus-visible` ring (token) for keyboard users (a11y floor). Keep it a pill track + sliding circle (identity preserved), just smoother.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | (grep -i "ThemeToggle" && exit 1 || exit 0)</automated>
  </verify>
  <done>ThemeToggle knob/icon animate via motion (reduced-motion → instant), `mounted` guard + localStorage + `data-theme` setAttribute preserved verbatim, focus-visible ring added. Type-checks clean.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` + `npm run lint` clean; `npm run build` succeeds.
- `rg "AnimatePresence" src/components/Navigation.tsx` → match; `rg "aria-expanded|aria-controls" src/components/Navigation.tsx` → match.
- `rg "e\.currentTarget\.style" src/components/Navigation.tsx` → NO residual imperative underline/color mutation.
- `rg "setAttribute\(.data-theme|localStorage" src/components/ThemeToggle.tsx` → still present (logic preserved).
- `rg "focus-visible" src/components/Navigation.tsx src/components/ThemeToggle.tsx` → both match.
- Manual (05-09 gate): Tab through nav (rings), open/close mobile menu (Esc closes, focus trapped, aria-expanded toggles), both themes.
</verification>

<success_criteria>
- Navigation: refined desktop + mobile menu with open/close choreography, keyboard accessible, focus-visible (VIS-05).
- ThemeToggle elevated on motion with logic preserved (VIS-06).
</success_criteria>

<output>
After completion, create `.planning/phases/05-component-upgrades/05-04-SUMMARY.md`. Record: the focus-trap approach used and how the menu background was made inert.
</output>
