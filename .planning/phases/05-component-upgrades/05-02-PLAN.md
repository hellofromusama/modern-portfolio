---
phase: 05-component-upgrades
plan: 02
type: execute
wave: 3
depends_on: ["05-01"]
files_modified:
  - src/components/InteractiveGlobe.tsx
  - src/components/IdeaNetworkCanvas.tsx
autonomous: true
requirements: [VIS-02, VIS-03, PERF-02]
user_setup: []
must_haves:
  truths:
    - "InteractiveGlobe renders crisp and DPR-correct on retina/DPR-2 (the named retina coordinate bug is fixed via setTransform, no compounding scale)"
    - "IdeaNetworkCanvas no longer re-reads getBoundingClientRect every frame (cached) and its DPR handling is verified correct"
    - "Both canvases read colors from useThemeColors and look correct in BOTH themes (not dark-only)"
    - "Both rAF loops pause off-screen, on tab-blur, and under reduced-motion via useAnimationGate — all existing geometry/interaction preserved"
  artifacts:
    - path: "src/components/InteractiveGlobe.tsx"
      provides: "DPR setTransform fix + useThemeColors tokens + useAnimationGate-gated rAF; POPs/arcs/drag/auto-rotate preserved"
      contains: "setTransform"
    - path: "src/components/IdeaNetworkCanvas.tsx"
      provides: "cached rect + physics polish + useThemeColors tokens + useAnimationGate-gated rAF; 3 node types preserved"
      contains: "useThemeColors"
  key_links:
    - from: "src/components/InteractiveGlobe.tsx"
      to: "src/hooks/useAnimationGate.ts"
      via: "shouldAnimate guards the rAF loop (pause off-screen/tab-blur/reduced-motion)"
      pattern: "useAnimationGate"
    - from: "src/components/IdeaNetworkCanvas.tsx"
      to: "src/hooks/useThemeColors.ts"
      via: "useThemeColors(['--accent-blue','--accent-violet','--accent-amber'...]) replaces hardcoded rgba"
      pattern: "useThemeColors"
---

<objective>
Upgrade the two retained Canvas-2D components (VIS-02 Globe, VIS-03 IdeaNetwork) WITHOUT a WebGL rewrite (research recommendation): fix the named retina DPR coordinate bug, make both theme-reactive via `useThemeColors`, gate both rAF loops with `useAnimationGate` (off-screen + tab-blur + reduced-motion), and polish IdeaNetwork physics — preserving ALL existing geometry, POPs, arcs, drag/auto-rotate, node types, and interaction. File-disjoint from every other Wave-3 plan.

Purpose: VIS-02/VIS-03 + PERF-02 at low risk, fixing the dark-only colors and forever-running loops without touching the look.
Output: edited `InteractiveGlobe.tsx`, `IdeaNetworkCanvas.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-component-upgrades/05-RESEARCH.md
@.planning/phases/05-component-upgrades/05-00-SUMMARY.md

<interfaces>
<!-- Confirm exact shapes against 05-00-SUMMARY.md before importing. -->
// useAnimationGate(ref) → { shouldAnimate, prefersReduced, inView, tabVisible }  (Phase 3)
// useThemeColors(varNames: string[]) → Record<string,string>  (raw hex; re-reads on data-theme; NEVER per-frame)
//   tokens available: --accent-blue, --accent-violet, --accent-emerald (+ confirm --accent-amber exists for the IdeaNetwork sparks; if not, derive amber from a token or add one in Phase-3 token space — but DO NOT add a new color family; reuse the closed accent set where possible).
// Canonical DPR pattern (research Pitfall 1): canvas.width = cssW*dpr; canvas.height = cssH*dpr; ctx.setTransform(dpr,0,0,dpr,0,0) TOGETHER. NEVER ctx.scale repeatedly (accumulates). Cache cssW/cssH; do NOT getBoundingClientRect() per frame.
</interfaces>

<precedent>
<!-- Read at execution: -->
// src/components/InteractiveGlobe.tsx — today: ctx.scale(dpr,dpr) re-applied in a render-effect that re-runs on dimensions/getRadius change → compounding scale (the named bug). 31 POPs, 15 CONNECTIONS arcs, drag-rotate (Pointer Events + setPointerCapture), auto-rotate y+=0.003, ResizeObserver, hardcoded blue/violet rgba.
// src/components/IdeaNetworkCanvas.tsx — DPR capped at 2 (good) but re-reads getBoundingClientRect() every frame (jank); 6 idea + ~54 node + 20 amber spark nodes; mouse attraction + spark orbit; violet/blue/amber rgba literals.
// src/components/Hero3DScene.tsx — already uses ctx.setTransform correctly: mirror that pattern.
// Both currently run their rAF forever with no off-screen/tab-blur pause.
</precedent>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: InteractiveGlobe — DPR fix + theme tokens + gate</name>
  <files>src/components/InteractiveGlobe.tsx</files>
  <action>
    **Read first:** the whole file + `Hero3DScene.tsx` (correct setTransform pattern) + confirm `useAnimationGate`/`useThemeColors` shapes from 05-00-SUMMARY.md.
    1. **DPR fix (research Pitfall 1 — the named bug):** replace the compounding `ctx.scale(dpr, dpr)` path with the canonical pattern: on resize/dimension change set `canvas.width = cssW * dpr; canvas.height = cssH * dpr;` and `ctx.setTransform(dpr, 0, 0, dpr, 0, 0)` TOGETHER (setTransform resets, so no accumulation). Ensure scale is applied exactly once per size change, never re-applied inside the per-frame draw. Cache CSS size from the ResizeObserver; never `getBoundingClientRect()` per frame.
    2. **Theme tokens:** `const colors = useThemeColors(['--accent-blue', '--accent-violet']);` Replace the hardcoded blue/violet RGBA literals with these tokens; interpolate alpha into the resolved hex (parse hex → rgba with the per-segment/back-face alpha the code already computes via its "ALPHA" placeholder). Read colors from state (re-reads on theme change), not per-frame from getComputedStyle.
    3. **Gate (PERF-02):** add a container ref; `const { shouldAnimate } = useAnimationGate(wrapRef);` Guard the rAF: when `!shouldAnimate`, do NOT schedule the next frame (or render a single static frame then stop); resume when it flips true. Keep `cancelAnimationFrame` on unmount.
    PRESERVE all 31 POPs, 15 arcs, slerp/great-circle math, drag-rotate (Pointer Events + setPointerCapture, ±π/2 clamp), auto-rotate, ResizeObserver, `touchAction:"none"`, and the optional `size` prop. Visual identity unchanged — just crisp, theme-correct, and gated.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | (grep -i "InteractiveGlobe" && exit 1 || exit 0)</automated>
  </verify>
  <done>Globe uses `ctx.setTransform(dpr,...)` (no compounding scale), reads colors via `useThemeColors`, gates the rAF via `useAnimationGate`, and preserves all POPs/arcs/drag/auto-rotate/ResizeObserver. No `getBoundingClientRect()` in the frame loop. Type-checks clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: IdeaNetworkCanvas — cache rect + physics polish + theme tokens + gate</name>
  <files>src/components/IdeaNetworkCanvas.tsx</files>
  <action>
    **Read first:** the whole file + confirm hook shapes.
    1. **Cache the rect (research VIS-03 spec):** stop calling `getBoundingClientRect()` every frame — read it once on mount + on resize (ResizeObserver or the existing resize handler) into a ref, and use the cached CSS size in the loop. Verify the DPR-2 cap path uses `canvas.width = cssW*dpr` + `ctx.setTransform` (not repeated `ctx.scale`).
    2. **Theme tokens:** `useThemeColors(['--accent-violet', '--accent-blue', ...amber])` — replace violet/blue/amber RGBA literals with tokens (interpolate alpha into resolved hex). For the amber sparks: if a token exists use it; otherwise reuse the closed accent set rather than introducing a new color family (record the choice).
    3. **Physics polish (research VIS-03):** light tuning of damping/influence — keep velocity damping ×0.995, mouse attraction `1 - dist/180`, spark orbit; tune for smoother settle (no behavior removal). Keep 6 idea + ~54 node + 20 spark nodes, dynamic connections within `min(w,h)*0.22`, traveling pulse dots, 120px cursor glow.
    4. **Gate (PERF-02):** container ref + `useAnimationGate`; pause the rAF when `!shouldAnimate`; resume when true; `cancelAnimationFrame` on unmount preserved.
    Visual identity + all three node types + interaction preserved.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | (grep -i "IdeaNetworkCanvas" && exit 1 || exit 0)</automated>
  </verify>
  <done>IdeaNetwork caches the rect (no per-frame getBoundingClientRect), uses `useThemeColors` for all node/connection colors, gates the rAF via `useAnimationGate`, polishes physics without removing behavior, preserves all node types/connections/interaction. Type-checks clean.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` + `npm run lint` clean on both files.
- `rg "ctx\.setTransform" src/components/InteractiveGlobe.tsx` → match; `rg "getBoundingClientRect" src/components/IdeaNetworkCanvas.tsx` → NOT inside the rAF frame body (only mount/resize).
- `rg "useAnimationGate" src/components/InteractiveGlobe.tsx src/components/IdeaNetworkCanvas.tsx` → both match.
- `rg "useThemeColors" src/components/InteractiveGlobe.tsx src/components/IdeaNetworkCanvas.tsx` → both match.
- `rg "rgba\(96, ?165, ?250|rgba\(139, ?92, ?246" src/components/InteractiveGlobe.tsx src/components/IdeaNetworkCanvas.tsx` → no residual hardcoded accent literals.
- Manual (deferred to 05-09 gate): DPR-2 emulation both themes → crisp; scroll off-screen / switch tab → rAF stops.
</verification>

<success_criteria>
- Globe + IdeaNetwork render crisp/DPR-correct (retina bug fixed), theme-reactive, off-screen+tab-blur paused, DPR capped at 2 (VIS-02, VIS-03, PERF-02).
- Zero geometry/interaction removed (additive/identity-preserving).
</success_criteria>

<output>
After completion, create `.planning/phases/05-component-upgrades/05-02-SUMMARY.md`. Record: the amber-token choice for IdeaNetwork, and confirmation the DPR fix used setTransform.
</output>
