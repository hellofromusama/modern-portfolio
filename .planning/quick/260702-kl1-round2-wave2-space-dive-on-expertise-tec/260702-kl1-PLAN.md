---
phase: 260702-kl1-round2-wave2-space-dive
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/expertise/page.tsx
  - src/app/expertise/expertiseData.ts
  - src/app/expertise/ExpertiseDive.tsx
  - src/app/expertise/ExpertiseExperience.tsx
  - src/app/tech-stack/page.tsx
  - src/app/tech-stack/techStackData.ts
  - src/app/tech-stack/TechStackDive.tsx
  - src/app/tech-stack/TechStackExperience.tsx
  - src/app/developer-australia/page.tsx
  - src/app/developer-australia/developerAustraliaData.ts
  - src/app/developer-australia/DeveloperAustraliaDive.tsx
  - src/app/developer-australia/DeveloperAustraliaExperience.tsx
  - src/app/team/page.tsx
  - src/app/team/TeamDive.tsx
  - src/app/team/TeamExperience.tsx
  - src/app/blog/page.tsx
  - src/app/blog/blogData.ts
  - src/app/blog/BlogDive.tsx
  - src/app/blog/BlogExperience.tsx
autonomous: true
requirements: [DIVE-EXPERTISE, DIVE-TECHSTACK, DIVE-DEVAU, DIVE-TEAM, DIVE-BLOG]

must_haves:
  truths:
    - "Each of /expertise, /tech-stack, /developer-australia, /team, /blog renders the space-dive (WebGL Canvas + floated glass panels) with that page's REAL content shown verbatim."
    - "Every clickable element works: HUD nav, CTAs, blog post links, blog category filters, team social links — no dead buttons, no 404 hrefs."
    - "metadata + JSON-LD on /expertise, /developer-australia, /blog are byte-identical to before (same Metadata export + same schema objects); /tech-stack and /team gain no metadata (they had none)."
    - "Each route has an sr-only crawlable real-DOM copy of its core text (AISeoContent style)."
    - "npm run build succeeds under strict type/lint checking with all 5 routes present."
  artifacts:
    - path: "src/app/expertise/ExpertiseExperience.tsx"
      provides: "stops[] built from technicalExpertise + certifications, renders <SpacePageShell>"
      contains: "SpacePageShell"
    - path: "src/app/expertise/expertiseData.ts"
      provides: "exported technicalExpertise + certifications (single source for schema, sr-only, dive)"
      contains: "export const technicalExpertise"
    - path: "src/app/tech-stack/TechStackExperience.tsx"
      provides: "stops[] from techStack + features, renders <SpacePageShell>"
      contains: "SpacePageShell"
    - path: "src/app/developer-australia/DeveloperAustraliaExperience.tsx"
      provides: "stops[] from cities + regional copy, renders <SpacePageShell>"
      contains: "SpacePageShell"
    - path: "src/app/team/TeamExperience.tsx"
      provides: "reuses <TeamSection/> verbatim in an interactive stop, renders <SpacePageShell>"
      contains: "TeamSection"
    - path: "src/app/blog/BlogExperience.tsx"
      provides: "blogPosts cards + working links + client-side category filter, renders <SpacePageShell>"
      contains: "SpacePageShell"
  key_links:
    - from: "src/app/*/page.tsx (server)"
      to: "src/app/*/*Dive.tsx"
      via: "render <XDive/> after sr-only + JSON-LD"
      pattern: "Dive"
    - from: "src/app/*/*Dive.tsx"
      to: "src/app/*/*Experience.tsx"
      via: "dynamic(() => import('./XExperience'), { ssr: false, loading: ScenePoster })"
      pattern: "ssr: false"
    - from: "src/app/*/*Experience.tsx"
      to: "@/components/three/space/SpacePageShell"
      via: "<SpacePageShell stops={STOPS} scrollVh={...} />"
      pattern: "SpacePageShell"
    - from: "src/app/blog/BlogExperience.tsx & src/app/team/TeamExperience.tsx"
      to: "SpaceStop.interactive"
      via: "mark link-dense/filter/social stops interactive: true so they settle"
      pattern: "interactive: true"
---

<objective>
Apply the ALREADY-BUILT space-dive (via the reusable `<SpacePageShell>`) to 5 content
pages — /expertise, /tech-stack, /developer-australia, /team, /blog — reusing each
page's EXISTING content VERBATIM. Every click works; SEO (metadata + JSON-LD + a new
sr-only crawlable copy) is preserved.

Purpose: Round 2 Wave 2 — extend the proven Wave-1 Services/Contact dive pattern to
the remaining content pages without rebuilding the engine or touching the shell/leaf
components (reuse by import only).

Output: Per route, a thin server `page.tsx` (metadata + JSON-LD byte-identical +
sr-only copy + thin Dive loader), a `"use client"` `*Dive.tsx` (dynamic ssr:false +
ScenePoster), and a `*Experience.tsx` that builds a `stops[]` config from the page's
real content and renders `<SpacePageShell>`. Plus small co-located data modules so the
schema/sr-only and the dive share ONE verbatim source of truth.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md

# PROVEN Wave-1 pattern to copy exactly (server page → thin Dive → Experience → shell):
@src/app/services/page.tsx
@src/app/services/ServicesDive.tsx
@src/app/services/ServicesExperience.tsx
@src/app/contact/ContactExperience.tsx   # note `interactive: true` on the form stop

# The reusable shell + contract (DO NOT MODIFY — import only):
@src/components/three/space/SpacePageShell.tsx
@src/components/three/space/shellSpec.ts
@src/components/three/space/ShellContent.tsx
@src/components/three/space/space-dom.css
@src/components/three/space/SpaceContent.tsx   # see TeamContent() — how /space floated <TeamSection/>

# SEO helper to model the sr-only copy on:
@src/components/AISeoContent.tsx

# Target pages — reuse their content VERBATIM:
@src/app/expertise/page.tsx
@src/app/tech-stack/page.tsx
@src/app/developer-australia/page.tsx
@src/app/team/page.tsx
@src/components/TeamSection.tsx
@src/app/blog/page.tsx

<interfaces>
<!-- Executor should use these directly — no codebase exploration needed. -->

From src/components/three/space/shellSpec.ts:
```typescript
export interface ShellPlanet { texture: string; radius: number; tint: number | null; ring?: boolean; }
export interface SpaceStop {
  id: string;
  label: string;          // "" = no HUD nav link (like the hero)
  anchor: number;         // 0..1 arrival point along the dive
  position: [number, number, number];
  planet?: ShellPlanet | null;   // omit = no planet at this stop
  contentWidth: string;   // explicit CSS width for the <Html> panel
  content: ReactNode;
  interactive?: boolean;  // form/CTA/link-dense/filter stops SETTLE (damp sway+parallax) so controls are clickable
}
// Standard dive maps anchor -> z to match CameraRig. Use for EVERY stop position.
export function anchorToPosition(anchor: number, x = 0, y = 1): [number, number, number];
```

From src/components/three/space/SpacePageShell.tsx (default export):
```typescript
function SpacePageShell(props: { stops: SpaceStop[]; scrollVh?: number }): JSX.Element;
// Owns the single fixed WebGL Canvas + scroll driver + HUD + loader. Stops with a
// non-empty label become HUD nav links. Stops with interactive:true settle the camera.
```

From src/components/three/space/spaceSpec.ts:
```typescript
export const ACCENT_CYCLE = ["#60a5fa", "#a78bfa", "#34d399", "#fbbf24"] as const;
```

Available planet textures in public/space/ (assign per stop; tint is a THREE hex or null):
  /space/2k_earth_daymap.jpg   (tint 0x5a9bff, radius ~6.5 — good hero planet)
  /space/2k_jupiter.jpg        (tint 0xe0a86a, radius ~7.5)
  /space/2k_neptune.jpg        (tint 0x3a6ff0, radius ~5.0)
  /space/2k_saturn.jpg         (tint 0xd8c79a, radius ~4.4, ring: true)
  /space/2k_mars.jpg           (tint 0xc1440e, radius ~4.0)
  /space/2k_venus_surface.jpg  (tint 0xe8c98a, radius ~4.0)
  /space/2k_mercury.jpg        (tint 0x9c8a7a, radius ~3.4)
  /space/2k_moon.jpg           (tint null,     radius ~2.4)
  (2k_stars_milky_way.jpg is the background — the shell already uses it; do not add as a planet.)

Glass/typography CSS classes already defined in space-dom.css (use these; DO NOT add CSS):
  Panels:    space-panel, space-hero, space-wide, space-section-head
  Type:      space-mono, space-eyebrow, space-hint, space-h1, space-h2, space-sub, space-body
  Chips:     space-chips, space-chip
  Grids:     space-grid, space-grid-skills, space-grid-projects
  Cards:     space-card, space-card-label, space-card-list, space-proj-card, space-proj-title, space-proj-desc
  CTA/links: space-cta, space-links, space-footer
  Team scope: space-team  (transparent-izes TeamSection's <section> + neutralizes its sticky column)
</interfaces>
</context>

<pattern_notes>
Apply this template to EVERY route (mirror Services/Contact byte-for-byte in shape):

1. `page.tsx` (server component, no "use client"):
   - KEEP the existing `export const metadata` and every JSON-LD `<script>` (schema objects)
     BYTE-IDENTICAL. Move only the data arrays into a co-located `*Data.ts` module and
     import them so the SAME values feed schema + sr-only + dive (single source of truth →
     guarantees identical JSON-LD output).
   - Add an `sr-only` crawlable real-DOM copy of the page's core text, modeled on
     AISeoContent (question-form headings + the same data rendered as lists; include the
     real CTA `<a href="/contact">`). This replaces the visible DOM the canvas now hides.
   - Render the thin `<XDive />` last.

2. `XDive.tsx` (`"use client"`): copy ServicesDive.tsx verbatim, swapping the import:
   `const XExperience = dynamic(() => import("./XExperience"), { ssr: false, loading: () => <ScenePoster variant="hero" /> });`
   wrapped in `<main style={{ background: "#05060a" }}>`.

3. `XExperience.tsx` (`"use client"`): import `SpacePageShell`, `anchorToPosition`,
   `type SpaceStop`, `ACCENT_CYCLE`, and the route's `*Data.ts`. Build `const STOPS: SpaceStop[]`
   from the REAL content (verbatim text), spreading `anchor` evenly across 0..1, assigning
   planet textures from the list above (stops beyond the texture set may omit `planet`), and
   `position: anchorToPosition(anchor, 0, 1)`. `contentWidth`: NARROW = "min(92vw, 640px)" for
   hero/CTA, WIDE = "min(92vw, 1100px)" for grids. Return `<SpacePageShell stops={STOPS}
   scrollVh={S} />` where `S = Math.max(420, STOPS.length * 110)`.

Rules for ALL routes:
- Additive only. DO NOT edit any file under src/components/three/space/ or the shell leaf
  components, and DO NOT change any content TEXT.
- Mark link-dense / interactive stops (forms, filters, social-link grids) `interactive: true`.
- Every href must resolve to an existing route (verify: no 404s). Single CTAs (→ /contact) do
  not need `interactive: true` (they click fine, as proven by Services hero/CTA).
</pattern_notes>

<tasks>

<task type="auto">
  <name>Task 1: /expertise space-dive</name>
  <files>src/app/expertise/expertiseData.ts, src/app/expertise/page.tsx, src/app/expertise/ExpertiseDive.tsx, src/app/expertise/ExpertiseExperience.tsx</files>
  <action>
    Implements DIVE-EXPERTISE. Follow &lt;pattern_notes&gt;.
    - expertiseData.ts: extract `technicalExpertise` (Record&lt;string, ExpertiseCategory&gt;) and
      `certifications`, plus the `Technology`/`Sector`/`ExpertiseCategory` interfaces, VERBATIM from
      the current page. Export all. (Pure module, no "use client".)
    - page.tsx: import from expertiseData. Keep `metadata` and the `expertiseSchema` JSON-LD object
      + `<script>` BYTE-IDENTICAL (same knowsAbout/hasCredential/hasOccupation computed from the
      imported arrays). Replace the visible sections with an `sr-only` copy: h1 "Technical Expertise
      &amp; Specializations", the hero paragraph + the 4 stat figures (50+/8+/20+/5), then per category
      an h2 (category) + description + each technology/sector name/level/experience/projects with its
      specializations|applications and achievements as lists, then the certifications list, then the
      CTA `<a href="/contact">`. Render `<ExpertiseDive />` last.
    - ExpertiseExperience.tsx: STOPS = hero (earth) + one stop per `Object.entries(technicalExpertise)`
      category (render technologies OR sectors as `space-card`s using space-grid-projects; reuse
      name/level/experience/projects/specializations|applications/achievements verbatim; cycle
      ACCENT_CYCLE for card accents) + a certifications stop (space-grid-skills of cert cards) + a CTA
      stop (space-panel with `<a class="space-cta" href="/contact">Schedule Expert Consultation →</a>`).
      Assign planet textures cycling the list; give each category stop a short HUD `label`. No stop
      needs `interactive` (only single CTAs). Render `<SpacePageShell stops={STOPS} scrollVh={...} />`.
  </action>
  <verify>
    <automated>cd "C:/Users/Usama.Javed/Desktop/modern-portfolio" && npx tsc --noEmit && npx eslint src/app/expertise</automated>
  </verify>
  <done>tsc + eslint clean; /expertise renders the dive; metadata + expertiseSchema JSON-LD unchanged; sr-only copy present; CTA links to /contact.</done>
</task>

<task type="auto">
  <name>Task 2: /tech-stack space-dive</name>
  <files>src/app/tech-stack/techStackData.ts, src/app/tech-stack/page.tsx, src/app/tech-stack/TechStackDive.tsx, src/app/tech-stack/TechStackExperience.tsx</files>
  <action>
    Implements DIVE-TECHSTACK. Follow &lt;pattern_notes&gt;.
    - techStackData.ts: extract the `techStack` (4 categories, each with category/icon/technologies[])
      and `features` (6) arrays VERBATIM from the current page (currently declared inside the
      component). Export both. (Pure module.)
    - page.tsx: server component (keep it server; it has NO metadata and NO JSON-LD today — add
      NEITHER, so SEO stays byte-identical). Import from techStackData. Add an `sr-only` copy: h1
      "Tech Stack" + subtitle "Behind the Scenes of This Portfolio" + the intro paragraph, then per
      techStack category an h2 (category) with each technology name + description as a list, then a
      "Key Features &amp; Capabilities" h2 with each feature title + description. Drop the old inline
      `<nav>` (the dive HUD replaces page chrome). Render `<TechStackDive />` last.
    - TechStackExperience.tsx: STOPS = hero (earth: "Tech Stack" + subtitle + intro) + one stop per
      techStack category (technologies as `space-card`s, using each tech's `color` class on the title
      verbatim) + a features stop (space-grid-skills of the 6 features) + a CTA stop (space-panel with
      the two links verbatim: `<a href="/contact">Discuss Your Project</a>` and
      `<a href="/#projects">View More Projects</a>` as space-cta). Assign planet textures cycling the
      list; short HUD labels per stop. Render `<SpacePageShell ... />`.
  </action>
  <verify>
    <automated>cd "C:/Users/Usama.Javed/Desktop/modern-portfolio" && npx tsc --noEmit && npx eslint src/app/tech-stack</automated>
  </verify>
  <done>tsc + eslint clean; /tech-stack renders the dive; no metadata/JSON-LD added; sr-only copy present; both CTA links resolve.</done>
</task>

<task type="auto">
  <name>Task 3: /developer-australia space-dive</name>
  <files>src/app/developer-australia/developerAustraliaData.ts, src/app/developer-australia/page.tsx, src/app/developer-australia/DeveloperAustraliaDive.tsx, src/app/developer-australia/DeveloperAustraliaExperience.tsx</files>
  <action>
    Implements DIVE-DEVAU. Follow &lt;pattern_notes&gt;.
    - developerAustraliaData.ts: extract the `cities` array (10 entries: name/state/desc) VERBATIM.
      Export it. Keep the `schema` + `faqSchema` objects in page.tsx (they are built from static
      literal text, not from cities). (Pure module.)
    - page.tsx: import cities. Keep `metadata`, `schema`, and `faqSchema` + both `<script>` tags
      BYTE-IDENTICAL. Replace the visible article with an `sr-only` copy of its verbatim text: h1
      "Best Developer in Australia (2026)", the lede paragraph, the "Why Hire a Perth Developer..."
      section (paragraph + 4 bullets), the "Developer Services Available Across Australia" section
      (each city "{name}, {state}" + desc), the "Key Project Results" 5 bullets, and the closing CTA
      `<a href="/contact">Book Free Consultation</a>`. Drop the old inline `<nav>`. Render
      `<DeveloperAustraliaDive />` last.
    - DeveloperAustraliaExperience.tsx: STOPS = hero (earth: h1 + byline + lede) + a "Why hire a Perth
      developer" stop (paragraph + bullets as space-card-list) + a cities stop (space-grid-projects of
      the 10 city cards, verbatim) + a "Key Project Results" stop (space-card-list of the 5 results) +
      a CTA stop (space-panel with the Book Free Consultation link). Assign planet textures cycling the
      list; short HUD labels. Render `<SpacePageShell ... />`.
  </action>
  <verify>
    <automated>cd "C:/Users/Usama.Javed/Desktop/modern-portfolio" && npx tsc --noEmit && npx eslint src/app/developer-australia</automated>
  </verify>
  <done>tsc + eslint clean; /developer-australia renders the dive; metadata + Article + FAQPage JSON-LD unchanged; sr-only copy present; CTA resolves.</done>
</task>

<task type="auto">
  <name>Task 4: /team space-dive (reuse TeamSection verbatim)</name>
  <files>src/app/team/page.tsx, src/app/team/TeamDive.tsx, src/app/team/TeamExperience.tsx</files>
  <action>
    Implements DIVE-TEAM. Follow &lt;pattern_notes&gt;.
    - page.tsx: convert to a SERVER component (remove "use client"; it had no metadata, so SEO stays
      byte-identical — add none). Add an `sr-only` copy of the team page copy verbatim: eyebrow "Team",
      h1 "Our team." and "Meet the talented individuals who make our vision a reality". Render
      `<TeamDive />` last. (The old inline team grid is replaced by the reused <TeamSection/> below.)
    - TeamDive.tsx: copy the ServicesDive shape (dynamic import ./TeamExperience, ssr:false, ScenePoster).
    - TeamExperience.tsx: import `TeamSection` from "@/components/TeamSection" and reuse it VERBATIM,
      wrapped exactly like /space's TeamContent: `<div className="space-team space-wide"><TeamSection /></div>`.
      STOPS = hero (earth: eyebrow "Team" + h1 "Our team." + the subtitle) + a team stop containing that
      wrapped `<TeamSection />` with `interactive: true` (so the panel settles and the LinkedIn/X/GitHub
      social `<a target="_blank">` links are clickable — the "#" placeholder socials on the two juniors
      are inert by design, that is unchanged). Give the team stop a "Team" HUD label. Render
      `<SpacePageShell stops={STOPS} scrollVh={...} />`.
  </action>
  <verify>
    <automated>cd "C:/Users/Usama.Javed/Desktop/modern-portfolio" && npx tsc --noEmit && npx eslint src/app/team src/components/TeamSection.tsx</automated>
  </verify>
  <done>tsc + eslint clean; /team renders the dive with <TeamSection/> floated verbatim; team stop is interactive; Usama's linkedin/twitter/github links open; TeamSection.tsx unedited.</done>
</task>

<task type="auto">
  <name>Task 5: /blog space-dive (working post links + interactive category filter)</name>
  <files>src/app/blog/blogData.ts, src/app/blog/page.tsx, src/app/blog/BlogDive.tsx, src/app/blog/BlogExperience.tsx</files>
  <action>
    Implements DIVE-BLOG. Follow &lt;pattern_notes&gt;.
    - blogData.ts: extract `blogPosts` (6) and `categories` VERBATIM. Export both. (Pure module.)
    - page.tsx: import from blogData. Keep `metadata` and the `blogSchema` JSON-LD + `<script>`
      BYTE-IDENTICAL (blogPost list computed from the imported array). Replace the visible sections
      with an `sr-only` copy: h1 "Web Development Insights &amp; Expert Guides" + the intro, the three
      "Quick Answers" Q&amp;A verbatim, and each post's title + excerpt + category + readTime as an
      article list. Render `<BlogDive />` last.
    - BlogExperience.tsx (client, holds `useState` for the active category): STOPS = hero (earth) +
      a "Quick Answers" stop (3 cards verbatim) + a featured-post stop (the `featured` post, verbatim)
      + a posts stop that renders the category filter buttons (from `categories`, verbatim labels
      "{name} ({count})") wired to `setActiveCategory` (client-side filter of the visible cards) AND the
      filtered `blogPosts` as cards; mark THIS stop `interactive: true`. Make every post card a real
      working `<a>`: WORKING-LINKS RULE — the only real article routes are /blog/best-developer-perth
      and /blog/ai-developer-perth; link the "Complete Guide to Hiring..." featured post →
      /blog/best-developer-perth and the "How to Integrate AI..." post → /blog/ai-developer-perth; for
      posts without a dedicated page, link the card to `/contact` (a working CTA). NO href may 404.
      Add a final newsletter/CTA stop (the "Stay Updated" copy verbatim; the email input + Subscribe
      may stay as-is presentational). Keep Blog JSON-LD in page.tsx (not here). Assign planet textures
      cycling the list; short HUD labels. Render `<SpacePageShell ... />`.
  </action>
  <verify>
    <automated>cd "C:/Users/Usama.Javed/Desktop/modern-portfolio" && npx tsc --noEmit && npx eslint src/app/blog</automated>
  </verify>
  <done>tsc + eslint clean; /blog renders the dive; blogSchema + metadata unchanged; category filter toggles visible cards; every post card links to an existing route (no 404); posts stop is interactive; sr-only copy present.</done>
</task>

<task type="auto">
  <name>Task 6: Full build + click/SEO audit (all 5 routes)</name>
  <files>(verification only — no new files)</files>
  <action>
    Final gate for the whole plan.
    1. Run `npm run build` (strict type + lint checking is enforced — the ignore blocks were removed
       in Phase 6). Build MUST exit 0 and the route table MUST list /expertise, /tech-stack,
       /developer-australia, /team, /blog.
    2. Confirm each `page.tsx` still renders its `<XDive />` and (where applicable) its unchanged
       JSON-LD `<script>` + `metadata`. Grep the 3 SEO routes to confirm the schema objects are intact:
       expertiseSchema, schema+faqSchema, blogSchema.
    3. Click/href audit: grep the 5 Experience files for `href=` and confirm every target is an
       existing route (/, /#projects, /contact, /blog/best-developer-perth, /blog/ai-developer-perth,
       and external http(s) socials). No href may point to a non-existent local route.
    4. Confirm each route has an `sr-only` block in its page.tsx.
    5. Confirm NO file under src/components/three/space/ and no shell leaf component was modified
       (git status / diff shows only the 19 files in files_modified).
  </action>
  <verify>
    <automated>cd "C:/Users/Usama.Javed/Desktop/modern-portfolio" && npm run build</automated>
  </verify>
  <done>npm run build exits 0 with all 5 routes present; JSON-LD + metadata intact on the 3 SEO routes; all hrefs resolve; sr-only present per route; space engine/shell files untouched.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean after each route task and `npm run build` exit 0 at the end.
- `npx eslint {route dir}` clean per task.
- Byte-identical SEO: expertiseSchema (/expertise), schema + faqSchema (/developer-australia),
  blogSchema (/blog), plus each unchanged `metadata` export.
- Every dive renders `<SpacePageShell>`; blog + team have `interactive: true` stops.
- Every href resolves (no 404); TeamSection.tsx and all src/components/three/space/* unedited.
</verification>

<success_criteria>
- /expertise, /tech-stack, /developer-australia, /team, /blog each render the space-dive with their
  real content floated VERBATIM.
- All clicks work: HUD nav, CTAs, blog post links, blog category filter, team socials.
- metadata + JSON-LD preserved byte-identical; sr-only crawlable copy added per route.
- No new dependencies; engine + shell reused strictly by import; `npm run build` green with all 5 routes.
</success_criteria>

<output>
After completion, create `.planning/quick/260702-kl1-round2-wave2-space-dive-on-expertise-tec/260702-kl1-SUMMARY.md`
</output>
