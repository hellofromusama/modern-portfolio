---
phase: quick-260702-j59
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/three/space/shellSpec.ts
  - src/components/three/space/SpacePageShell.tsx
  - src/components/three/space/ShellContent.tsx
  - src/components/three/space/ShellHUD.tsx
  - src/content/services.ts
  - src/app/services/page.tsx
  - src/app/services/ServicesDive.tsx
  - src/app/services/ServicesExperience.tsx
  - src/app/contact/page.tsx
  - src/app/contact/ContactDive.tsx
  - src/app/contact/ContactExperience.tsx
  - src/app/projects/[id]/page.tsx
  - src/app/projects/[id]/ProjectDive.tsx
  - src/app/projects/[id]/ProjectExperience.tsx
  - .planning/quick/260702-j59-round2-wave1-space-dive-shell-apply-to-s/space-pages-smoke.mjs
autonomous: true
requirements:
  - R2W1-SHELL      # reusable <SpacePageShell> generalized from the working /space engine
  - R2W1-SERVICES   # Services catalog floated as dive stops (existing content verbatim)
  - R2W1-CONTACT    # Contact dive with the REAL working form (focus/type/submit)
  - R2W1-PROJECT    # Project-detail dive, data-driven for ALL project ids, CTAs/liveUrl work
  - R2W1-SEO        # server metadata + JSON-LD preserved + crawlable sr-only real-DOM copy
  - R2W1-SMOKE      # re-runnable Playwright interaction smoke authored in task dir

user_setup: []

must_haves:
  truths:
    - "Visiting /services shows the space dive with the services catalog floating as cards; HUD nav, theme + sound toggles, and click-to-fly all work."
    - "Visiting /contact shows the dive with the REAL contact form; a user can focus a field, type, and submit, and sees the existing success OR error status message."
    - "Visiting /projects/<id> for ANY project id shows the dive with that project's detail (title/subtitle/description/tech/features/results) floating; its CTA links and liveUrl navigate."
    - "Each of the 3 routes still returns server metadata (and the Services JSON-LD is unchanged); /projects/[id] still runs generateStaticParams + generateMetadata."
    - "Each page's core text exists in a crawlable sr-only real-DOM copy so bots can read content the canvas hides."
    - "Faded / invisible floated panels do not intercept clicks; only the visible panel is interactive."
    - "The existing /space experience is unchanged and still works."
  artifacts:
    - path: "src/components/three/space/SpacePageShell.tsx"
      provides: "Generic single-GL-context dive host driven by a stops[] config"
      min_lines: 60
    - path: "src/components/three/space/shellSpec.ts"
      provides: "SpaceStop / ShellPlanet types + anchorToPosition helper"
      contains: "interface SpaceStop"
    - path: "src/components/three/space/ShellContent.tsx"
      provides: "Generic distance-opacity FadingSection floating each stop's content"
      contains: "distanceFactor"
    - path: "src/components/three/space/ShellHUD.tsx"
      provides: "Generic HUD (nav from stop labels + theme/sound toggles + flight gauge)"
      min_lines: 40
    - path: "src/content/services.ts"
      provides: "services + processSteps arrays extracted verbatim, shared by server page + experience"
      contains: "export const services"
    - path: "src/app/services/ServicesExperience.tsx"
      provides: "Services stop-config rendering SpacePageShell"
      contains: "SpacePageShell"
    - path: "src/app/contact/ContactExperience.tsx"
      provides: "Contact dive with the real emailjs form floated verbatim"
      contains: "emailjs"
    - path: "src/app/projects/[id]/ProjectExperience.tsx"
      provides: "Data-driven project-detail stop-config (works for every id)"
      contains: "SpacePageShell"
    - path: ".planning/quick/260702-j59-round2-wave1-space-dive-shell-apply-to-s/space-pages-smoke.mjs"
      provides: "Re-runnable Playwright per-page interaction smoke"
      contains: "chromium"
  key_links:
    - from: "src/app/services/ServicesExperience.tsx"
      to: "src/components/three/space/SpacePageShell.tsx"
      via: "import + <SpacePageShell stops={...}>"
      pattern: "SpacePageShell"
    - from: "src/app/contact/ContactExperience.tsx"
      to: "emailjs.send"
      via: "existing handleSubmit preserved verbatim -> success/error state"
      pattern: "emailjs\\.send"
    - from: "src/app/projects/[id]/ProjectExperience.tsx"
      to: "/contact and project.liveUrl"
      via: "real <a>/<Link> inside floated panels"
      pattern: "liveUrl"
    - from: "src/app/services/page.tsx"
      to: "src/app/services/ServicesDive.tsx"
      via: "server page renders the client ssr:false loader"
      pattern: "ServicesDive"
    - from: "src/components/three/space/ShellContent.tsx"
      to: "panel pointer-events"
      via: "distance-driven opacity + pointerEvents auto/none toggle"
      pattern: "pointerEvents"
    - from: "src/components/three/space/Planet.tsx"
      to: "flyToAnchor"
      via: "planet onClick (click-to-fly), reused verbatim"
      pattern: "flyToAnchor"
---

<objective>
Round 2 / Wave 1: extract a **reusable `<SpacePageShell>`** from the proven `/space` engine, then apply the full 3D-dive design to **3 real routes** — `/services`, `/contact`, `/projects/[id]` — reusing each page's EXISTING content verbatim, with EVERY button / link / form still working, and SEO preserved.

Purpose: Turn the one-off `/space` cosmos into a generic shell any page can adopt, and make three live pages become dives without losing a single interaction or crawlable word.

Output:
- A generic `SpacePageShell` (+ `ShellContent`, `ShellHUD`, `shellSpec`) generalized from `/space`'s leaf components, driven by a `stops[]` config.
- Three route units, each = server page (metadata/JSON-LD/sr-only) + `"use client"` ssr:false loader + client experience building stops from existing content.
- A re-runnable Playwright smoke (`space-pages-smoke.mjs`) in the task dir.

Non-negotiable: `/space` stays byte-identical and working (reuse-by-import only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

# The WORKING engine to generalize (reuse-by-import; DO NOT edit these):
@src/components/three/space/SpaceExperience.tsx
@src/components/three/space/CameraRig.tsx
@src/components/three/space/SpaceContent.tsx
@src/components/three/space/SpaceHUD.tsx
@src/components/three/space/Planet.tsx
@src/components/three/space/spaceSpec.ts
@src/components/three/space/space-dom.css
@src/components/three/space/spaceFonts.ts
@src/components/three/space/SpaceBackground.tsx
@src/components/three/space/Starfield.tsx
@src/components/three/space/SpaceLighting.tsx
@src/components/three/space/SpaceLoader.tsx
@src/app/space/page.tsx

# Target pages (reuse content verbatim):
@src/app/services/page.tsx
@src/app/contact/page.tsx
@src/app/projects/[id]/page.tsx
@src/content/projects.ts
@src/content/types.ts

# SEO patterns to preserve:
@src/components/AISeoContent.tsx
@src/components/three/ScenePoster.tsx

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase. Use directly — no exploration. -->

Reusable leaf components (import AS-IS, unchanged):
  CameraRig({ progress: RefObject<number>, mouse: RefObject<{x,y}>, reduced: boolean, moveLight?: RefObject<THREE.PointLight|null> })
    -> reads CAMERA_START_Z=30 / CAMERA_END_Z=-232 from spaceSpec; publishes --space-scroll.
  SpaceBackground({ reduced })          — BackSide Milky-Way sky sphere
  Starfield({ mouse, reduced })          — 2 foreground THREE.Points layers
  SpaceLighting()                        — ambient+directional+FogExp2
  SpaceLoader()                          — intro "CALIBRATING FLIGHT PATH" overlay
  Planet({ spec: PlanetSpec, reduced })  — real textured planet; onClick -> flyToAnchor(spec.anchor)
  from spaceSpec.ts: PlanetSpec { id, texture, radius, position:[x,y,z], tint:number|null, ring?:boolean, anchor:number }
                     SCROLL_VH=640, FOV=60, CAMERA_START_Z=30, CAMERA_END_Z=-232

PROVEN pattern from SpaceContent.FadingSection (reproduce generically):
  worldZ = stop.position[2]; dist = camera.position.z - worldZ
  if (dist < 2 || dist > 130) op=0
  else if (dist >= 45) op=(70-dist)/25   // fade in
  else if (dist >= 18) op=1              // arrival
  else op=(dist-3)/15                    // fade out
  el.style.opacity = clamp(op); el.style.pointerEvents = op > 0.5 ? "auto" : "none"
  <Html transform occlude={false} position={stop.position} distanceFactor={10}
        pointerEvents="auto" style={{ width: stop.contentWidth }} className="space-html"
        zIndexRange={[0,0]} prepend={false}>

Content sources (reuse verbatim):
  getProject(id): Project | undefined   — Project superset type (see src/content/types.ts)
  projectList: Project[]                 — ALL ids (currently 12); drives generateStaticParams
  ScenePoster({ variant?: "default"|"hero" })  — loading/fallback poster
  Contact form: emailjs.init('5eLu74wM2kSgwd6fT') + emailjs.send('service_gk2ggy2', ...);
    status state "idle"|"success"|"error" — PRESERVE VERBATIM (it uses emailjs, NOT /api/send-email).
</interfaces>

<layering_decision>
KEEP the PROVEN /space layer config (do NOT set the canvas wrapper to pointer-events:none):
  - canvas wrapper: pointerEvents:"auto", zIndex:0, background:#05060a  (so WebGL raycast / click-to-fly works)
  - HUD layer: fixed, zIndex:10, pointerEvents:"none" with pointerEvents:"auto" only on its buttons
  - each floated <Html> panel: pointerEvents auto when visible, toggled to none when faded (FadingSection)
Rationale: a blanket pointer-events:none on the wrapper disables planet raycasting, which would break
click-to-fly — a HARD requirement. The per-panel toggle already prevents faded panels from eating clicks,
which is the real intent of "canvas wrapper pointer-events". This is the config that already works on /space.
</layering_decision>
</context>

<tasks>

<task type="auto" wave="1">
  <name>Task 1: Extract the reusable SpacePageShell (generalize /space)</name>
  <files>src/components/three/space/shellSpec.ts, src/components/three/space/ShellContent.tsx, src/components/three/space/ShellHUD.tsx, src/components/three/space/SpacePageShell.tsx</files>
  <action>
Create a generic dive shell by GENERALIZING SpaceExperience/SpaceContent/SpaceHUD. Reuse the leaf
components (CameraRig, SpaceBackground, Starfield, SpaceLighting, Planet, SpaceLoader) by import,
UNCHANGED. Do NOT edit /space or any existing file. Least-risky path (per task brief): leave
SpaceExperience.tsx AS-IS; the new pages consume the shell.

1) `shellSpec.ts` (pure module, NO "use client"):
   export interface ShellPlanet { texture: string; radius: number; tint: number | null; ring?: boolean }
   export interface SpaceStop {
     id: string; label: string;            // "" = no HUD nav link (like hero)
     anchor: number;                        // t in 0..1 along the dive
     position: [number, number, number];    // world position of the floated panel/planet
     planet?: ShellPlanet | null;           // omit/null = no planet at this stop
     contentWidth: string;                  // explicit CSS width for the <Html> panel
     content: React.ReactNode;
   }
   // Standard dive maps anchor->z to match CameraRig (spaceSpec CAMERA_START_Z=30, CAMERA_END_Z=-232):
   export function anchorToPosition(anchor: number, x = 0, y = 1): [number, number, number] {
     const wz = (30 + anchor * -262) - 34;  // spaceSpec anchor formula (verbatim)
     return [x, y, wz];
   }

2) `ShellContent.tsx` ("use client"): a GENERIC FadingSection that reproduces SpaceContent's exact
   distance-opacity curve + pointerEvents toggle (see <interfaces>), but reads from a SpaceStop instead
   of the hard-wired SECTION_ANCHORS. Default export `ShellContent({ stops }: { stops: SpaceStop[] })`
   maps stops -> <FadingSection key={stop.id} stop={stop} />. Render stop.content inside the inner
   opacity div; use stop.contentWidth for the <Html> width. import "./space-dom.css" already handled by host.

3) `ShellHUD.tsx` ("use client"): a GENERIC copy of SpaceHUD parameterized by nav stops. Signature
   `ShellHUD({ navStops }: { navStops: { id: string; label: string; anchor: number }[] })`. Keep SpaceHUD's
   behavior verbatim: rAF read of --space-scroll -> flight gauge height + percent + active-nav highlight;
   nav buttons call flyTo(anchor) (window.scrollTo smooth); theme-accent toggle (☾/☀, local accent swap);
   ambient-sound toggle (the 3-sine Web Audio drone, off by default) with the same cleanup effect.
   The layer is fixed, zIndex:10, pointerEvents:"none" with auto only on buttons.

4) `SpacePageShell.tsx` ("use client"): GENERALIZE SpaceExperience. Props:
   `{ stops: SpaceStop[]; scrollVh?: number }` (default scrollVh = SCROLL_VH from spaceSpec).
   Body mirrors SpaceExperience EXACTLY except data comes from `stops`:
     - wrap in <div className={spaceFontVars}> and import "./space-dom.css".
     - fixed canvas wrapper: position:fixed, inset:0, background:#05060a, zIndex:0, pointerEvents:"auto".
     - useAnimationGate(wrapRef) -> reduced = prefersReduced; frameloop = inView && tabVisible ? "always":"never".
     - mouse ref (mousemove) + progress ref (scroll) listeners: copy verbatim from SpaceExperience.
     - <Canvas dpr={[1,2]} camera={{ position:[0,0,CAMERA_START_Z], fov:FOV, near:0.1, far:3000 }}>:
         <SpaceLighting/> + camera-tracking <pointLight ref={moveLight} .../> + <CameraRig .../>
         <Starfield mouse reduced/>
         <Suspense fallback={null}>
           <SpaceBackground reduced/>
           {stops.filter(s => s.planet).map(s => <Planet key={s.id} reduced spec={{
              id:s.id, texture:s.planet!.texture, radius:s.planet!.radius, position:s.position,
              tint:s.planet!.tint, ring:s.planet!.ring, anchor:s.anchor }} />)}
           <ShellContent stops={stops} />
         </Suspense>
     - tall scroll driver div: height:`${scrollVh}vh`, pointerEvents:"none", aria-hidden.
     - <ShellHUD navStops={stops.filter(s => s.label).map(({id,label,anchor})=>({id,label,anchor}))} />
     - <SpaceLoader/>.
   FOLLOW <layering_decision> exactly (wrapper auto, HUD none+auto-buttons, panels auto/none). Reuse
   CameraRig AS-IS (its z-range is fixed by spaceSpec — place stops within the standard 30->-232 dive via
   anchorToPosition or explicit positions).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>The 4 shell files exist and type-check with zero NEW tsc errors; SpacePageShell renders sky+planets+stars+lighting+CameraRig dive + ShellContent (drei Html) + ShellHUD + SpaceLoader from a stops[] prop, reusing the /space leaf components unchanged. /space files untouched (git diff clean for src/components/three/space/{SpaceExperience,SpaceContent,SpaceHUD,Planet,CameraRig,spaceSpec,...}).</done>
</task>

<task type="auto" wave="2">
  <name>Task 2: Services dive (catalog floated as stops) + SEO</name>
  <files>src/content/services.ts, src/app/services/ServicesExperience.tsx, src/app/services/ServicesDive.tsx, src/app/services/page.tsx</files>
  <action>
Turn /services into a dive that floats the EXISTING services catalog verbatim, keeping metadata + the
Service JSON-LD + a crawlable sr-only copy.

1) `src/content/services.ts` (pure module, no "use client"): MOVE the `services` and `processSteps`
   arrays out of the current services/page.tsx VERBATIM (byte-for-byte text) and `export const services`
   + `export const processSteps`. This is additive centralization (mirrors src/content/* pattern).

2) `ServicesExperience.tsx` ("use client"): build a `stops: SpaceStop[]` from the imported content and
   render <SpacePageShell stops={stops} />. Distribute anchors evenly (0, .2, .4, .6, .8, 1.0) via
   anchorToPosition. Suggested stops (reuse the SAME copy from content/services.ts — do not reword):
     - hero stop (label:"", narrow width min(92vw,640px)): the page's H1 "Web Development & SEO Services
       I Provide" + the intro paragraph + the two hero CTAs (<a href="/contact">Get Free Consultation</a>
       — keep it a real working link).
     - one stop per service category (label = category, wide width min(92vw,1100px)): float each category's
       service cards (name/description/features/timeframe/priceRange) inside .space-card / glass markup.
     - a process stop (label:"Process"): the 4 processSteps.
     - a CTA stop (label:"Start", narrow): "Ready to Start Your Web Development Project?" + working
       <a href="/contact">Start Your Project Today</a>.
     - optionally a FAQ stop reusing <FAQ/> verbatim (like /space reused <TeamSection/>) — wrap in a
       scoped class if needed so its section bg is transparent (space-dom.css already transparentizes
       `.space-panel section` / add a wrapper if required). Keep it optional; do not break tsc if omitted.
   Give planets to a few stops using /space textures (e.g. /space/2k_earth_daymap.jpg, 2k_jupiter.jpg,
   2k_saturn.jpg with ring:true) — reuse existing public/space assets only.
   Every link inside floated panels must be a real <a>/<Link> (click works).

3) `ServicesDive.tsx` ("use client", line 1): the ssr:false loader (mirror /space/page.tsx):
   const ServicesExperience = dynamic(() => import("./ServicesExperience"),
     { ssr:false, loading: () => <ScenePoster variant="hero" /> });
   export default function ServicesDive(){ return <main style={{background:"#05060a"}}><ServicesExperience/></main>; }

4) `page.tsx` (SERVER — no "use client"): KEEP the existing `export const metadata` and the `serviceSchema`
   JSON-LD <script> VERBATIM (import services from content/services.ts to build it — schema stays
   byte-identical). Render, in order: the JSON-LD script; a crawlable `<div className="sr-only">` real-DOM
   copy (AISeoContent-style: H1 + question-form H2s + the category/service/process text + a real
   <a href="/contact">) so bots read the catalog the canvas hides; then <ServicesDive />.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint src/app/services src/content/services.ts src/components/three/space</automated>
  </verify>
  <done>`npx tsc --noEmit` clean; /services server page still exports metadata + emits the unchanged Service JSON-LD + an sr-only real-DOM catalog copy, then renders the ssr:false ServicesDive; the dive floats the services catalog as fading cards with working /contact CTA links, HUD nav, toggles, and click-to-fly. services/page.tsx JSON-LD data is byte-identical to before (same services array).</done>
</task>

<task type="auto" wave="2">
  <name>Task 3: Contact dive with the REAL working form + SEO</name>
  <files>src/app/contact/ContactExperience.tsx, src/app/contact/ContactDive.tsx, src/app/contact/page.tsx</files>
  <action>
Turn /contact into a dive that floats the REAL contact form. The form + its emailjs submit + status
handling must be preserved VERBATIM and keep working inside the dive.

1) `ContactExperience.tsx` ("use client"): copy the CURRENT contact/page.tsx form + handlers VERBATIM —
   formData state, handleChange, handleSubmit (emailjs.init('5eLu74wM2kSgwd6fT') + the two emailjs.send
   calls + submitStatus "idle"|"success"|"error" + the mailto fallback), copyEmail, and the full form JSX
   (name/email/subject/message inputs, Send button, Copy Email button, success/error status blocks).
   NOTE: the form uses emailjs (NOT /api/send-email) — preserve exactly; do not swap the transport.
   DROP only the page chrome that the dive replaces: <Navigation>, the fixed animated-background divs,
   and the outer page-scroll wrappers. Then build `stops: SpaceStop[]`:
     - hero stop (label:"", narrow): "Get In Touch" heading + intro line.
     - FORM stop (label:"Message", width min(92vw,1100px)): the real <form onSubmit={handleSubmit}> +
       the contact-info column (email copy button + real LinkedIn/GitHub <a target="_blank"> links +
       location + response-time card). Inputs must focus/type; submit must fire emailjs and flip
       submitStatus (success/error block renders). Keep the styled-jsx keyframes block if reused.
   Render <SpacePageShell stops={stops} />. Give 1-2 stops a planet from public/space assets.
   Ensure the form panel keeps pointerEvents:auto while visible (it is the interactive surface).

2) `ContactDive.tsx` ("use client", line 1): ssr:false loader mirroring ServicesDive
   (dynamic(() => import("./ContactExperience"), { ssr:false, loading: () => <ScenePoster variant="hero"/> })).

3) `page.tsx` (SERVER — no "use client"): ADD `export const metadata` (title:"Contact ...", a description)
   — this is a net SEO gain (the old client page had none). Render a crawlable `<div className="sr-only">`
   real-DOM copy: "Get In Touch" + the intro copy + email address + real LinkedIn/GitHub <a> links +
   response-time text. Then render <ContactDive />.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint src/app/contact src/components/three/space</automated>
  </verify>
  <done>`npx tsc --noEmit` clean; /contact is a server page exporting metadata + an sr-only crawlable copy, rendering the ssr:false ContactDive; the floated REAL form accepts focus/typing and, on submit, runs the existing emailjs flow and shows the success OR error status block; LinkedIn/GitHub links and copy-email button work; the emailjs transport and status logic are unchanged from the original page.</done>
</task>

<task type="auto" wave="2">
  <name>Task 4: Project-detail dive (data-driven, all ids) + SEO</name>
  <files>src/app/projects/[id]/ProjectExperience.tsx, src/app/projects/[id]/ProjectDive.tsx, src/app/projects/[id]/page.tsx</files>
  <action>
Turn /projects/[id] into a data-driven dive that floats the project detail for ANY id (currently 12,
data-driven so it covers all). Keep generateStaticParams + generateMetadata + notFound.

1) `ProjectExperience.tsx` ("use client"): signature `ProjectExperience({ project }: { project: Project })`
   (import type Project from "@/content/types"). Build `stops: SpaceStop[]` from the project fields
   VERBATIM (no reworded copy):
     - hero stop (label:"", narrow): category badge + project.title + project.subtitle + project.description
       + timeline/team; if project.liveUrl, a REAL <a href={project.liveUrl} target="_blank"
       rel="noopener noreferrer">Visit Live Website</a> (must navigate).
     - tech stop (label:"Tech"): project.tech chips.
     - overview stop (label:"Overview", wide): project.longDescription (whitespace-pre-line).
     - features stop (label:"Features", wide): project.features list.
     - results/challenges stop(s): project.challenges + project.results.
     - CTA stop (label:"Contact"): real <a href="/contact">Discuss Your Project</a> + <a href="/#projects">
       View More Projects</a> (both must navigate).
   Render <SpacePageShell stops={stops} />. Planets from public/space assets on a couple of stops.
   All CTAs/liveUrl must be real working anchors.

2) `ProjectDive.tsx` ("use client", line 1): ssr:false loader that RECEIVES the project prop and forwards it:
   `export default function ProjectDive({ project }: { project: Project }) {
      const Exp = dynamic(() => import("./ProjectExperience"), { ssr:false, loading: () => <ScenePoster variant="hero"/> });
      return <main style={{background:"#05060a"}}><Exp project={project} /></main>; }`
   (dynamic ssr:false is legal here because this is a client component; the server page cannot do it.)

3) `page.tsx` (SERVER async): KEEP generateStaticParams (projectList) + generateMetadata VERBATIM. In the
   component: await params, `const project = getProject(id); if (!project) notFound();`. Render a crawlable
   `<div className="sr-only">` real-DOM copy of the project (title/subtitle/description/longDescription/
   tech/features/challenges/results + real <a href="/contact"> and, if present, <a href={project.liveUrl}>),
   then `<ProjectDive project={project} />`. Passing the plain Project object from server -> client loader is
   fine (serializable).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint "src/app/projects/[id]" src/components/three/space</automated>
  </verify>
  <done>`npx tsc --noEmit` clean; /projects/[id] server page keeps generateStaticParams + generateMetadata + notFound + an sr-only crawlable project copy, then renders the ssr:false ProjectDive with the fetched project; the dive floats the project detail for every id, and its /contact CTA, /#projects link, and liveUrl (when present) are real working anchors. npm run build prerenders all project slugs.</done>
</task>

<task type="auto" wave="3">
  <name>Task 5: Author the re-runnable Playwright interaction smoke</name>
  <files>.planning/quick/260702-j59-round2-wave1-space-dive-shell-apply-to-s/space-pages-smoke.mjs</files>
  <action>
Author a standalone ESM Playwright script the ORCHESTRATOR runs via the session scratchpad Playwright
(do NOT add playwright to the repo package.json; do NOT run it here — the executor only `node --check`s it).

Requirements for `space-pages-smoke.mjs`:
  - `import { chromium } from "playwright";` const BASE = process.env.BASE_URL || "http://localhost:3000";
  - Launch one browser; run three page checks. For each page: create a page, `await page.bringToFront()`,
    goto the route, wait for the canvas (`await page.waitForSelector("canvas")`) and for the loader to
    fade (wait for a floated panel / give it a fixed settle timeout).
  - Per-page assertions (log PASS/FAIL, exit non-zero on any FAIL):
      /services:
        * click each HUD nav button (buttons in the top glass <nav>); assert scrollY changes or no error.
        * click a real catalog CTA link and assert its href contains "/contact".
        * click the theme toggle (☾/☀) and the sound toggle (♪/♫); assert they toggle without error.
        * click on the <canvas> at a center-ish point (best-effort click-to-fly); assert no crash
          (planets are WebGL — coordinate-click, tolerate scroll change or none; do not hard-fail on it).
      /contact:
        * focus the name input, type a value; focus + type email/subject/message.
        * submit the form (click the Send button); wait for and assert the success OR error status block
          appears (emailjs may succeed or fail in the test env — either status is a PASS).
        * click each HUD nav button; toggle theme + sound.
        * assert a LinkedIn/GitHub <a target="_blank"> href is present.
      /projects/<a valid id, e.g. kashmir-fund>:
        * click the "Discuss Your Project" CTA and assert its href is "/contact" (or click + assert
          navigation to /contact); assert a "/#projects" link exists; if liveUrl rendered, assert its href.
        * click each HUD nav button; toggle theme + sound; canvas click-to-fly best-effort.
  - Close the browser; print a summary; `process.exit(failures ? 1 : 0)`.
  - Make selectors resilient (getByRole/text where possible) and the script idempotent/re-runnable.
  Add a 2-line header comment: how the orchestrator runs it (BASE_URL env + `node space-pages-smoke.mjs`
  against a running dev/prod server) and that playwright comes from the session scratchpad, not the repo.
  </action>
  <verify>
    <automated>node --check ".planning/quick/260702-j59-round2-wave1-space-dive-shell-apply-to-s/space-pages-smoke.mjs"</automated>
  </verify>
  <done>space-pages-smoke.mjs exists in the task dir, passes `node --check` (syntax valid), imports playwright (not added to repo package.json), and per page (bringToFront) clicks each nav link, focuses+types+submits the contact form asserting success/error, clicks a project CTA/link asserting href/navigation, toggles theme+sound, and click-to-fly's a planet best-effort. It is not executed by the executor.</done>
</task>

</tasks>

<verification>
Executor HARD gates (run after all tasks):
- `npx tsc --noEmit` — zero errors (project builds strict since Phase 6 flipped the ignore flags).
- `npx eslint src/app/services "src/app/projects/[id]" src/app/contact src/components/three/space src/content/services.ts` — clean.
- `npm run build` — succeeds; all project slugs prerender; /services, /contact, /projects/[id] compile.
- `node --check` on space-pages-smoke.mjs — syntax valid.
- `/space` regression guard: `git diff --stat` shows NO changes to src/components/three/space/SpaceExperience.tsx, SpaceContent.tsx, SpaceHUD.tsx, Planet.tsx, CameraRig.tsx, spaceSpec.ts, SpaceBackground.tsx, Starfield.tsx, SpaceLighting.tsx, SpaceLoader.tsx, space-dom.css, spaceFonts.ts (they are reused-by-import only).

ORCHESTRATOR (not executor): run space-pages-smoke.mjs via the session scratchpad Playwright against a running server (`BASE_URL=http://localhost:3000 node space-pages-smoke.mjs`) — all per-page assertions PASS.
</verification>

<success_criteria>
- A generic `<SpacePageShell stops={...}>` exists, generalized from /space's leaf components, with `ShellContent` (distance-opacity FadingSection + pointer-events toggle) and `ShellHUD` (nav from stop labels + theme/sound toggles + flight gauge). /space is unchanged.
- /services, /contact, /projects/[id] each render as a working dive reusing existing content VERBATIM.
- Every interaction works: HUD nav, theme + sound toggles, click-to-fly; Services CTAs -> /contact; the REAL contact form focuses/types/submits (emailjs, success/error state); project CTAs + liveUrl navigate; faded panels don't eat clicks.
- SEO preserved: server metadata retained (Services + Projects), Service JSON-LD unchanged, generateStaticParams/generateMetadata intact; each route ships a crawlable sr-only real-DOM copy of its core text.
- tsc + eslint + build all green; smoke script authored and node --check-valid.
</success_criteria>

<output>
After completion, create `.planning/quick/260702-j59-round2-wave1-space-dive-shell-apply-to-s/260702-j59-SUMMARY.md`.
</output>
